**THREE COLORS OF MADNESS**

***Technical Design Document***

**Internal working document. Not for external distribution. Companion to the Design Bible.**

**Dejunai (founder, author/producer; lead on dialogue content specifically) · Claude Sonnet (co-author, co-designer, and revision historian since v1 of both the Design Bible and this document — designed and built the dialogue grammar and interpreter with the author in VS Code; has authored and maintained this document throughout) · Codex (core programmer — live integration of the dialogue system into play, subsequent fixes and extensions to the interpreter and its documentation, and direct review of this document against source) · Antigravity (dependency/gate auditing and dialogue-content execution under the author's lead, added 2026-09-14) · Manus AI (independent outside source-backed repository audit, first pass 2026-09-14, added 2026-09-15) · Perplexity (research support for dialogue content, added 2026-09-14) · Copilot 365 (ideation support for dialogue content, added 2026-09-14)**

## **Contents**

**Part One — What This Document Is**

**Part Two — Current Build**

**Part Three — Architecture As Built**

**Part Four — Architecture Decisions Needed**

**Part Five — Open Production Questions**

**Part Six — Next Steps**

# **Part One — What This Document Is**

**This is the Technical Design Document (TDD) for Three Colors of Madness. It is the companion to the Design Bible, not a replacement for any part of it.**

**The division between the two documents is simple. The bible owns the what and the why — thesis, Design Laws, narrative, the meaning of every mechanic. This document owns the how — engine architecture, data schemas, production sequencing, and the specific open questions a playtest still needs to settle before a mechanism the bible describes gets its final shape. Where a passage in the bible states a mechanism rather than just a rule, that is deliberate: the mechanism itself is what's enforcing a Design Law, and this document does not relitigate it. Where the two documents ever appear to disagree, the bible is correct and this document is wrong until revised — never the reverse.**

**This document is expected to change often and change fast. Its job is to stay honest about the current state of the build, not to be permanently settled — including this document's own mistakes, corrected in place rather than smoothed over.**

**This revision (v39), 2026-09-15, folds in a spot-checked outside audit and an independent cross-check of v38's own portal-system claims — see Part Three, "Outside Audit — Manus AI," for the full detail; summarized in order below.**

**(1) A first-appearance outside tool, "Manus AI," delivered a full source-backed repository audit against commit `23a2269`, claiming direct repo clone access and actual Godot 4.7.2 test execution. Per this document's standing rule, nothing from it is merged on report alone — a representative sample of its most load-bearing and most surprising claims was independently spot-checked against real source (real shell access), not the whole document. Confirmed genuine: `tests/test_placement_audit.gd` has a real parse error at line 55 (a truncated `pri` identifier with nothing after it) and cannot currently run; `AVERAGE JOE` is a shared literal speaker label used across twelve dialogue files (`business_resident_01`–`06`, `pickman_resident_01`–`06`), with a systematic trombone/violin instrument-family split by odd/even file suffix — each file is internally consistent, but the shared label across all twelve is exactly what would trip `tests/instrument_voice_flow.gd` if it keys off the literal speaker string rather than per-NPC identity. Per the author, 2026-09-15: `AVERAGE JOE` is a known placeholder, not a naming decision — replacing it with twelve authentic period-appropriate names is now an open todo, which would also resolve the test collision as a side effect. One claim checked and not confirmed as stated: `route_lower` does not appear in current `tests/town_expansion_flow.gd` as the audit describes — logged as an open discrepancy, not resolved either way. One claim consistent with but not conclusively proving the audit's account: `tests/staging_flow.gd` still asserts the pre-migration `Story.SCENES.boy` pattern against `gatehouse_boy.dialogue`. The `CaseState.VERSION` discrepancy the audit raised (reporting 10 against this document's then-current 11) is explained, not a live bug: the audit's clone was necessarily behind unpublished local commits at the time it ran. See Part Three and Part Four for the open items this spot-check leaves for a future pass.**

**(2) This session's own independent re-verification of the portal authoring system (Part Three, v38) against real source, performed separately from and without reference to the session that authored v38's own account: the `GATE:`/`TIME:` grammar, the once-per-identity charging logic, the GO-flag validation, and all five Codex-confirmed defect fixes described in v38 are independently confirmed present in current source, corroborating v38's claims rather than superseding them. No discrepancies found.**

**This revision (v41), 2026-09-15, folds in four wording/framing refinements from Codex's final pass on v40, each independently re-verified against real source — see `docs/qa/TDD_PENDING_NOTES.md` for the full verification log. Codex called v40 "substantially accurate" and reconfirmed the corrected corpus figures, renamed residents, D1/R2 architecture, portal timing, staging-test status, placement-audit failure, and retired `route_lower` claim; these four refinements were the condition of approval. (1) `tests/town_expansion_flow.gd` reframed: direct read confirms its own `quit()` call sits on the file's very last line, reached only after many more assertions past the line-37 failure — Godot's `assert()` does not exit a headless script on failure, it simply breaks, which is why nothing after "START EXPANSION" ever prints; this is normal `assert()` behavior, not a missing `quit()` call. Whether the line-37 assertion itself is a real save/load defect or an artifact of this session's own restricted `user://` sandbox is not settled either way — recorded as a test-harness/environment issue pending reproduction under normal Godot storage, not a confirmed game/save defect. See Part Four. (2) Telemetry duration fields corrected as not uniformly normalized: `district_transition()` rounds `real_seconds_elapsed` to whole seconds (`roundi`) but `game_minutes_elapsed` to one decimal minute (`snappedf(..., 0.1)`) — two different granularities on sibling fields of the same event; D1 columns are both `REAL` and unaffected either way. See Part Four. (3) Conversation-event game-side semantics recorded: it fires from `chapter_one_dialogue.gd::_segment_done()` only once the complete authored dialogue path finishes — confirmed by an explicit source comment at the call site: menu opens, abandoned card sequences, and intermediate `FORK` segments are not counted as completed conversations. See Part Four. (4) Portal file count wording corrected: ten unique portal ids live across **five** live `.portal` files, not "all six" as v40 stated — the sixth file, `background_portal_template.portal`, is an explicitly-marked cookbook/template ("Examples only: these statements and identifiers are NOT new story canon") containing only `example_*` placeholder ids, none live. See Part Three.**

**This revision (v40), 2026-09-15, folds in eight corrections from Codex's own review of v39, each independently re-verified against real source and, for the first time this pass, by actually running the disputed Godot tests directly rather than relying on static source reading alone — see `docs/qa/TDD_PENDING_NOTES.md` for the full item-by-item verification log. (1) Portal `TIME:` semantics corrected: the v38/v39 language describing an omitted `TIME:` as "leaving `_travel()`'s own automatic `DayClock.travel_cost()` charge alone" was wrong — `portal_lang.gd` always fills an omitted `TIME:` with the string `"3"`, a valid float, so `_perform_go()`'s `else` branch — the one that would use `_travel()`'s own automatic charge — is unreachable in current source; every portal, explicit `TIME:` or not, takes the "charge once on first completion via `portal_done()`" branch. See Part Three. (2) Portal scope corrected: v38/v39's "four scoped travel points" undercounted the system — direct reads of all six `.portal` files confirm ten unique portal ids (`service_entrance`, `exit`, `street_precinct`, `street_almy`, `street_room`, `street_estate`, `route_post`, `lounge_exit`, `route_speakeasy`, `tunnel_exit`), not four. See Part Three. (3) Telemetry/hosting corrected: a direct, full read of `cloudflare/three-colors-worker/src/index.js` (141 lines) confirms the Worker now dual-writes every accepted event batch to both D1 (idempotent `INSERT OR IGNORE`, keyed on a SHA-256 `event_key` hash of the event) and R2 in one `Promise.all`, including a new `conversation` event type (`npc_id`, `topic_id`, `coat_state`, `world`, `day`, `phase`) — this document's Worker\+R2-only account and its "offline script against R2" tradeoff are stale. See Part Four. Codex's own claimed deployed-version id and a live-D1-retrieval test are not independently confirmed this pass (no network access from this session) and are logged as reported-only. (4) Corpus figures corrected: an actual rerun of `tests/dialogue_catalog_flow.gd` returns 438 topics, not 454; an actual rerun of `tests/instrument_voice_flow.gd`, after the `AVERAGE JOE` fix below, returns 705 NPC lines, not 614; the 144-cue manifest is unchanged. See Part Two. (5) `AVERAGE JOE` — RESOLVED: all twelve placeholder speaker labels (`business_resident_01`–`06`, `pickman_resident_01`–`06`) were renamed to authentic period-appropriate names by a separate session (VS Code Claude Sonnet), independently reconfirmed here — zero collisions across the full corpus, and `tests/instrument_voice_flow.gd` now passes clean. (6) `tests/staging_flow.gd` upgraded from "not confirmed failing" to CONFIRMED failing, per Codex's own execution report; this session could not reproduce the exact invocation directly (the script errors when run standalone, needing an integrated `--qa-staging`-style mode not attempted this pass), but the already-confirmed static source match together with Codex's own test run are treated as confirmation. (7) `route_lower`/`tests/town_expansion_flow.gd` — the v39 open discrepancy is closed: the audit's `route_lower` claim still doesn't match current source as stated, reconfirmed this pass, and is retired as stale/misattributed. In its place, a real, newly-identified bug is logged: `tests/town_expansion_flow.gd` hangs indefinitely after printing "START EXPANSION" — the save/load assertion at line 37 (`assert(g.state.estate_visits_completed==1 and not g.state.birch_bodies_removed)`, immediately after `g._save_game()`/`g._load_game()`) is the likely fault line; root cause not yet diagnosed. See Part Four. (8) `tests/test_placement_audit.gd`'s line-55 parse error remains open and unchanged, reconfirmed by direct execution this pass. See Part Four.**

**This revision (v38), 2026-09-14 (later same day), folds in two more items independently confirmed against real source this pass: a fourth flat-file authoring system, and a resolved regression carried forward from v37's own checklist.**

**(1) The portal authoring system — a third sibling to `.dialogue` and `.object`, not an extension of either. A travel point's defining content is where it leads, not what it says, so `scripts/shared/portal_lang.gd`/`portal_runtime.gd`/`portal_state.gd` add one directive neither other format has: `GO: destination | x,y,z | yaw | flags`, a circuit breaker exactly like `FORK` — reaching it halts step processing and hands control to a new adapter, `scripts/chapters/chapter_one_portals.gd`, which is the only thing that actually calls `_travel()`. `case_state.gd` moved to schema version 11, additively (`portal_state` added; save versions 1–10 still validate). Ten unique portal ids are migrated and wired across five live `.portal` files, plus a sixth, example-only template (corrected v40, 2026-09-15, file-count wording corrected v41 — this document previously undercounted the system as four): `service_entrance` and `exit` (estate), `street_precinct`/`street_almy`/`street_room`/`street_estate`/`route_post` (town), `lounge_exit` (lounge), `route_speakeasy` (lower), and `tunnel_exit` (tunnel, two destinations depending on evidence). An independent Codex review, across two passes, found and confirmed-fixed five defects before approving it: a GATE bypass reachable only from the contiguous town's shared exterior (where `state.world` never becomes `"lower"`); an immediate `GO` with no post-arrival cards never actually completing its portal session; an authored `TIME:` charge applied after `_travel()`'s own transition telemetry instead of before; that same authored `TIME:` charging on every crossing instead of once per identity; and unknown `GO` flags parsing silently instead of erroring. See Part Three, "Portal Authoring System," for the full architecture and Part Six for the closed checklist item.**

**(2) A regressed camera-jump-on-menu-close bug, reported by the author and independently confirmed fixed against source: browsers can report the entire pointer movement that happened while the mouse was free as one inflated relative-motion delta on the first sample after pointer lock re-acquires post-menu-close, and can emit more than one such synthetic sample before the next physics frame notices the mouse-mode change — the prior single-input-swallow guard was never sufficient for a multi-sample burst. Fixed with a `RECAPTURE_MOTION_GUARD_SECONDS = 0.12` window that discards every motion sample for its duration, not just the first, confirmed by a new `tests/camera_recapture_flow.gd`. This resolves Part Six item 19, left open since v37.**

**This revision (v37), 2026-09-14, folds in two batches of substance accumulated in `docs/qa/TDD_PENDING_NOTES.md` since v36, both independently confirmed against real source (real shell access this pass, not device-bridge reports or relayed claims) before being merged here, per this document's own standing rule. See Part Three, Four, and Six for the full detail; summarized in order below.**

**(1) The object-evidence authoring system — a genuine architectural replacement, not an extension. Per the author, discovering that evidence like `lodging` lived scattered across GDScript object-interaction scripts, outside the dialogue system, invisible to any dialogue-only audit, and requiring Godot-language fluency the author doesn't have to touch, was "the final straw." The replacement is a flat-file-per-object grammar authored in the same declarative style as `.dialogue` files (`GATE:`/`EVIDENCE:` are identical syntax), with its own parser/runtime/state (`object_lang.gd`/`object_runtime.gd`/`object_state.gd`) and object-specific directives `.dialogue` files don't have, the first being `TAKE: item_id` for pickup/collection (the author's own working name, `COLLECT:`, was superseded by the implementer's `TAKE:` — a naming call, not a discrepancy). `case_state.gd` moved to schema version 10 additively (`inventory`/`object_state` added; save versions 1–9 still validate). All eleven scoped hotspot ids are now migrated and wired: estate's seven (`wounds`, `eight`, `knife`, `watch`, `gas`, `register`, `shoes`), town's three (`gazette`, `lodging`, `exemption`), and tunnel's one (`tunnel_record`), living in real `objects/estate.object`/`objects/town.object`/`objects/tunnel.object` files and dispatched through a new `chapter_one_objects.gd` adapter that `chapter_one.gd` now calls from `_interact()`, `_town_interaction()`/`_town_observation()`, and `_tunnel_interaction()`. Four gaps an independent Codex review found in the first migration pass — FORK/OUTCOME not actually presenting live choices, `LABEL:` not actually driving hover text, `INCLUDE`d files dropping repeated cascade variants, and missing parse-time id/TIME validation — are all confirmed fixed in source. Both systems now fail loud instead of silently: see the new Part Three section below. Two known, accepted limitations carry forward as open architecture items in Part Four: hotspot visibility is one-way only (a hotspot that disappears when its `GATE:` goes false doesn't reappear if the `GATE:` later becomes true again — not a live bug today since every current `GATE:` is one-directional by nature), and unknown `GATE:` fields/functions still silently parse rather than raising an error. Documented in `docs/OBJECT_AUTHORING.md` and `docs/OBJECT_MIGRATION_HOWTO.md`, both independently verified clean against source; corroborated further by the new `docs/ARCHITECTURE.md`. Test coverage: `tests/object_template_flow.gd` (grammar/template), `tests/object_content_flow.gd` (the real shipped `.object` files and a live FORK through the adapter).**

**(2) Fail-loud guards, object-side and dialogue-side — both implemented and confirmed live. Design intent, per the author: stop broken or incomplete gate logic from letting an NPC or object go silently unresponsive (an NPC "totem pole," in the author's words) — surface an obvious on-screen developer error instead. The original proposal was a default no-gate topic appended to every `.dialogue` file reading "EOF Error, Please report to developers"; revised before implementation, on the author's own reconsideration, to a single centralized check instead of one bit of duplicated boilerplate per file across ~70 files today and every future one. The object-side guard lives in `chapter_one_objects.gd::interact()`/`_fail_loud()`: when a hotspot the player can actually click (already present in the location's `.points`) resolves to no currently-eligible `GATE:` block, it `push_error()`s and shows an on-screen "EOF ERROR — PLEASE ALERT THE DEVELOPERS" panel naming the location and object id. It deliberately lives in the chapter adapter rather than the shared `object_lang.gd`/`object_runtime.gd`, because an object file having nothing eligible is often *correct* — the hotspot may simply not exist yet — so a parser-level fallback would misfire on legitimate absence; only the adapter, which already knows the hotspot is clickable, can tell the two cases apart. The dialogue-side guard, confirmed landed in `chapter_one_dialogue.gd::interact()` (mirroring where the object-side guard landed, not the shared `dialogue_runtime.gd`), fires when `Runtime.enter()` returns an empty session *and* the NPC's menu has zero eligible entries *and* the actor isn't `"odell"` (the documented exception, matching Odell's one-shot intake conversation and post-answer notebook dismissal): it `push_error()`s `"DIALOGUE FAIL-SAFE: NPC '%s' has no eligible default or menu topics! (EOF Error)"` and shows a `[DEVELOPER WARNING]` / `"EOF Error: NPC '<npc_id>' has no available dialogue state. Please alert developers."` card. Both guards' documentation (`docs/OBJECT_AUTHORING.md`, `docs/DIALOGUE_AUTHORING.md`, `README.md`) was independently confirmed accurate by a direct, repo-wide grep for the literal error text, which matches nowhere outside the two adapters. See Part Three.**

**(3) The Day 2 montage is removed from new-playthrough progression; the two days it was compressing are now enacted, playable content. Per `docs/qa/MONTAGE_ARCHIVE_PASS.md`, independently verified against source: Day 1 is unchanged. Sleeping ends Day 1 and returns control on Day 2 at 6:00 a.m., which now has real content culminating in the steward's second visit, relocated to the smoking lounge and ending on the verbatim-confirmed line `THE STEWARD: "Come back tomorrow, and don't bring your badge."` Sleeping before that second visit is blocked with a redirect back to the lounge; sleeping after it advances to Day 3 at 6:00 a.m., where the existing plain-coat third steward visit is unchanged. The montage's still renderer, intertitle data, and the `montage_index` save field are deliberately kept, not deleted, solely so a save made mid-montage before this change can still resume correctly — `chapter_one.gd::_load_game()`'s montage-resume branch is gated entirely behind an old save already carrying a set `montage_index`, making it structurally unreachable from a fresh game while remaining fully intact for anyone mid-montage on an existing save. Test coverage extended, not replaced: `tests/steward_sleep_flow.gd`, `tests/day_clock_flow.gd`, `tests/dialogue_live_flow.gd`, `tests/staging_flow.gd`, `tests/town_flow.gd`. See Part Three, "Three Separate Investigative Threads," item 2.**

**Also corrected this pass, a stale status rather than new work: the Tab-menu pocket watch (Part Four, Part Six Stage 5) shipped 2026-09-14, commit `3f582c2` — Part Four's and Part Six's "ready to build" language for it is now stale and is corrected below to reflect it as built. A follow-up remains open, at the author's own request: the shipped panel shows day/time/phase as text, which the author considers anachronistic for a 1920s pocket watch; the fix is a proper analog clock face with hands over the same underlying `DayClock` data, purely visual, no logic change. See Part Four and Part Six.**

**Not merged this pass, still holding-pen per `docs/qa/TDD_PENDING_NOTES.md`, reported only and not independently confirmed: Antigravity's coordinate-collision audit (three schedule fixes, plus an unreconciled 61-vs-70-NPC figure against this document's own v36 count); the removal of a static placeholder NPC near Pickman Street; a regressed camera-jump-on-menu-close bug, not yet handed to Codex/Antigravity; and non-urgent `docs/qa/` housekeeping. See Part Six.**

**This revision (v36), 2026-09-14, merges an Antigravity pass — a separate tool from Codex, with its own real Godot/shell access, that performed a full dependency-and-gate audit across all `.dialogue` files rather than a documentation-accuracy review, and made source changes directly rather than only reporting findings. Five items, in order of what actually changed: (1) Catalog figures updated again: `dialogue_catalog_flow.gd` now reports 47 NPCs and 368 topics, up from Codex's last rerun of 41/349, against 48 total `.dialogue` files (up from 42). Six new files/NPCs are not explained by anything Antigravity's own report describes editing (its content edits were to seven already-existing files, none newly created); the author has since confirmed the cause, per the author directly, 2026-09-14: Codex is actively mid-pass on Stage 2 scheduled-life population as this revision is being written, so the NPC/topic count is a moving target this document is capturing mid-flight, not an anomaly. Instrument-voice figures (144 cues/614 lines) were not rechecked this pass and are carried forward unchanged. (2) Two genuine fork-locked dead ends were found and fixed, distinct from the twelve-fact shortlist below: in `local_historian.dialogue`'s `dr_fenn_library`, choosing the annotations branch permanently locked out `estate_conduit_map`, dead-ending `ropewalk_foreman.dialogue`'s `estate_mooring_cable`; in `widow_kessler.dialogue`'s `butcher_parcels`, choosing the silt-cuffs branch permanently locked out `gray_salt_cakes`, dead-ending `ropewalk_foreman.dialogue`'s `cellar_salt`. Both fixed by adding an alternate carrier (`stationer.dialogue`'s `fenn_conduit_tracing`; `EVIDENCE: gray_salt_cakes` added to `fish_smoker.dialogue`) and loosening the downstream `ropewalk_foreman` gates to accept either route. These are the same class of problem the missable-NPC pattern below addresses, just triggered by a player's dialogue choice rather than a missed schedule window — worth folding into that pattern's scope going forward. (3) The `curriculum_abridgment` bottleneck: both the primary and backup route for the school-reader-omission thread were gated on the same 4-witness `crew_omission` tally, so the "backup" wasn't actually independent of the primary's own precondition. Fixed by adding `curriculum_abridgment` as an alternate grant in `school_parent.dialogue`'s `the_reader_lesson` and loosening `the_covering_letter`'s gate — missing the 4-witness tally no longer blocks the thread outright. (4) The Ashcroft/Whitlock/Wick schedule-cleanup item (Part Six item 5, open since v16) is RESOLVED: all three files migrated from `dawn`/placeholder destinations to the documented `morning/midday/evening/night` vocabulary. (5) A stale test assertion in `tests/dialogue_playback_flow.gd` — written for the old auto-retirement fork behavior and never updated when the resolution-with-recap mechanism (Part Three) replaced it — is corrected to match the current contract; the test now passes. Full suite rerun and reported passing: `audit_dialogue_ast.gd` (0 errors/warnings, 47 scanned definitions), `dialogue_playback_flow.gd`, `dialogue_catalog_flow.gd`, `dialogue_template_flow.gd`, `dialogue_live_flow.gd`, `town_expansion_flow.gd`. On the original twelve-fact shortlist specifically: this pass's static dependency/gate audit covered all evidence chains and did not surface a lockout affecting any of the twelve, which is reassuring but not the same claim as the manual in-game playtest this document's own standing rule still calls for — that remains unconfirmed, as it has since v32. This session cannot independently verify any of the above (no `device_bash`); merged here on the same basis as Codex's passes — internally consistent, technically plausible, not taken as confirmed until the author or a future pass checks it directly.**

**This revision (v35), 2026-09-14, merges a third Codex pass, reviewing v34 itself rather than the original v31. Codex approved v34 as substantially accurate, with one numerical correction and two status refinements, all merged below. (1) The 337-topic figure (Part One/Two/Verification) was itself already stale by the time v34 shipped it — a rerun of `tests/dialogue_catalog_flow.gd` against the currently-committed corpus reports 349 topics, not 337; the twelve-topic gap matches the twelve backup topics authored in v32. Updated throughout, with v34's own too-narrow growth explanation (crediting the six new NPCs alone) corrected to note that existing-NPC dialogue expansion and the twelve backup topics also contributed to the topic count specifically. The 614-NPC-line and 144-cue voice figures are independently reconfirmed correct as stated. (2) The "not yet run" status in Part Four's redundant-carrier section and Part Six items 10 and 12 was stale and is corrected — but not to a claim that the twelve backup routes are automated-tested, which they aren't: `tests/dialogue_lang_flow.gd` runs against its own dedicated fixture files and `tests/dialogue_template_flow.gd` tests the authoring template mechanism, neither referencing any of the twelve new topic IDs. What's actually confirmed: those generic tests pass, `tests/dialogue_catalog_flow.gd` parses the full corpus clean (41 NPCs, 349 topics), and the twelve topic/evidence pairs exist in source. Each backup topic's individual `GATE:` and runtime reachability, and a manual in-game playtest, remain outstanding and are not claimed here. (3) Part Six item 11 (Stage 2, "not yet started") is corrected to the more exact framing Codex proposed: some scheduled-life population already exists — not `speakeasy_bar`, which is a fixed Stage 4 interaction and not population evidence, but the six scheduled NPCs (`drayman`, `fish_smoker`, `lamplighter`, `night_owl_one`/`night_owl_two`, `speakeasy_bartender`) — while the planned broader layered population pass has not started. "Not yet started" was defensible only under the narrower reading and is now stated precisely rather than left ambiguous. Everything else Codex checked against this revision — the 42-file/41-NPC resolution, all twelve backup routes' topic/evidence IDs, the live `$84,000` recurrence, Odell's fork location, and the contiguous-town/Web-export/telemetry/debrief/eavesdropping/Worker-version corrections — was confirmed accurate as v34 stated it; no further changes made on those points.**

**This revision (v34), 2026-09-14, merges a second Codex pass — this one run with a real Godot executable and direct source access (actual `tests/dialogue_catalog_flow.gd`/`instrument_voice_flow.gd` runs and greps shown, not claims), a materially stronger evidentiary bar than the first pass this document's v33 note describes. Three items merged: (1) Part Two's dialogue-catalog and instrument-voice figures (35 NPCs/217 topics/455 lines) were stale and are updated to the rerun's results — 41 NPCs, 337 topics, 614 NPC lines (the 144-cue manifest is unchanged); this also resolves the v33 caveat flagging the 35-vs-42-file inconsistency, since the real rerun explains it (42 `.dialogue` files, 41 of which resolve to a distinct NPC). (2) A newly-confirmed `$84,000` regression, distinct from the item v22 already closed: `county_clerk.dialogue`'s `clerk_ophion_1` `NOTEBOOK:` entry still carries the literal `$84,000` numeral-and-symbol form, unlike the two originally-cited wage-claim lines, which remain correctly spelled out. Logged as open below rather than folded into v22's "resolved, closed" status, since it's a different line discovered later, not a reopening of the original fix. (3) Captain Odell's previously "not found" one-shot intake fork is now located by direct read: it lives inside `odell.dialogue`'s `TOPIC: default` block (`GATE: NOT topic_done(odell, default)`), so it retires via the ordinary one-shot default mechanism rather than an explicit `OUTCOME:` — closing that loose end. One item from the same pass deliberately not treated as a fresh correction: the report re-flagged v31's "still open: wiring the game side" telemetry line as wrong, but that line is dated revision-history text describing the state as of v31 specifically, already superseded within this same document by later entries and by Part Six item 15 — no live contradiction, just a paragraph easy to misread in isolation if you stop there. No change made on that account. Still unmerged, unchanged from v33: commit-provenance for `b39598d`/`085299d`, Codex's own current TDD version number, and whether Stage 2 population has meaningfully begun — see `docs/qa/CODEX_TDD_V31_CORRECTIONS_2026-09-14.md`.**

**This revision (v33), 2026-09-14, merges a batch of corrections Codex reported against this document, relayed by the author. Per this document's own standing rule, nothing below was taken on Codex's report alone — each item merged here was independently confirmed by cross-referencing this document's own already-source-confirmed content (no `device_bash` shell was available this pass, so nothing requiring `git log`/`git show` or a live Godot/catalog-test rerun is merged; those stay recorded separately in `docs/qa/CODEX_TDD_V31_CORRECTIONS_2026-09-14.md` pending that tooling). Merged: (1) Part Two's "hub-and-spoke, not continuous" geography line was stale against this document's own v25/v26/v29 entries and Part Six Stage 1, which already describe a landed, contiguous two-phase town — corrected in place. (2) Part Four's telemetry paragraph calling the feature "not started" directly contradicted the Debrief/Hosting/Transport paragraphs immediately below it in the same Part, all already marked built and deployed — rewritten as historical rationale. (3) Part Two's Verification section and Part Six Stage 1 flatly contradicted each other on whether a Web export exists (one said yes, in-repo; the other said none had been produced) — reconciled: a Web export exists, but no profiling/measurement has been run against it, which is the actual open task. (4) Part Four and Part Six's eavesdropping paragraphs said "no systems exist yet," which this document's own v29 entry already contradicts for the one specific case it describes — the fixed `speakeasy_bar` overheard exchange. Corrected to distinguish "no general proximity/eavesdropping system" (still true) from "one fixed, deliberately atmosphere-only eavesdropping interaction already shipped" (true since v29). (5) Part Six item 15's opening label "LIVE end to end" is narrowed to what's actually confirmed — deployed and transport-tested, pending an actual tester playthrough — matching the caveat the same paragraph already ended on. (6) Part Three's "Codex also maintains its own parallel copy of this document... currently at its own v18" is reframed as a dated, historical statement (as of 2026-09-13) rather than an assertion about Codex's current version, which this session has no way to verify. (7) A caveat is added next to Part Two/Verification's 35-NPC/217-topic/455-line catalog figures, flagging that this document's own later full-file audit (v32) counted 42 `.dialogue` files against those same figures' 35 — an internal inconsistency this document cannot resolve without rerunning the catalog test, so those figures should not be treated as current until that happens. Left unmerged, deliberately, and still recorded only in the corrections file: commit-provenance claims for `b39598d` and `085299d` (needs `git log`/`git show`); Codex's own current TDD version number (needs asking Codex directly, not inferable from source); and whether Stage 2 scheduled-life population has meaningfully begun (Codex's cited evidence, the `speakeasy_bar` addition, is this document's own Stage 4 atmosphere content by its own v29 account, not Stage 2 population — genuinely ambiguous, not confidently cross-referenceable, left as reported-only).**

**This revision (v32), 2026-09-13, closes the missable-NPC redundant-carrier pattern (Part Four/Six): all twelve load-bearing facts on the shortlist now have a second-source topic authored, confirmed against the actual `.dialogue` files in this repo (not drafted from the summary table alone). One correction to this document's own prior count in the same pass: v24's shortlist table lists twelve rows (three newspaper-correction facts, three historian/schoolteacher facts, five freight/quay/Naomi-sighting facts, one standalone) but the surrounding prose calls it "eleven facts" throughout Parts Four, Five, and Six — a miscount carried forward unexamined since that table was built. All twelve rows are addressed below; the "eleven" figure in earlier prose should be read as twelve going forward. Assignments, each granting the identical `EVIDENCE:` id as the primary source (so every existing downstream `GATE:` opens regardless of which route the player actually took, with no `chapter_one_archive.gd` LINKS changes required, since no new evidence ids were introduced): `press_suppression` → `stationer` (`halleck_arithmetic`); `gazette_correction_terms` → `post_office_clerk` (`correction_requirements`); `gazette_correction_printed` → `tailor` (`correction_slip_seen`); `reader_omission_letter` → `school_parent` (`the_covering_letter`); `sanitized_textbook` → `school_parent` (`the_reader_lesson`); `ophion_myth_classical` → `tailor` (`pruitt_family_naming`); `quay_inquiry` → `quay_docker` (`the_office_correction`); `new_bedford_letters` → `school_parent` (`the_new_bedford_letters`, built on her own already-established sighting of Naomi's envelope rather than invented from nothing); `naomi_quay_inquiry` → `chandlers_boy` (`the_woman_with_the_notebook`); `estate_freight` → `drayman` (`the_kitchen_stone`); `chandlery_island_delivery` → `apothecary` (`the_camphor_stranger`); `kessler_carriages` → `tailor` (`the_altered_shoulder`). Each backup topic's `GATE:` mirrors its primary topic's own precondition with `NOT evidence(id)` in place of the primary's `NOT topic_done(...)`, using only vocabulary already confirmed in `docs/DIALOGUE_AUTHORING.md`. School\_parent and tailor each absorbed three backups rather than one apiece — both were already the most socially-connected NPCs on their respective threads (Mrs. Teague sits at the schoolhouse gate and her husband sits on the school committee; Mr. Krebs dresses the club's own members across three generations), so this reads as characterization rather than a workaround. Not yet done: live verification in Godot (this pass was authored and committed to the repo via the device bridge, without a `device_bash` shell available to this session, so `tests/dialogue_lang_flow.gd`/`dialogue_template_flow.gd` and a manual playtest of each backup topic — reaching it, confirming the correct NPC state, confirming the grant — are still outstanding, per this document's own standing rule that syntax alone never establishes reachability). See Part Four and Part Six.**

**This revision (v31), 2026-09-13, closes out the telemetry endpoint: it's live at `https://three-colors-worker.dejunai.workers.dev`, confirmed against the actual Worker source in this repo rather than taken on report. It's built on R2, not the originally-specced D1 — accepted as shipped, by the author's own explicit call, rather than sent back to Codex for rework. See Part Four for the full tradeoff and Part Six item 15 for status. Still open: wiring the game side (the endpoint setting and the per-event log calls) and actually exercising it from a real playthrough.**

**This revision (v30), 2026-09-13, records two items from the author. First, confirmed against source: Codex removed the developer-only Shift-brisk speed inflation as the default — see Part Four, resolving the fourth of four factors in the conversation-time/travel-time scale-mismatch diagnosis, though the first three factors remain open. Second, scoped but not started: anonymous per-playthrough telemetry for external Web testers, using a unique session id with no personal data in the log payload. See Part Four for the full metrics list (the author's own request, verbatim in intent) and a feasibility breakdown by category. The transport decision is now made — automatic upload, not manual download-and-send — with a proposed (not yet built) minimal serverless hosting approach and the accepted infrastructure-level IP-logging tradeoff named alongside it.**

**This revision (v29), 2026-09-13, records two items from a live source-diff pass against four new commits from Codex (Phase 2: the lower district and full waterfront folded into the contiguous exterior, following the same seam-by-seam pattern as Phase 1 — see Part Six item 10), plus one small fix made directly in this pass rather than handed to Codex. First, the diff confirmed Phase 2's `contiguous_town_phase_two.gd` composes cleanly with Phase 1's `shared_spot()` (both are no-op passthroughs for districts they don't own, so chaining `ContiguousTownTwo.shared_spot(ContiguousTown.shared_spot(spot))` in `chapter_one_dialogue.gd` is correct), and that a movement-bounds concern raised during the diff was a false alarm — `chapter_one.gd` already assigns `"town"` a wide enough `Rect2` to cover the new geometry, confirmed by direct read, not assumption. Second, the diff surfaced a live but unwired stub: `town_expansion.gd` added a `speakeasy_bar` interaction target labeled for "eavesdropping," ahead of the five-stage sequence above (which explicitly places ambient/eavesdropping content at Stage 4, after the town and backup routes). Rather than leave a dead, misleading `[E]` prompt in a shipped scene or hand it back to Codex as a defect, it was wired directly in this pass: `chapter_one.gd`'s existing generic fallback already resolves any interaction id against `Story.SCENES`, so no dispatch code was needed — only a new `Story.SCENES["speakeasy_bar"]` entry (`town_story.gd`), authored as a short overheard barroom exchange that echoes the `crew_omission` thread ("nobody remembers the crew's names either") without granting `EVIDENCE:`/`NOTEBOOK:` — deliberately kept atmosphere-only so it doesn't reopen the evidence surface just triaged down to eleven load-bearing facts. Author's standing instruction, recorded here: check back only when a change is load-bearing; small, reversible, in-genre content fixes like this one don't need a round trip.**

**This revision (v28), 2026-09-13, records Codex's proposed priority sequence for the "more life" work items introduced in v27, adopted by the author. Part Six's flat, unordered list of those items (formerly items 10–13) is superseded below by a five-stage order: (1) finish and visually validate the contiguous town; (2) populate it in layers with scheduled life; (3) add the eleven identified backup routes; (4) introduce ambient paired conversations and eavesdropping; (5) add the Tab-menu pocket watch alongside that schedule-focused pass. Codex's stated rationale: this should produce the intended Gothic effect — the town feels too socially dense to strip-mine comfortably, while the investigation stays robust enough to survive a human player's imperfect route. One dependency flagged during adoption, not yet confirmed with Codex: if stage 2 (populating the finished town with scheduled life) touches or extends `dialogue_catalog.gd`'s scheduling machinery for any of the eleven backup-route source NPCs (`gazette_editor`, `schoolteacher`, `local_historian`, `post_office_clerk`, `quay_bookkeeper`, `quay_docker`, `chandlers_boy`, `widow_kessler`), stage 3's backup routes should be authored after stage 2 lands for those NPCs specifically, not before — otherwise redundancy gets written against a schedule that is about to move under it.**

**This revision (v27), 2026-09-13, records two forward-looking items from the author — neither is implementation work. First, a pocket-watch HUD button: ready to build, no new systems required, see Part Four. Second, NPC-to-NPC eavesdropping: logged explicitly as a "thought-bomb" per the author's own words, not a priority and not being scoped or built now — "the systems as they are won't support this, and I am not asking to generate new systems as much as add it to the TDD." See Part Four for the real gaps this would need to close whenever it's revisited. Separately, the author confirms all `[Tab]` HUD/menu interactions, including the pocket watch, should cost no `TIME:` at this stage — free, revisited later if pacing tuning ever calls for it.**

**This revision (v26), 2026-09-13, is a stopping-point checkpoint — Codex is on a 2-hour cooldown, so this is the literal end of today's Phase 1 work, confirmed against source rather than taken from the report alone. Branch `dev/contiguous-town-phase-1`, six steps landed: (1) a walkable stone incline replaces the flat Pickman-to-business portal; (2) the existing business-street geometry now sits at the top of that incline in the same exterior, interiors unchanged, exit positions transformed to the correct storefront via `business_return()`; (3) a second climb reaches toward the upper quarter, silhouette-only at first; (4) the upper quarter itself is folded in the same way, via a new shared `upper_street.gd` builder also used by the old flat hub; (5) a return loop (a second climb and a second descent) closes the walk into a full loop instead of a dead end, plus a self-corrected retaining-wall pass after a playtest made the raised business ground read as an unsupported bridge; (6) scheduled business/upper NPC positions are now translated into the shared exterior's real coordinates via a new `ContiguousTown.shared_spot()`, closing the "NPCs still at old coordinates" gap flagged after the first three steps. One regression was found and fixed mid-session (Step 1-3 pass only): the `eight_identified` birch-grove-placard key remap in `chapter_one.gd` had been silently deleted; restored. The second diff pass (Steps 4-6) introduced no regressions. Remaining, per Codex's own doc: profile the complete exterior on desktop and Web before any streaming/LOD/model-upgrade work. No Web export has been produced for any step so far.**

**This revision (v25), 2026-09-13: first attempt at contiguous exteriors began today, on a new branch — Phase 1 (upper ridge, business district, Pickman Street, lower quarter, waterfront, estate, offshore island). Spatial relationship study produced; no implementation yet.**

**This revision (v24) records three items from 2026-09-12. First, a diagnosed but unresolved pacing defect, no solution chosen: see Part Four, "Conversation-time/travel-time scale mismatch" (four confirmed contributing factors — the `CONVERSATION_MINUTES`/`DEFAULT_MINUTES` scale mismatch against `TRAVEL_MINUTES`, the passive `WANDER_RATE` real-time tick, and the author's dev-only brisk-speed multiplier skewing their own playtesting read). Second, closed: the "Fork revisit-with-recap pattern," reported by the author mid-session as Codex quietly "on it," then confirmed against real source and test — see Part Three, "`FORK:`/`OUTCOME:` Resolution With Recap," which supersedes this document's own prior "auto-retirement" description and its own unbuilt "companion recap topic" proposal. All eight previously one-shot-protected forks are confirmed migrated to the new mechanism. Third, at the time reported only and not source-confirmed: Captain Odell was said to retain one deliberately one-shot fork (his automatic intake conversation) whose actual source location was not found in that pass. Located since, 2026-09-14 (v34), by direct read: it's `odell.dialogue`'s `TOPIC: default` block (`GATE: NOT topic_done(odell, default)`) — the `FORK:` lives inside the one-shot default greeting itself, so it retires via the ordinary default-topic mechanism, not an explicit `OUTCOME:`. See Part Four and Part Six.**

**This revision (v23) records two items from a live dialogue-review pass with Claude (Cowork), per Dejunai, 2026-09-12. First, closed: eight `FORK:`\-bearing topics across `post_office_clerk.dialogue`, `county_clerk.dialogue`, `gazette_editor.dialogue`, `local_historian.dialogue`, `mr_vale.dialogue` (NPC id `quay_bookkeeper`), `mr_whitehouse.dialogue`, `school_parent.dialogue`, and `widow_kessler.dialogue` had no repeat-protection at all — no `NOT topic_done(...)` in their `GATE:`, and no `OUTCOME:` on their `CHOICE:` branches to trigger the auto-retirement Part Three already documents for that mechanism. Each was fully replayable, letting the player harvest every `CHOICE` branch's evidence across separate visits instead of committing to one, defeating the "mutually exclusive" intent `dialogue_lang.gd`'s own authoring comments describe for `FORK:`. Fixed by adding `NOT topic_done(npc, topic_id)` to each `GATE:`, matching the convention already used correctly elsewhere (`quay_docker.dialogue`'s `estate_freight_carts`, `schoolteacher.dialogue`'s `the_slain_boy`). Second, opened and deliberately deferred: see Part Four, "Fork revisit-with-recap pattern."**

**This revision (v22) resolves the `$84,000` formatting item, per Dejunai, 2026-09-12. It was never a bug in the sense outside accounts of this project have assumed — a circulating "leaked design docs" narration (an AI-generated podcast working from that fictional premise) dramatized it as a deliberate, expensive UI glitch simulating real-time save-file corruption. That account is fabricated. The actual issue was a plain text-rendering defect: the `$` character did not display correctly in the wage-claim dialogue script (see Part Five, The Wage Claim). The fix removed the currency symbol from the source entirely and spelled the amount out in words — "eighty-four thousand dollars" — with no numerals or symbol left anywhere in the line. This is also exactly why the item was previously unreproducible by searching for `$84,000` or any numeral form: that string does not exist in the corpus in that shape. Closed below in Parts Three, Four, and Six.**

**Prior revision (v21) reconciled this project-doc copy against the real repository, per Dejunai, 2026-09-12. The two features this document's v20 had specified as "not yet implemented, ready for Codex" — `WEIGHT:` on `TOPIC: default`, and `FORK:`/`OUTCOME:` one-time resolution — were in fact already implemented by Codex by the time v20 was written here; Codex had also already carried the repository's own TDD forward to its own v18, independently of this project-doc lineage. This revision was built by pulling the real repository directly (`scripts/shared/dialogue_lang.gd`, `dialogue_runtime.gd`, `dialogue_state.gd`, `tests/dialogue_lang_flow.gd`, `docs/DIALOGUE_AUTHORING.md`, and the repository's own `docs/design/7) Three Colors of Madness - TDD v18.md`) rather than from a description of them, so the specifics below are confirmed against source and test, not reported secondhand. Three things changed as a result:**

* **`WEIGHT:` on `TOPIC: default` is implemented, confirmed against source and test. It works exactly as this document's v20 speculated, with two real details v20 could not have known: repeat-avoidance is tracked by the winning topic's source line number, not its text (so two differently-worded lines still count as "the same line" only if they're literally the same authored entry), and misuse is actually two separate parser errors, not a silent no-op as v20 assumed — see Part Three and the correction below. Repository v18 does not document this feature at all; it is documented here for the first time against real source.**  
* **`FORK:`/`OUTCOME:` one-time resolution is implemented, confirmed against source and test, and materially matches this document's v20 spec, with the real mechanism now recorded precisely: `OUTCOME: decision_id = value_id`, gated to auto-retire every topic containing that decision once any of its branches is taken. Repository v18 already documents this feature (as of its own v16–v18 passes); this revision folds that documentation in accurately rather than duplicating v20's speculative version of it.**  
* **One correction to v20's own WEIGHT spec, made in place: v20 stated that authoring `WEIGHT:` outside `TOPIC: default` is "ignored silently rather than erroring." Source shows the opposite — it is a hard parse error (`"WEIGHT is only valid on TOPIC: default"`). v20 also implied a bare no-repeat rule; source additionally falls back to allowing repetition only when excluding the previous line would leave the pool empty, which v20 got right, and weights are read as a positive float, not restricted to integers, which v20 did not specify either way. Corrected below; no design intent changes as a result of any of this, since the feature already ships and already works — this is a documentation-accuracy fix, not a behavior change.**

**This pass also folds in real changes present in the repository's own v18 that this project-doc copy had not yet recorded on its own thread: the waterfront hub, updated dialogue-catalog figures (now 35 NPCs, 217 topics, 455 voiced lines after the September 12 dialogue expansion), and — materially — that the ElevenLabs voice-pack replacement is now complete and mapped into the shipping 144-cue manifest, superseding this document's own earlier "still being assembled, not a strict drop-in" language. See Part Two, Three, and Four.**

**Prior revision (v20) specified two new dialogue-grammar extensions proposed by Dejunai, 2026-09-12, believing them not yet implemented. They were already implemented by the time this was written — see the reconciliation above. v20's spec language is retained in spirit (nothing about the feature intent was wrong) but its "not yet implemented, ready for Codex" framing is now corrected throughout.**

**Prior revision (v19) records that the `odell_precinct` topic-gate defect is fixed, per Dejunai, 2026-09-12. The gate now resolves correctly rather than remaining repeatable — see the updated Part Three, Part Four, and Part Six entries below. This closes an item that had been independently confirmed by four separate methods (Codex's direct source read, Grok's dialogue-file pass, AntigravityIDE's AST audit, and "Antigravity (noIDE)"'s raw-file pass) before the fix landed.**

**Prior revision (v18) corrects v17 against Codex's direct check of source and live test output, approved with corrections, 2026-09-12. v17 was right in direction — dialogue audio is real and wired in — but wrong or premature in several specifics, all fixed below:**

* **The waterfront was missing from Part Two's scope list. It's a real playable outdoor hub with scheduled residents, travel time, save restoration, and the offshore whaling station visible as unreachable scenery. Added.**  
* **The dialogue-count figures in v16/v17 (30 files, 148 topics, 135 nonempty) are stale. Current catalog test reports 35 live NPCs, 217 nonempty topics, and 455 voiced NPC lines. Updated throughout.**  
* **v17's audio caveat ("the specific integration mechanism... has not been independently confirmed against source") is now resolved, not merely reported. The architecture is confirmed: `VOICE: cue_id` is authored immediately before an NPC's spoken line; the parser attaches that cue to the line; the runtime emits `[speaker, text, voice]`; Chapter One plays the matching WAV when the card appears and stops it on advance; a 144-entry manifest validates cue names; instrument voice volume is independently adjustable; Walter, narration, beats, objects, notebook entries, and system text intentionally remain silent. v17's phrase "every dialogue line plays a clip" overstated this — corrected throughout to "every authored NPC line has an instrumental voice cue."**  
* **The dialogue interpreter is now the shared foundation planned for all three chapters. `FORK:` choices still hardcode Walter as the player speaker and remain silent in Chapter One; protagonist identity must become chapter-provided before Chapter Two content is integrated. `OUTCOME: decision_id = value_id` now records immutable, branch-local decisions, retires the source fork automatically, and exposes `outcome()` / `outcome_is()` gates for downstream consequences. Existing forks remain replayable until explicitly authored with an outcome.**  
* **v17's "\~500-file library" scope language is removed. The shipped game uses 144 reusable cue IDs, not one unique recording per spoken line — a cue is shared across every line tagged with it, not generated per-line. The completed ElevenLabs source delivery contains 228 recordings; a compatibility build now produces the 144 stable game cues from them — this project-doc copy's own earlier "still being assembled" language for that library is now stale and is corrected below.**  
* **Returning staff are now correctly recorded as implemented rather than hypothetical: `odell_precinct` and `coroners_assistant_morgue` exist as live scheduled NPCs for later days. A namespace defect found during review was fixed without a version bump: all three Odell precinct topics now test completion under `odell_precinct`, the identity under which the runtime records them. The returning-staff regression test covers each topic.**  
* **Verification is updated: a real Godot 4.7.2 standard executable is now available (not just the mono editor used for the native staging-suite pass), and the current authoritative checks pass — dialogue catalog (35 NPCs, 217 topics), instrument voices (144 cues, 455 NPC lines), live dialogue flow, and usability/object interactions.**  
* **Part Six's closing paragraph is corrected: Chapter One's completed placeholder voice integration does not mean an architectural choice has been made for Chapters Two/Three. That decision stays explicitly deferred; v17 implied otherwise.**  
* **The Ashcroft/Whitlock/Wick schedule-cleanup item is reconfirmed accurate and unchanged: those three files still use `dawn` and placeholder destinations.**  
* **Four refinements after this revision's initial approval, still 2026-09-12, no version bump: the cue manifest validates names during dialogue loading and automated content validation, not "at author time" (see Part Three); a short current-state note on the shared dialogue/examination UI frame is added to Part Three; the completed ElevenLabs source delivery is mapped into the stable 144-cue interface; and the Grok MIDI placeholder WAVs are removed from the playable build.**

**Prior revision (v17), 2026-09-12: recorded the author's report that wordless instrumental voice audio had been wired into all dialogue using a sub-par/regressed Grok batch as placeholder, and flagged the underlying mechanism as unconfirmed against source. That mechanism is now confirmed — see above and Part Three.**

**Prior tightening pass (v16), following Codex's third review, this time of v15, directly against source. Three targeted corrections, each verified independently below rather than accepted on report — Codex's own framing, confirmed: these are not grounds for another broad rewrite.**

* **v15's steward paragraph mistook the conditions that block *sleep* for the conditions that gate the *first steward conversation*. They're different checks on different functions. Corrected in Part Three, with the third encounter's actual coat/day requirement restored.**  
* **v15 proposed a "dedicated tracing pass" to connect the newspaper-correction thread to Almy's response. Unnecessary — Codex traced it directly and it's confirmed implemented. Documented in Part Three; the follow-up item removed from Parts Four and Six.**  
* **v15 said the day/night clock drives scheduled-resident placement "only." Too restrictive — it also drives the sun, lighting, fog, and street lamps. The word is removed; the underlying distinction from story-driven body removal stands.**

**Prior tightening pass (v15), following Codex's second review of v14:**

* **v14 incorrectly chained three separate dialogue threads into one dependency chain. Confirmed and split apart in Part Three.**  
* **v14's "every NPC disappears at night" was too broad. Confirmed wrong by direct reading of `chapter_one_dialogue.gd`'s `allowed()` function: the day/night placement rule in `dialogue_catalog.gd`'s `slot()` applies only to catalog-scanned additional residents (`extra_actors`). The chapter's story actors (Odell, the coroner's assistant, the groundskeeper, the old woman, the steward) have their own bespoke, state-based availability rules with no time-of-day gating at all. Corrected in Part Three.**  
* **v14 conflated the day/night clock with body removal. It doesn't govern it. Corrected in Part Two.**  
* **The `background_npc_template` extension item is closed — Codex confirmed the file is back to `.dialogue`; this document's own read of the delivered zip already showed the same thing. No further action.**  
* **v14 under-documented real implemented mechanics that matter for future work more than this document's own revision history does. Added below: the `filed()` gate (distinct from `evidence()`), the newspaper-correction thread's actual prerequisites, and the steward's three-visit/Day-2-montage progression.**  
* **Provenance needed adjusting to credit Codex's actual subsequent work on the interpreter and its documentation, not just integration and review, while keeping the original design credited correctly. Fixed in the byline above.**

**One constraint made explicit since v16, per the author: validating and testing the newest development revision does not authorize replacing the frozen, already-tested Web build. That build stays the release artifact until the author says otherwise; development-revision testing is diagnostic, not a release decision.**

# **Part Two — Current Build**

## **Engine and Target**

**Godot 4.7 (the Web export identifies as 4.7.2.stable), using the GL Compatibility renderer (not Forward+), targeting broad hardware reach over cutting-edge rendering fidelity. Window target 1440×900, canvas-items stretch mode. No import dependencies beyond the engine itself. Unchanged since v1.**

## **Scope of the Current Slice**

**The Ophion estate grounds (rose garden, service passage, tunnel entry), Pickman Street (boardinghouse, cobbler's shop/Walter's room, precinct, morgue), three neighborhood hubs (business district, upper-residential ridge, lower-residential quarter), a post office, and the waterfront: a playable outdoor hub with scheduled residents, its own travel time, full save restoration, and the offshore whaling station visible as unreachable scenery. Geography is now contiguous, not hub-and-spoke — corrected in place, per this document's own v25/v26/v29 entries and Part Six Stage 1: Phase 1 (Pickman Street, business district, upper ridge) and Phase 2 (lower district, full waterfront) both landed 2026-09-13 as one shared walkable exterior, with only interiors remaining separate scenes. The day/night clock drives scheduled-resident placement, and separately drives the sun, directional lighting, fog color, and street-lamp visibility, purely as presentation. The clock does not govern body removal. Story progression does — the rose-garden bodies clear after the player's first departure to town; the birch victims (the woman and boy) remain visible and unattended through at least a second estate visit and clear only once Day 3 has begun, with no accompanying scene or notification either time.**

**The live dialogue system spans 70 live NPCs and 438 nonempty authored `TOPIC:` blocks, corrected v40, 2026-09-15, per Codex's own rerun of `tests/dialogue_catalog_flow.gd` (this document's previous 454 figure, v38/v39, was itself in error) — and itself likely to keep moving as content authoring continues; re-derive rather than trust this snapshot indefinitely, per this document's own standing practice. Real cross-NPC and paperwork-gated content exists beyond simple witness menus — see Part Three.**

**Wordless instrumental voice cues are implemented across every authored NPC line. 705 NPC lines currently carry a voice cue, corrected v40, 2026-09-15, per an actual rerun of `tests/instrument_voice_flow.gd` after the `AVERAGE JOE` rename fix (up from the previous 614 figure — that rerun had been silently failing on the `AVERAGE JOE` collision, per this document's v40 revision note above) — drawn from a 144-entry cue manifest, unchanged (a cue is reused across every line tagged with it — this is not one recording per spoken line). See Part Three for the confirmed architecture. Walter, narration, beats, objects, notebook entries, and system text are intentionally silent by design, not by omission. Corrected this pass: the ElevenLabs voice-pack replacement is complete, not still being assembled. This document's own v18–v20 language calling it a work-in-progress, not-a-strict-drop-in library is stale. The completed ElevenLabs Sound Effects delivery contains 228 source recordings; a documented compatibility build (`tools/build_elevenlabs_voice_library.py`) now maps the broader trombone vocabulary and supplied violin mood takes onto all 144 stable game cue IDs, so every existing `.dialogue` file's `VOICE:` assignments remain valid without any source-file changes. Trombone takes map to complementary delivery styles; violin length variants use pitch-preserving cadence changes from the supplied mood takes. Output is normalized mono 44.1 kHz 16-bit PCM, with a generated manifest preserving source-to-cue provenance. The sub-par/regressed Grok MIDI placeholder batch has been removed from the playable build entirely. The broader ElevenLabs source vocabulary remains available for later selective expansion without forcing dialogue changes now.**

**Object-evidence content is now authored the same way, in a parallel flat-file grammar rather than hardcoded in GDScript. All eleven scoped hotspots (estate's seven, town's three, tunnel's one) are migrated and live — see Part Three, "Object-Evidence Authoring System," for the full architecture, and Part Four for the two known, accepted limitations carried forward as open items.**

**The Day 2 montage no longer plays on a new playthrough; Day 2 is now enacted, playable content culminating in the steward's second visit — see Part Three, "Three Separate Investigative Threads," item 2, and Part Six.**

**Not yet implemented, unchanged from prior revisions: the glass-shattering ontological break, the fatal-comprehension ending, Chapters Two and Three as playable slices, gamepad support, full key rebinding, a full encumbrance/leveling system, and Ekon's later consumption of Walter's record.**

## **Saves**

**Unchanged from prior revisions (schema 9). Not independently re-verified this pass beyond the waterfront's save-restoration behavior noted above.**

## **Verification**

**A real Godot 4.7.2 standard executable is available (distinct from the mono-only editor used for the earlier native staging-suite pass, which could run gameplay tests but not export Web builds). Current authoritative checks, run against it, pass:**

* **Dialogue catalog: 70 NPCs, 438 topics — corrected v40, 2026-09-15, per Codex's own rerun of `tests/dialogue_catalog_flow.gd` (the previous 454 figure, updated v38, was itself in error).**  
* **Instrument voices: 144 cues, 705 NPC lines — corrected v40, 2026-09-15, per an actual rerun of `tests/instrument_voice_flow.gd` after the `AVERAGE JOE` rename fix (cue count unchanged; the previous 614 figure, updated v34, undercounted lines that had been silently failing the audit).**  
* **Live dialogue flow: PASS.**  
* **Usability and object interactions: PASS.**

**This pass's WEIGHT/OUTCOME reconciliation is independently confirmed by direct source read of `dialogue_lang.gd` (parser), `dialogue_runtime.gd` (selection/commit logic), and `dialogue_state.gd` (persisted outcome storage), plus `tests/dialogue_lang_flow.gd`'s existing coverage: a weighted-default no-immediate-repeat assertion, a two-error assertion for misused `WEIGHT:` (non-default topic *and* non-positive value in the same fixture), and a two-error assertion for malformed `OUTCOME:` (used outside a `FORK:` choice, and an incomplete assignment). This is source-and-test confirmation, not a description accepted on report.**

**Earlier direct source reading covered `chapter_one_dialogue.gd` (the live dialogue adapter, its `allowed()` function specifically) and `chapter_one_staging.gd` (the steward's three-visit progression). The voice-cue architecture described in Part Three is confirmed against source, not merely reported. Separately, per the author and Codex: the author has played the build repeatedly and external testers have already surfaced real issues (an exit problem, a ledger problem, a linking problem) against the frozen Web build specifically. What remains unverified is a cold playthrough of the newest development revision, including its voice cues and the waterfront hub — and per Part One's constraint, confirming that revision plays correctly is a diagnostic step, not grounds to replace the frozen build.**

**A new Web export exists in-repo, 2026-09-13 (`build/web/`), confirmed present by direct listing: `index.wasm` (\~39.5 MiB), `index.pck` (\~35.8 MiB), `index.js`, `index.html`, and icon assets. This is the newest development revision — the complete contiguous town (Part Six, Stage 1\) plus live telemetry and the debrief pairing — exported to the tester-distribution format, not yet the same thing as the diagnostic cold playthrough named above, and the formal Web-profile measurement handoff (`docs/qa/PHASE_ONE_WEB_PROFILE_HANDOFF.md`) still has no recorded figures against it. Per Part One, this new export does not itself authorize replacing the frozen build; it's an available candidate once the diagnostic playthrough happens.**

# **Part Three — Architecture As Built**

## **Story Actors vs. Catalog Residents**

**Direct reading of `chapter_one_dialogue.gd`'s `allowed(g, actor)` function shows two genuinely different systems:**

* **Catalog-scanned additional residents (`extra_actors`, populated by `catalog.scan()` in `setup()`) are placed exclusively through `dialogue_catalog.gd`'s `slot()`, which does return an empty placement at night regardless of authored schedule.**  
* **Story actors — Odell, the coroner's assistant (`assistant`), the groundskeeper (`crew`), the old woman, and the steward (`barman`) — have their own bespoke availability rules in the same `allowed()` function, none of which reference the day/night clock at all: Odell and the assistant are available whenever `world == "estate"` and the estate isn't complete; the groundskeeper once the lounge has been exited; the old woman while in town and not yet recorded as evidence; the steward keyed to the lounge and whether Mrs. Almy has been spoken to. These are chapter-authored, state-driven staging rules, entirely separate from `dialogue_catalog.gd`'s time-of-day system, and none of them hide their actor at night.**

**Returning staff for later days are implemented, not hypothetical. `odell_precinct` and `coroners_assistant_morgue` exist as live scheduled NPCs, giving both figures a presence beyond their initial estate encounter. The Odell precinct namespace defect found during review is resolved: `day_two_check`, `day_two_pressure`, and `day_three_final` test `topic_done(odell_precinct, ...)`, matching the identity used by dialogue completion. A regression test verifies that each topic disappears after completion without conflating precinct Odell with the estate encounter.**

## **Three Separate Investigative Threads**

**Three separate threads, each with its own prerequisites, confirmed independently rather than chained together:**

1. **The historian/schoolteacher witness-count thread. `local_historian.dialogue`'s `crew_omission_followup` topic gates on `topic_done(local_historian, crew_omission_official) AND topic_count(crew_omission) >= 4 AND NOT topic_done(...)` — a real "ask enough people, consistently tagged, before the historian goes further" mechanic, confirmed in source. `schoolteacher.dialogue` carries a related chain keyed off `evidence(naomi)` and `evidence(reader_omission_letter)`. This thread's own internal chain (historian → four witnesses → historian → schoolteacher) is real; it does not connect to either thread below.**  
2. **The steward appointment thread. `chapter_one_staging.gd`'s `sleep()` blocks *sleeping* — not the first conversation — when `steward_visits == 0`, `intake_done` is false, or `evidence.has("naomi")` is false; while blocked, the player just gets an "Get up" panel. Reaching the steward at all is gated separately, through `chapter_one_dialogue.gd`'s `allowed()`: the lounge itself only becomes reachable via the `service_entrance` travel action once `visited.has("almy")` is true, and the steward's dialogue is only `allowed` while `world == "lounge"`. So the real first-visit gate is "spoken to Almy, then travel to the lounge," not the sleep-block conditions. Visit two previously occurred inside a Day 2 montage (`draw_montage()`, a deliberately temporary "pacing bridge... replace the montage with enacted investigation later" per the source's own comment); as of 2026-09-14 (v37), the montage no longer plays on a new playthrough — see "Montage Removal / Day 2 Enactment" below — and visit two is now enacted, playable Day 2 content, culminating in the steward's second visit, relocated to the smoking lounge and ending on `THE STEWARD: "Come back tomorrow, and don't bring your badge."` Completing it still sets `day = 3` and `steward_visits = 2`, matching the retired montage's own effect exactly, so nothing about the substantive third encounter's gate changed shape. The substantive third encounter is gated by `case_state.gd`'s `steward_ready()`, which requires all four of `visited.has("almy")`, `day == 3`, `steward_visits >= 2`, and `coat == "Plain wool coat"` — the plain coat specifically, not just the day, is a real and separate requirement for the steward to open up. This thread does not depend on the historian/schoolteacher thread or the newspaper thread.**  
3. **The newspaper-correction thread, gated on a distinct mechanism: `filed()`, not `evidence()`. `gazette_editor.dialogue`'s `print_correction` topic requires `evidence(gazette_correction_terms) AND filed(eight) AND filed(naomi) AND filed(lodging)` and awards `EVIDENCE: gazette_correction_printed` on completion. `filed()` checks received report pages specifically, not the current evidence inventory, per `background_npc_template.dialogue`'s own comment — meaning this thread depends on paperwork actually having been filed and received, separate from simply having collected the underlying evidence. `mrs_almy.dialogue`'s `show_printed_correction` topic gates directly on `evidence(gazette_correction_printed)`; once available, showing her the slip has her read Naomi's name aloud and acknowledge Walter's effort ("You brought back something I can read. Thank you."), recorded via `NOTEBOOK: correction_seen`. The connection between the correction thread and Almy's response is implemented and confirmed, not merely gated in principle.**

## **Montage Removal / Day 2 Enactment — implemented, confirmed, 2026-09-14 (v37)**

**Per `docs/qa/MONTAGE_ARCHIVE_PASS.md` (new, untracked at time of verification), independently confirmed against source, not taken on the doc's own word. The montage sequence (`scripts/chapters/montage_still.gd`) is disconnected from the active flow of a new playthrough — its art and code are not deleted, just unwired — because the author now has enough authored content to fill the two days the montage was compressing. Day 1 is unchanged (existing intake, Naomi ID, first steward visit). Sleeping ends Day 1 and returns control on Day 2 at 6:00 a.m. Day 2 now has real content culminating in the steward's second visit, relocated to the smoking lounge, ending on the verbatim-confirmed line `THE STEWARD: "Come back tomorrow, and don't bring your badge."` (`dialogue/steward.dialogue`'s `steward_second` topic, `GATE: day >= 2 AND day < 3 AND (topic_done(steward, steward_first) OR topic_done(steward, steward_first_lead))`). Sleeping before that second visit is blocked with a redirect back to the lounge. After the second visit, sleeping advances to Day 3 at 6:00 a.m.; the existing plain-coat third steward visit is confirmed unchanged.**

**Old-save resume is deliberately preserved, not deleted: `chapter_one.gd::_load_game()` reads `elif state.montage_index >= 0: staging.draw_montage(self)` — gated entirely behind an old save already carrying a set `montage_index`, so the montage-resume path is structurally unreachable from a fresh game (a new game never sets `montage_index`) while remaining fully intact for anyone mid-montage on an existing save. `montage_index` itself is confirmed present in `case_state.gd` (repo root); `chapter_one_staging.gd` still contains the full still-renderer/montage-index logic, untouched.**

**Test coverage extends existing files rather than adding new ones, consistent with extending coverage for the new Day 2 beats: `tests/steward_sleep_flow.gd`, `tests/day_clock_flow.gd`, `tests/dialogue_live_flow.gd`, `tests/staging_flow.gd`, `tests/town_flow.gd`. Nothing in the montage granted evidence, so nothing needed to move to the new Day 2 content to avoid a regression; the new content has its own `GATE:`\-driven pacing rather than inheriting the old montage's timing.**

## **`$84,000` Formatting Item — resolved, closed (both known instances)**

**Original instance resolved, per Dejunai, 2026-09-12. This was a plain text-rendering defect, not a deliberately engineered glitch: the `$` character failed to render correctly in the wage-claim dialogue script. The fix removes the currency symbol from the source entirely — the amount is spelled out in words ("eighty-four thousand dollars"), with no numerals or symbol present anywhere in the line. This is also exactly why the string was never reproducible by searching for `$84,000` or any numeral form: it does not exist in that shape anywhere in the corpus, in the two lines this fix touched (`county_clerk.dialogue`, `odell_precinct.dialogue`). This is unrelated to, and far less dramatic than, an external "leaked design docs" account (an AI-generated podcast) describing this as an intentional, expensive UI glitch that simulates real-time save-file corruption. That account is fabricated and should not be treated as a source for anything about this project.**

**A separate, previously-unnoticed instance of the same defect was confirmed live, 2026-09-14, by direct read: `county_clerk.dialogue`'s `clerk_ophion_1` `NOTEBOOK:` entry — a different line from the two the original fix touched — read `"1820s probate records: $84,000 insurance paid to Pruitt/Corliss/Wexford syndicates..."` with the literal `$` and numeral form intact. Fixed the same day: that line now reads "eighty-four thousand dollars," with no `$` or numeral present, confirmed by direct edit of the source file. Both known instances of this defect are now closed; the string does not exist in that shape anywhere in the corpus.**

## **Instrumental Voice Cue System — confirmed architecture**

**The wordless instrumental voice pack (originally briefed to Grok — see `voice_pack_brief_grok.md`) is wired into every authored NPC line, and the mechanism is confirmed against source rather than inferred:**

* **`VOICE: cue_id` is authored immediately before an NPC's spoken line in the `.dialogue` source.**  
* **The parser attaches that cue to the line as part of compilation.**  
* **The runtime emits a `[speaker, text, voice]` triple per line (extending the prior `speaker`/`text`\-only shape).**  
* **Chapter One plays the matching WAV when the dialogue card appears on screen, and stops it when the player advances past that line.**  
* **A 144-entry manifest validates cue names during dialogue loading and automated content validation, catching typos or unregistered cue IDs.**  
* **Instrument voice volume has its own independent mixer control, separate from music/SFX.**  
* **Walter, narration, beats, object descriptions, notebook entries, and system text are intentionally silent — this is deliberate scope, not an oversight, and matches the design intent that the voice layer characterizes NPCs the player interrogates, not the protagonist or the game's own narration.**

**This resolves what earlier revisions (through v17) treated as an open or unconfirmed integration question. The same parser and runtime is the shared foundation for Chapters Two and Three; the remaining work is to provide protagonist identity per chapter and support their full spoken-audio manifests — see Part Four.**

## **`WEIGHT:` on `TOPIC: default` — implemented, confirmed against source and test**

**Ships in `dialogue_lang.gd`/`dialogue_runtime.gd` and is covered by `tests/dialogue_lang_flow.gd`. Confirmed mechanism:**

* **`WEIGHT: n` (a positive number, not restricted to integers) is authored as line-level metadata inside a `TOPIC: default` block, alongside `GATE:`/`LABEL:` on that topic.**  
* **Authoring on any topic other than `default` is a hard parse error — `"WEIGHT is only valid on TOPIC: default"` — not a silent no-op. A non-positive value (`WEIGHT: 0` or negative) is a second, separate parse error. This document's v20 draft, written before this reconciliation, guessed the misuse case would fail silently; source shows it is caught at parse time instead, which is the safer of the two behaviors and requires no further work.**  
* **Multiple `TOPIC: default` blocks are allowed, and the first eligible block controls selection. If it is unweighted, it wins deterministically, preserving specific-before-fallback gate cascades. If it carries `WEIGHT:`, the runtime draws only from eligible defaults that explicitly carry `WEIGHT:`; later eligible unweighted defaults remain outside the pool as deterministic fallbacks. This prevents ambient chatter from displacing required default conversations such as the gardener's plain-coat testimony.**  
* **No-immediate-repeat rule, confirmed exactly as v20 specified: the runtime tracks the winning default's source line per NPC and excludes it from the next draw's pool, falling back to allowing a repeat only when excluding it would leave the pool empty (i.e., a single-candidate default). This tracking is by source line, not by line text, session-scoped, and explicitly not saved — `docs/DIALOGUE_AUTHORING.md` calls this "cosmetic."**  
* **Defaults never appear as menu entries, weighted or not. Non-`default` topic IDs must stay unique per NPC as before; sharing a topic ID across different NPCs still intentionally supports `topic_count()`.**  
* **Authoring guidance (from `docs/DIALOGUE_AUTHORING.md`, not a parser requirement): use weighted defaults for interchangeable exhausted/ambient remarks, and keep required evidence, notes, progression, and unique information in deterministic (non-default) topics instead.**  
* **Scope discipline holds exactly as intended when this was proposed: this is presentation/repetition variety only. No new evidence, gates, topics, or narrative content is implied or required — existing single-line defaults remain fully legal and behave exactly as they did before this feature shipped.**

**This closes the "specified, not yet implemented" status this document's v20 gave the feature. `docs/DIALOGUE_AUTHORING.md` already documents it for authors; this is its first appearance in this TDD.**

## **`FORK:`/`OUTCOME:` Resolution With Recap — implemented, confirmed against source and test, 2026-09-12 (supersedes this section's prior "auto-retirement" description)**

**Ships in `dialogue_lang.gd`/`dialogue_runtime.gd`/`dialogue_state.gd`, covered by `tests/dialogue_template_flow.gd` (the earlier `tests/dialogue_lang_flow.gd` predates this behavior and does not exercise it). Codex implemented this independently, reported to the author as "on it" with no spec handed over; this section is written from direct source and test reading, not from that report. Confirmed mechanism, and it is materially different from — and simpler than — both this document's prior "auto-retirement" description and its own speculative "companion recap topic" proposal below:**

* **Inside a `FORK:` block, any `CHOICE:` branch may additionally contain `OUTCOME: decision_id = value_id` — both sides must be a simple identifier, or the parser raises a parse error, exactly as before. The chosen value is held as a pending outcome for the session and only committed to `dialogue_state.gd`'s `outcomes` dictionary once the branch finishes playing; `set_outcome()` still refuses to overwrite an existing key, so a decision is immutable for the rest of the playthrough.**  
* **`topic_available()` no longer inspects `outcome_keys` at all — it is now pure `GATE:` evaluation. A topic with a resolved `OUTCOME:` fork is NOT excluded from the NPC's menu. This replaces the auto-retirement mechanism this section previously documented (that mechanism no longer exists in source).**  
* **Instead, `render()` computes which of a topic's `outcome_keys` already have a recorded value (`locked_outcomes`), and when playback reaches a `FORK:` step, `_locked_fork_options()` filters the presented `CHOICE:` options down to only the one(s) whose authored `OUTCOME:` matches what was already chosen — so reopening a resolved fork's topic shows the full conversation again, with the fork menu itself narrowed to one option: the branch actually taken. The rejected branch is not offered and cannot be reached again.**  
* **Replay is genuinely free: `dialogue_state.gd`'s `record_fact()`, `discover()`, `complete_topic()`, and `set_outcome()` are all idempotent (guard on existing keys/values), and `commit_through()`'s `TIME:` charge is gated on `first_completion`, so replaying the locked branch fires no duplicate `NOTEBOOK:`/`EVIDENCE:` effects and charges no additional day-clock time. Confirmed directly by `tests/dialogue_template_flow.gd`: "Committed OUTCOME must leave its source topic available for review," "Replay must expose only the previously committed branch," and "Replaying a resolved decision must not charge time again."**  
* **No separate authoring is required to get recap behavior — unlike this document's own prior speculative design (a companion `TIME: 0` recap topic per branch), the same original topic replays itself, filtered. Net authoring cost for a fork opting into this is exactly `OUTCOME:` on each `CHOICE:` and nothing else; the `GATE:` does not need `NOT topic_done(...)` or `NOT outcome(...)` for this purpose.**  
* **Forks using only `CHOICE:` (no `OUTCOME:`) are unaffected and keep their plain repeatable behavior, as before — this stays opt-in per fork.**  
* **Not independently confirmed this pass: the author reports Captain Odell has one fork that deliberately stays fully one-shot (no recap), because it is his automatic opening/intake conversation and replaying it would interrupt every later Odell visit. No `FORK:` was found in `odell_precinct.dialogue` itself during this reconciliation — that conversation likely lives in a different, non-`.dialogue`\-file scripted flow (e.g., the hardcoded intake sequence). Recorded as reported, not source-confirmed; worth tracing if it ever needs to change.**

**This supersedes the former "Unprotected Forks — resolved, closed" section below and the "auto-retirement" framing this section previously gave `OUTCOME:`. It also closes Part Four's "Fork revisit-with-recap pattern" — see there.**

## **Unprotected Forks — superseded, see above**

**A live dialogue-review pass, 2026-09-12, found eight `TOPIC:` blocks whose `FORK:` had no repeat-protection at all: no `NOT topic_done(npc, topic_id)` in the `GATE:`, and no `OUTCOME:` on any `CHOICE:` branch. Affected: `post_office_clerk.dialogue` (`ophion_club_mail`), `county_clerk.dialogue` (`wage_claim_inquiry`), `gazette_editor.dialogue` (`press_the_arithmetic`), `local_historian.dialogue` (`dr_fenn_library`), `mr_vale.dialogue`/`quay_bookkeeper` (`missing_cart`), `mr_whitehouse.dialogue` (`name_the_dead`), `school_parent.dialogue` (`the_estate_talk`), and `widow_kessler.dialogue` (`butcher_parcels`). The interim fix (that day) added `NOT topic_done(npc, topic_id)` to each `GATE:`, making all eight permanently one-shot. Confirmed this pass: Codex has since migrated all eight to the resolution-with-recap mechanism above — each `CHOICE:` now carries its own `OUTCOME:`, and the interim `NOT topic_done(...)` clause has been removed from every one of the eight `GATE:` lines, verified directly against the current source of all eight files. They are no longer permanently one-shot; each now recaps its chosen branch on revisit per the mechanism above.**

## **CHOICE:/FORK: Shared Interpreter and Consequential Outcomes**

**`FORK:` choices still hardcode Walter as the player speaker and remain silent in Chapter One — no voice cue plays for a fork prompt itself. `VOICE:` immediately before `FORK:` is deliberately rejected; spoken lines inside a branch can carry normal voice cues. The project extends this interpreter for Chapters Two and Three rather than replacing it, so protagonist labeling and full spoken-audio playback must become chapter-provided concerns before their dialogue is integrated — see Part Four. `OUTCOME:` (above) now supplies the persistent, once-only choice residue needed across all three chapters; existing `CHOICE:`\-only forks keep their current replay behavior.**

## **Presentation Frame — current state**

**Dialogue cards, NPC topic menus, and Walter's response menus share the warm wood-and-brass conversation frame. Physical examinations use a separate blue-gray archival frame — a deliberate visual split between "talking to someone" and "examining something," rather than one generic panel style for all player-facing text. The shared card renderer keeps the Continue control anchored to the bottom of the presentation frame regardless of text length, so a short line and a long line don't shift where the player's eye goes to advance.**

## **Background NPC Template Extension — resolved, closed**

**Codex confirms `background_npc_template` is back to the correct `.dialogue` extension; this document's own read of the delivered zip already showed no `.dialogue.txt` anywhere in the corpus. No further action needed.**

## **Dialogue Authoring System — provenance**

**The dialogue grammar and interpreter (`dialogue_lang.gd`, `dialogue_runtime.gd`, `dialogue_state.gd`) were designed and built by the project's author working with a Claude Sonnet instance in VS Code. Codex's role extends beyond integration: live-wiring the interpreter into actual play, subsequent fixes and extensions to the interpreter itself — including the schedule-alias handling in `dialogue_catalog.gd`'s `slot()`, the `VOICE:`/cue-manifest extension, and the `WEIGHT:`/`OUTCOME:` extensions confirmed above — and authoring `docs/DIALOGUE_AUTHORING.md` (September 10, kept current since) as a writer-facing reference, confirmed substantively accurate against the interpreter's actual behavior. As of 2026-09-12, Codex also maintained its own parallel copy of this document directly against source, at its own v18 at that time; this revision reconciled this project-doc copy against that source of truth rather than letting the two drift further apart. Reframed v33: that v18 figure is a dated historical statement, not a claim about Codex's current version — this session has no `device_bash`/git access to confirm what, if anything, Codex's own copy is at now.**

**Dialogue-content provenance, distinct from the interpreter/engine provenance above, recorded 2026-09-14 (v36): the author leads dialogue content specifically — the actual `TOPIC:`/`NOTEBOOK:`/`EVIDENCE:` writing and editorial judgment. Antigravity is executing dialogue-content work under that lead, including the current Stage 2 scheduled-life population pass (the NPC/topic-count growth this document is actively tracking as it happens — see Part Two) and the missable-NPC/fork-lock audit and fixes in this document's v36 revision note. Perplexity provides research support for dialogue content; Copilot 365 provides ideation support. Codex's own role remains interpreter/engine-side, as described above, distinct from this dialogue-content track.**

**Object-evidence system provenance, recorded 2026-09-14 (v37): built by the author working with a separate Claude Sonnet session in VS Code, under the author's direct lead — the same working pattern that originally produced the dialogue grammar and interpreter above. Codex reviewed the first migration pass independently and found the four gaps closed in "Object-Evidence Authoring System" above. `docs/OBJECT_AUTHORING.md`'s own byline currently credits "Copilot" — confirmed by the author to be a GitHub-tooling attribution stamp applied regardless of which agent actually wrote the code, not real attribution; both the author and the implementing agent prefer it credited as Claude Sonnet (VS Code). Flagged here as a correction for whoever next touches that doc — not made directly by this document, which doesn't own it.**

## **Object-Evidence Authoring System — implemented, migrated, and wired, 2026-09-14 (v37)**

**Replaces the prior state, where evidence like `lodging` lived scattered across GDScript object-interaction scripts — outside the dialogue system, invisible to any dialogue-only audit, and requiring engine-scripting fluency to touch. The replacement is a flat-file-per-object grammar authored in the same declarative style as `.dialogue` files: `GATE:`/`EVIDENCE:` are identical syntax to dialogue's, so object/evidence authoring is text editing, not engine scripting. It is not a 1:1 reuse of the dialogue grammar — object files carry their own directives dialogue files don't, the first being `TAKE: item_id` for pickup/collection (the author's own working name during design, `COLLECT:`, was superseded by the implementer's `TAKE:` — a deliberate naming call, not an inconsistency).**

* **Parser/runtime/state: `scripts/shared/object_lang.gd`, `object_runtime.gd`, `object_state.gd`, confirmed to exist and match `docs/OBJECT_AUTHORING.md`'s description. `object_lang.gd` has explicit `is_valid_identifier()` checks and parse-time errors for `TAG`, `TIME`, `EVIDENCE`, `TAKE`, `OUTCOME`, `NOTEBOOK`, and `OBJECT` ids — added during Phase 1 gap-fixing, see below.**  
* **`case_state.gd` moved to schema version 10, additively: `inventory`/`object_state` fields added, old save versions 1–9 still validate.**  
* **All eleven scoped hotspot ids are migrated and wired, confirmed by direct read of `chapter_one.gd`: estate's seven (`wounds`, `eight`, `knife`, `watch`, `gas`, `register`, `shoes`), town's three (`gazette`, `lodging`, `exemption`), and tunnel's one (`tunnel_record`) — deliberately excluding NPC-stand-in ids (which stay in `.dialogue` files) and tunnel's bespoke game-logic ids (which belong in neither grammar). Real content lives in `objects/estate.object`, `objects/town.object`, `objects/tunnel.object`. `chapter_one.gd::_interact()` calls `objects.interact(self, "estate", id)`; `_town_interaction()`/`_town_observation()` call `objects.interact(self, "town", id)`; `_tunnel_interaction()` calls `objects.interact(self, "tunnel", id)` — through a new adapter, `scripts/chapters/chapter_one_objects.gd`, mirroring the shape of the existing `chapter_one_dialogue.gd` adapter.**  
* **FORK/OUTCOME presents live choices through `chapter_one_objects.gd::_present_fork()`/`_choose()`, which calls `ObjectRuntime.resume()`. `LABEL:` drives hotspot hover text through `sync_points()`, which overwrites `points[id]["title"]` from the label on every sync.**  
* **Old hardcoded fallback content (e.g. `tunnel_record`'s prior `_cards([["THE LAST SUPPORT",...)` call) is left physically present in `chapter_one.gd` but dead/unreachable — the function returns before reaching it once the object call succeeds. Matches the migration doc's own documented "leave the old fallback, it's harmless once dead" pattern; not yet cleaned up, not a bug.**  
* **An independent review (Codex, reviewing the first migration pass separately from the implementing agent) found and confirmed-fixed four gaps: FORK/OUTCOME wasn't actually presenting live choices; `LABEL:` wasn't actually driving hover text; `INCLUDE`d files were dropping repeated cascade variants; and `OBJECT`/`TAG`/`EVIDENCE`/`NOTEBOOK` ids plus `TIME` weren't validated at parse time. All four are confirmed resolved in current source, not merely claimed.**  
* **Known, accepted limitations, carried forward as open items in Part Four: hotspot visibility is one-way only (`sync_points()` erases a hotspot once its `GATE:` goes false, but nothing restores it if the `GATE:` later becomes true again — not a live bug today, since every current `GATE:` is one-directional by nature); and unknown `GATE:` fields/functions still silently parse instead of raising an error.**  
* **Documentation: `docs/OBJECT_AUTHORING.md` (the grammar/authoring reference) and `docs/OBJECT_MIGRATION_HOWTO.md` (the practical migration recipe), both independently verified clean against source, specific falsifiable claims checked line-by-line with no discrepancies found. `docs/ARCHITECTURE.md` (new, cross-cutting) independently corroborates the same state. Test coverage: `tests/object_template_flow.gd` (grammar/template mechanism — GATE cascade, FORK/OUTCOME/TAKE on fixture objects) and `tests/object_content_flow.gd` (audits the real shipped `.object` files directly and drives a live FORK through the adapter).**

**Provenance: built by the author working with a separate Claude Sonnet session in VS Code, under the author's direct lead, mirroring how the dialogue grammar and interpreter were originally built (see "Dialogue Authoring System — provenance" below). An independent Codex review caught the four gaps above before this section was written.**

**Portal authoring system provenance, recorded 2026-09-14 (v38): built by the author working with a separate Claude Sonnet session in VS Code (this document's own revision historian, per the byline above), under the author's direct lead, mirroring exactly how the dialogue and object systems were each built. Codex reviewed the migration in two passes and found the five defects closed in "Portal Authoring System" below — three integration defects on the first pass, two smaller hardening items on the second, all five confirmed fixed in source before this section was written, the same standard applied to the object-evidence review above.**

## **Portal Authoring System — implemented, migrated, and wired, 2026-09-14 (v38)**

**A third sibling to `.dialogue` and `.object`, not built on either. A travel point's defining content is *where it leads*, not what it says, so alongside the same shared GATE grammar and `PORTAL:` id-cascade convention the object system already established, its step vocabulary adds one thing neither other format has: `GO: destination | x,y,z | yaw | flags`. Reaching a `GO` halts step processing immediately and hands control back to the caller, exactly like `FORK`, just with no choice to make — only `chapter_one_portals.gd`, never the shared runtime, actually calls `_travel()`. Content authored after a `GO` renders as arrival narration once the caller resumes via a new `Runtime.after_go()`, the `GO`-specific counterpart to `resume()`.**

* **Parser/runtime/state: `scripts/shared/portal_lang.gd`, `portal_runtime.gd`, `portal_state.gd`, mirroring `object_lang.gd`/`object_runtime.gd`/`object_state.gd`'s shape exactly, confirmed to match `docs/PORTAL_AUTHORING.md`'s description.**
* **`case_state.gd` moved to schema version 11, additively: `portal_state` added, old save versions 1–10 still validate.**
* **Ten unique portal ids are migrated and wired, corrected v40, 2026-09-15 (Codex review), across five live `.portal` files, wording corrected v41, 2026-09-15 — a sixth file, `background_portal_template.portal`, is a documented cookbook/template containing only non-canonical `example_*` placeholder ids and contributes no live portal to the count. This document's v38/v39 "four scoped travel points" undercounted the system: `service_entrance` and `exit` (`portals/estate.portal`, the latter locked/unlocked on `visited(almy)`); `street_precinct`, `street_almy`, `street_room`, `street_estate`, and `route_post` (`portals/town.portal`); `lounge_exit` (`portals/lounge.portal`); `route_speakeasy` (`portals/lower.portal`, phase/coat-gated, three blocks — a blocked attempt always surfaces an authored dialogue card, morning/noon-locked `CELLAR BULKHEAD` or coat-mismatch-locked `THE VIEWING SLIT`, never a toast); and `tunnel_exit` (`portals/tunnel.portal`, two destinations depending on `evidence(lower_foundation)`). `chapter_one.gd::_interact()` calls `portals.interact(self, state.world, id)` generically — unlike objects, which are only ever called for three hardcoded known locations — through a new adapter, `scripts/chapters/chapter_one_portals.gd`, mirroring `chapter_one_objects.gd`'s shape.**
* **`TIME:` semantics, corrected v40, 2026-09-15 (Codex review): `portal_lang.gd` always fills an omitted `TIME:` with the string `"3"`, a valid float — so there is no live code path that "leaves `_travel()`'s own automatic `DayClock.travel_cost()` charge alone." Every portal, whether `TIME:` is omitted (defaulting to 3) or explicitly authored (including `TIME: 0`), takes the same branch in `_perform_go()`: the amount is applied once, the first time this portal identity completes (checked via `portal_done()` before charging), exactly like `object_runtime.gd::commit_through()`'s own `first_completion` guard for the equivalent object case. This document's v38/v39 language describing a second, "automatic `_travel()` charge" branch for an omitted `TIME:` was wrong and is retired here.**
* **Cross-system sharing, matching the object system's own precedent: `OUTCOME`/`flag()` read/write through the shared `dialogue_state` instance; `evidence()`/`filed()` reuse `dialogue_runtime.gd`'s helpers; `dialogue_runtime.gd` and `object_runtime.gd` both gained matching `portal_done`/`portal_count` entries in their `make_context()`, and `object_runtime.gd` also gained `spoken_to`/`taken`. A new `visited(id)` GATE function (reading `case_state.gd`'s `visited` array directly) was added specifically because it is subtly stricter than the existing `spoken_to(npc_id)` (true the instant a conversation merely begins, not once it fully finishes) — `service_entrance`'s original hardcoded gate needed the stricter sense exactly, and picking the wrong one would have silently unlocked the door one exchange earlier than before.**
* **A small, explicitly named escape hatch (`_before_travel()`/`_extra_cards()` in `chapter_one_portals.gd`) handles the rare travel point whose post-arrival content is genuinely dynamic — computed from live state, not static prose — rather than forcing it into the flat grammar: currently just `tunnel_exit`'s custody-result card (which branches on which supplement was sent) and `lounge_exit`'s `state.lounge_exited` flag. Documented as a known, accepted limitation in `docs/PORTAL_AUTHORING.md`, the same principle the object/dialogue systems already established for their own similarly dynamic content.**
* **An independent Codex review, across two passes, found and confirmed-fixed five defects before approving the system for current content. First pass, three integration defects: (1) `route_speakeasy`'s GATE could be bypassed entirely when reached from the contiguous town's shared exterior, where `state.world` stays `"town"` and never becomes `"lower"` — fixed by checking `portals.interact(self, "lower", id)` explicitly in `chapter_one.gd::_town_interaction()` before the raw, ungated `estate.routes` dispatch that district geometry registers unconditionally; (2) an immediate `GO` with no post-arrival cards never called `commit_through()` on its resumed segment, so it never actually completed — fixed by always routing the resumed result through `_play()`/`_advance()` rather than special-casing an empty card list; (3) an authored `TIME:` was applied after `_travel()`'s own transition telemetry rather than before, so the logged event reflected the pre-charge clock — fixed by reordering the charge ahead of the `_travel()` call. Second pass, two hardening items: authored `TIME:` charged on every crossing rather than once per identity (see the `TIME:` paragraph above); and unknown `GO` flags parsed silently instead of erroring — fixed by validating flags at parse time against exactly `nosave`/`elapsed`.**
* **Codex's own stated gap that motivated the second review's request: the original test suite (`tests/portal_lang_flow.gd`, `tests/portal_template_flow.gd`) proves the parser/runtime by manually calling `commit_through()`/`after_go()`, which is exactly why the completion defect above escaped it. A new `tests/portal_content_flow.gd` closes that gap: it drives every migrated route through the actual `chapter_one.gd` adapter in a live `main.tscn` instance, including `route_speakeasy` reached via a real `_travel("town", ...)` into the contiguous exterior (not a direct-to-`"lower"` shortcut), and a synthetic `TIME:`-bearing portal proving the once-per-identity charge.**
* **Documentation: `docs/PORTAL_AUTHORING.md`, mirroring `docs/OBJECT_AUTHORING.md`'s structure, including its own "known limitation" section for dynamic post-arrival content. `docs/ARCHITECTURE.md` and `README.md` both updated to match.**

## **Fail-Loud Guards — implemented, object and dialogue, 2026-09-14 (v37)**

**Design intent, per the author: stop broken or incomplete gate logic from letting an NPC or object go silently unresponsive — an NPC turning into an "unresponsive totem pole," in the author's words — instead of surfacing an obvious on-screen developer error. The original proposal was a default no-gate topic appended to every `.dialogue` file reading "EOF Error, Please report to developers"; revised before implementation, on the author's own reconsideration, to one centralized check per system instead of duplicated boilerplate across ~70 dialogue files today and every future one.**

* **Object-side: `chapter_one_objects.gd::interact()`/`_fail_loud()`. Fires when a hotspot the player can actually click (already present in the location's `.points`) resolves to no currently-eligible `GATE:` block. `push_error()`s and shows an on-screen "EOF ERROR — PLEASE ALERT THE DEVELOPERS" panel naming the location and object id. Deliberately lives in the chapter adapter, not the shared `object_lang.gd`/`object_runtime.gd`: an object file having nothing eligible is often *correct* (the hotspot may simply not exist yet), so a shared parser-level fallback would misfire on legitimate absence — only the adapter, which already knows the hotspot is clickable, can distinguish the two cases.**  
* **Dialogue-side: `chapter_one_dialogue.gd::interact()`, landing in the chapter adapter rather than the shared `dialogue_runtime.gd` (mirroring where the object-side guard landed). Fires when `Runtime.enter()` returns an empty session *and* the NPC's menu has zero eligible entries *and* the actor isn't `"odell"` (the documented exception, matching Odell's one-shot intake conversation and `DIALOGUE_AUTHORING.md`'s "explicit exceptions like Odell's post-answer notebook dismissal"). `push_error()`s `"DIALOGUE FAIL-SAFE: NPC '%s' has no eligible default or menu topics! (EOF Error)"` and synthesizes a one-card session showing `[DEVELOPER WARNING]` / `"EOF Error: NPC '<npc_id>' has no available dialogue state. Please alert developers."`**  
* **Both guards confirmed live by direct, repo-wide grep for the literal error text (`"DEVELOPER WARNING"`, `"EOF Error"`, `"no available dialogue state"`): matches occur only in the two chapter adapters, nowhere else in the corpus. Documented in `docs/OBJECT_AUTHORING.md` ("Fail-loud: an unresolved hotspot is an error, not silence") and in `docs/DIALOGUE_AUTHORING.md`/`README.md`, both confirmed accurate against source.**

## **Outside Audit — Manus AI, spot-checked, 2026-09-14/15**

**First appearance of a new outside tool this pass, nicknamed "Mangus" by the author. Delivered `Three Colors of Madness — Source-Backed Project Audit.md` against commit `23a2269` on `dev/contiguous-town-phase-2`, the same HEAD independently confirmed via `git log -1` at the time. Unlike prior secondhand reports, this one claimed direct repo access and actual Godot 4.7.2 test execution rather than static reading alone — a materially stronger evidentiary bar, comparable to Codex's own "real Godot executable" passes. Per this document's standing rule, treated as reported, not accepted at face value: a representative, explicitly non-exhaustive sample of its most load-bearing and most surprising claims was independently spot-checked against real source.**

* **CONFIRMED: `dialogue/` contains exactly 71 `.dialogue` files, matching the audit's "70 concrete NPC definitions plus one template" claim, consistent with Part Two's own 70-NPC figure.**  
* **CONFIRMED: `tests/test_placement_audit.gd` line 55 is literally the truncated identifier `pri` with nothing after it — a genuine parse error. This test cannot currently run. See Part Four.**  
* **CONFIRMED AND EXPANDED, 2026-09-15: `AVERAGE JOE` is a shared literal speaker label used across twelve distinct dialogue files, not the four originally sampled — `business_resident_01`–`06` and `pickman_resident_01`–`06`. The instrument-family split is systematic, not random: odd-numbered files (`_01`, `_03`, `_05`) use `trombone_*` `VOICE:` cues, even-numbered files (`_02`, `_04`, `_06`) use `violin_*` cues, consistently across both the business and Pickman sets. Each individual file is internally consistent — one instrument family per file — so the collision is only in the literal speaker string being shared across all twelve. This is still the audit's specific root-cause claim for `tests/instrument_voice_flow.gd` failures: a test keyed on the literal `AVERAGE JOE` label, rather than per-file/per-NPC identity, would see both instrument families under one name and fail, even though no single file mixes them itself. Per the author: `AVERAGE JOE` is a known placeholder, not a naming decision — see Part Four for the open todo to replace all twelve with authentic period-appropriate names, which resolves this collision as a side effect rather than as its primary purpose.**  
* **CONSISTENT WITH, NOT CONCLUSIVE PROOF OF: `gatehouse_boy.dialogue` exists on disk, and `tests/staging_flow.gd` does still assert against the pre-migration pattern (`g.dialogue.cards==g.Story.SCENES.boy`/`boy_repeat`/`boy_return`) — matching the audit's "stale regression test after dialogue migration" claim for the staging check, though this session has not traced whether the assertion actually fails at runtime or merely looks stale by inspection. See Part Four.**  
* **DISCREPANCY, NOT RESOLVED: the audit's `town_expansion_flow` claim (that it "expects the former `route_lower` transition from Pickman Street," causing an invalid dictionary access) does not match current source as literally stated — a repo-wide search for `route_lower` finds it only in `tests/contiguous_town_phase_two_flow.gd` (asserting it's correctly *absent*) and `contiguous_town_phase_two.gd` (cleanup code), not in `tests/town_expansion_flow.gd` itself, which has no `route_lower` reference at all in current source. Not resolved here — possible explanations include the test having already been fixed post-audit without a version bump, the audit describing a different file, or a gap in this session's own check — flagged rather than settled. See Part Four.**  
* **REACHED BUT NOT FULLY RESOLVED: whether a blocked `route_speakeasy` entry shows a dialogue card or a toast. `tests/speakeasy_flow.gd` does assert a `"CELLAR BULKHEAD"` dialogue card; the interaction id is built in `scripts/chapters/lower_street.gd` (scene construction only), with dispatch not yet traced to a specific card-vs-toast branch.**  
* **RESOLVED BY EXPLANATION, NOT A LIVE BUG: the audit reported `CaseState.VERSION = 10` (correcting this document's then-stale "schema 9" line); direct source read at the time showed `const VERSION = 11`, with `portal_state.pack()` already in the payload. The audit's clone was of the published repository, not local working files — local commits were unpublished at the time pending Codex's portal-system review (since resolved, see Part Three, "Portal Authoring System"). Practical rule going forward: read any future outside-tool audit as "as of whatever was last pushed," not "as of local HEAD," until told otherwise.**  
* **NOT INDEPENDENTLY CHECKED THIS PASS, still reported-only: the 705-NPC-voice-line figure (against this document's own 614), the 438-topic figure (against this document's own 454, itself already a moving target — see Part Two), the pacing/title-settings/prologue-input test failures, the Chapter Two procedural-trinket-proof claim, and the Web export playthrough assessment.**

**Overall takeaway: this audit reads as substantive and largely accurate where checked — a real, valuable outside pass, not noise — with the caveat that a couple of specifics (schema version, the `route_lower` claim) were already moving targets or need a second look, not confirmed defects in the audit's methodology. Whether to work through the remaining unverified claims, or treat what's confirmed here as sufficient to act on (fix the two genuine bugs below, then reassess), is the author's call.**

# **Part Four — Architecture Decisions Needed**

**CHOICE:/FORK: the shared-interpreter decision is settled. Chapters Two and Three will extend the same parser/runtime. Before Chapter Two dialogue lands, the hardcoded Walter choice label must move to chapter-provided protagonist identity, and its full-voice asset contract must be added without breaking Chapter One's instrumental cues. `OUTCOME:` now supplies the persistent, once-only choice residue needed across all three chapters — this part of the decision is closed, see Part Three.**

**Odell-precinct topic-gate namespace defect — resolved, closed. All three precinct topics now gate on `topic_done(odell_precinct, ...)`, with regression coverage for location-specific completion.**

**Story-actor vs. catalog-resident staging — clarified, not a defect. See Part Three. Two legitimately separate systems, not one system applied inconsistently.**

**`$84,000` formatting — RESOLVED, closed 2026-09-12 for the original two lines and 2026-09-14 for a separately-discovered `NOTEBOOK:`\-line instance. See Part Three. Plain text-rendering defect caused by the `$` character; fixed in all three known instances by spelling the amount out in words with no currency symbol or numerals.**

**Window/door mesh clipping in interiors — RESOLVED, closed 2026-09-13 (commit "windows are not doors"). The precinct interior's left window (at `x = -5`) visually clipped through a door mesh at the same wall; the fix removes that window while keeping the precinct's right window and its morgue door intact. The `stationer` and `haberdasher` shop interiors had the mirror-image defect — their right window (at `x = 5`) clipped the "rooms above" door — fixed by removing that window while keeping each shop's left window and rooms-above door intact. Confirmed by a new `tests/precinct_window_flow.gd`, which asserts each interior's surviving/removed window and untouched door by exact mesh position, not by description.**

**Revelation pacing — resolved as policy. Invent freely now, prune deliberately later, per the author.**

**`WEIGHT: n` on `TOPIC: default` — IMPLEMENTED, confirmed 2026-09-12 against source and test. See Part Three for the full confirmed mechanism. No further work needed on the feature itself; `docs/DIALOGUE_AUTHORING.md` already documents it for authors.**

**`FORK:`/`OUTCOME:` one-time resolution — IMPLEMENTED, confirmed 2026-09-12 against source and test. See Part Three. Closes the former "repeatable forks" item below.**

**Fork revisit-with-recap pattern — IMPLEMENTED, confirmed 2026-09-12 against source and test. See Part Three, "`FORK:`/`OUTCOME:` Resolution With Recap." Raised while fixing the eight unprotected forks (Part Three): the original one-shot fix (`NOT topic_done(npc, topic_id)`) retired the whole topic permanently, so a player who picked `CHOICE` A could never revisit that NPC to be reminded what A said. This document's own first proposal (a companion `TIME: 0` recap topic per branch, tripling topic count) was never built. Codex instead built a cheaper, simpler mechanism directly in the runtime: `topic_available()` no longer excludes a topic once its outcome resolves, and `_locked_fork_options()` narrows the `FORK:` to only the chosen branch on replay — no companion topics, no extra `decision_id` bookkeeping beyond the `OUTCOME:` already on each `CHOICE:`. Confirmed by direct source reading (`dialogue_runtime.gd`) and by `tests/dialogue_template_flow.gd`'s explicit assertions. All eight of the originally-unprotected forks have been migrated to this mechanism (confirmed against their current source — see Part Three). Captain Odell has one fork left deliberately one-shot, because it's his automatic opening/intake conversation. Located, 2026-09-14 (v34), by direct read — not `odell_precinct.dialogue` (his later, returning-staff identity), but `odell.dialogue`'s `TOPIC: default` block (`GATE: NOT topic_done(odell, default)`): the `FORK:` sits inside the one-shot default greeting itself, so it retires through the ordinary default-topic mechanism rather than through an explicit `OUTCOME:`. This closes the previously-open "not found this pass" item.**

**Tunnel exhausted-resources fail state — still open, unchanged.**

**Conversation-time/travel-time scale mismatch — open, diagnosed, no solution chosen, 2026-09-12. The author observed that talking to an entire district's worth of NPCs never advances `DayClock.phase()`. Root cause, confirmed against source: two separate time-charging mechanisms coexist. `day_clock.gd`'s `CONVERSATION_MINUTES = 30.0` only fires for a small enumerated list of legacy story-scene conversations (the `ESTATE_TALKS`/`TOWN_TALKS` dictionaries — `boy`, `assistant`, `gardener`, `identify`, `behan`, etc.), not for ordinary dialogue-file topics. Regular `.dialogue` file topics instead go through `dialogue_runtime.gd`'s `commit_through()`, whose real default is `const DEFAULT_MINUTES = 3.0` — matching the author's original recollection exactly. The author has already authored explicit `TIME:` overrides on 38 of the 42 current dialogue files (values mostly 3–10, some to 15, several deliberate 0s for pure description beats), so the grammar is not unused, contrary to this document's own initial (incorrect) read during this pass. The actual problem is scale: `TRAVEL_MINUTES = 30.0` is roughly 10x `DEFAULT_MINUTES = 3.0`, so almost all of a day's clock movement comes from crossing between districts, not from conversation length — a full local roster of NPCs can be exhausted for a fraction of what one district crossing costs, which is why `phase()` never moves during a district-local conversation binge. Two directions were named but neither chosen: raise conversation-time costs enough to rival travel (e.g., defaults closer to 10–15 rather than 3–5), or shrink the phase thresholds (`NOON`/`EVENING`/`NIGHT`) to match the scale conversations already operate at. Third factor, confirmed against source, 2026-09-12: a passive real-time tick also runs independently of conversations and travel. `chapter_one.gd`'s `tick_world(delta)` calls `DayClock.advance(state, delta*DayClock.WANDER_RATE)` every frame while `page == "play"`, where `WANDER_RATE = 5.0/60.0` — the clock advances 5 game-minutes per real-time minute regardless of what the player is doing, including standing still. At that passive rate alone, the full `MORNING`\-to-`NIGHT` span (840 minutes) elapses in about 168 real minutes (2.8 real hours) of simply being in the free-roam world, with zero conversations or travel. So an idle or wandering player still eventually runs out of day on this passive tick alone — the scale mismatch above is specifically about conversation/travel activity failing to move the clock in proportion to what the player is doing, not about the clock being unable to move at all. Fourth factor, confirmed against source, 2026-09-12: the author's own playtesting moves through the world at an inflated, dev-only speed. `main.gd` sets `speed = 10.5` (accel `39.0`) when the `brisk` action (Shift) is held, versus `2.15` (accel `13.0`) normal walk — roughly 4.9x, and per the author this brisk value is itself already 3x whatever brisk speed is intended to ship at. Since `WANDER_RATE` accrues against real elapsed seconds, not distance travelled, moving between NPCs and exits at this speed sharply reduces the real time — and therefore the passive game-minutes — spent in transit compared to a normal-speed player. This means the author's own impression that "time isn't moving fast enough" is being formed at a movement speed no shipped player will ever use, on top of the conversation/travel scale mismatch already logged above — any tuning decision here should be re-checked at normal (non-brisk, or intended ship-speed) movement before being treated as representative. Not a priority while investigation logic is the focus — recorded for later tuning, by feel, once revisited. See Part Six. Fourth factor RESOLVED, 2026-09-13, confirmed against source: the developer-only brisk-speed inflation is no longer the default. `main.gd` now defines three tiers — `WALK_SPEED = 2.15` (unchanged), a new `BRISK_SPEED = 4.0` (the intended shipping Shift-sprint, \~1.9x walk), and `DEVELOPER_BRISK_SPEED = 10.5` (the old inflated value, \~2.6x the new shipping brisk speed, matching the author's own "already 3x" estimate closely). A new `developer_brisk` boolean, false by default and not save-persisted, gates which of the latter two Shift applies; `[F3]` toggles it, printing which pace is active. So ordinary Shift-held play now runs at the intended ship speed by default, and the old dev-fast value is opt-in per session for the author's own quick traversal, not silently active during every playtest. This directly resolves the concern raised alongside the other three factors: the author's own impression that "time isn't moving fast enough" was partly an artifact of testing at a speed no shipped player would use — future day-clock pacing reads (this item's first three factors remain open and undecided) should now be representative by default.**

**Playthrough telemetry — historical rationale for a decision now built and deployed; see the Debrief pairing, Transport decision, and Hosting paragraphs immediately below, and Part Six item 15\. Originally scoped 2026-09-13 as "architecture decision needed, not started" — corrected v33, since that framing now contradicts the paragraphs it sits beside in this same Part. The author wanted lightweight, anonymous instrumentation to answer exactly the pacing/design questions this document has been diagnosing by feel: a unique per-playthrough id (no personal data of any kind — no name, account, IP, or device identifier collected in the log payload itself) tagging a local event log, so external Web testers' actual behavior can be measured rather than inferred from the author's own (now-corrected) playtesting speed. The author's own requested metrics, verbatim in intent: real minutes from session start to the first meaningful objective; real and in-game minutes spent per district-to-district transition; number of distinct NPCs spoken to before each `DayClock.phase()` change; whether the player voluntarily checks the time once the Stage-5 pocket watch exists; whether a player ever misses a scheduled NPC and what they attempt next; real time required to return to the boardinghouse and sleep at the end of Day 3; and two subjective impressions — whether the town feels "alive and difficult to fully consume" versus "too large for the available day," and whether the player believes time passes during meaningful investigation specifically, not only while walking.**

**Feasibility assessment, by category, not yet handed to Codex to size formally:**

* **Mechanically loggable, and cheap: most of these events already funnel through a small number of existing chokepoints, so instrumentation is additive logging calls, not new plumbing. District transitions and both real and game-clock time already meet at `chapter_one.gd`'s `_travel()`/`tick_world()` and `DayClock`; which NPCs have been spoken to is already tracked in `state.visited`, appended once per completed conversation in `chapter_one_dialogue.gd`'s `_segment_done()`; phase changes are one `DayClock.phase()` read; the Day-3 boardinghouse/sleep milestone already has dedicated targets (`sleep`, `day_close`) to time against; a pocket-watch check event is one more log call once Stage 5 (Part Six) exists.**  
* **Mechanically loggable, but needs no new detection logic — solvable by offline analysis instead: whether a player "misses" a scheduled NPC has no existing in-game signal (there is no "you just missed them" message — an absent NPC's slot simply has nothing there, per `dialogue_catalog.gd`'s `slot()`). Rather than building live miss-detection, the cheaper path is to log raw location-and-timestamp events only, then reconstruct misses afterward by replaying the same deterministic schedule table against the logged times — `slot()` takes no player state that isn't already in the log. "What they try next" falls out of the same event sequence for free once logged.**  
* **Not mechanically loggable, but not left undone: the two subjective impressions ("alive and difficult to consume" vs. "too large"; whether time feels tied to investigation vs. walking) are self-reports, not telemetry — see the debrief pairing below (built, 2026-09-13), which is exactly the short exit questionnaire this bullet originally called for.**

**Debrief pairing — BUILT, 2026-09-13. Telemetry answers what a tester did; it says nothing about what they thought they were doing or how it felt. Rather than a free-text survey — this codebase has never built a text-entry widget anywhere; every interaction runs through the shared `_panel`/`_button`/`_paragraph` idiom (`chapter_interface.gd`) — this is two single-tap optional questions shown once, right after the Day 3 "close\_day" narrative cards resolve and before the town is marked finished (`chapter_one_staging.gd`'s `sleep()`, the Day-3-success branch, via two new local functions `_debrief_town_feel()`/`_debrief_time_natural()`). First: a five-way choice ("Alive, and hard to fully take in" / "Confusing" / "Too large for the time given" / "Easy enough to navigate" / "Skip"). Second, simplified from an originally-proposed free-text "why" down to the author's own correction ("we dont need why or why not really just a yes no check box"): Yes / No / Skip to whether time felt natural while investigating. Both answers land in one new `debrief` event (`town_feel`, `time_natural`, each including `"skipped"`) via a new `playthrough_log.debrief()` method; the Worker's `EVENT_FIELDS` allowlist and enum validation (`cloudflare/three-colors-worker/src/index.js`) were extended to match. Also closed in the same pass: `day3_bed_reached()` existed in `playthrough_log.gd` since it was first written but had no call site anywhere — it is now wired at the same Day-3-success branch, immediately before the close\_day cards play. See `docs/LOG_PLAYER_ASK.md` TASK 4 for the full spec.**

**Debrief pairing — VERIFIED, HARDENED, AND DEPLOYED, 2026-09-13 (commit "Verify and guard player debrief," by the author directly). The author found the above already on disk, kept the panel logic exactly as written, and closed two real gaps rather than reworking it: (1) a `debrief_sent` guard on `playthrough_log.debrief()`, matching the existing `day3_bed_sent` pattern, so a second call cannot double-log; (2) a `state.finished` early-return at the top of `chapter_one_staging.gd`'s `sleep()`, so revisiting the bed after the town is already finished returns straight to the ending screen instead of replaying the close\_day cards and the debrief questions a second time — a real defect the original implementation left open. Confirmed by two new automated tests, not taken on report: `tests/playthrough_log_flow.gd` exercises the full event lifecycle including `debrief()`'s de-duplication, and `tests/debrief_flow.gd` drives the actual panel end to end (both Skip choices, one `debrief` event with `town_feel`/`time_natural` both `"skipped"`, `state.finished` and the ending screen reached, and a second `sleep()` call afterward reaching the ending directly with no repeat panel and no second event). The Worker's matching validation is covered by a new `cloudflare/three-colors-worker/test.mjs` (valid batch stored, an unknown field rejected, a valid `debrief` stored, an out-of-enum `town_feel` rejected, and a real itch.io CORS preflight). The Worker itself was then redeployed and verified live — `docs/qa/PLAYER_LOG_ENDPOINT_HANDOFF.md` records Cloudflare Worker version `87e1bd4e-9646-468a-b3be-1397534feb4b`, with real GitHub Pages and itch.io preflights, a synthetic `session_start`, and a live `debrief` POST all returning HTTP 204\. Net effect: no deployment step remains outstanding on this feature — it shipped and was confirmed live in the same pass, ahead of the plan to get to it later.**

**Transport decision — MADE, 2026-09-13: automatic upload, not manual download-and-send. The Web export is the tester-distribution format specifically because it needs no installer (per the author's earlier framing — "the comprimise so testers don't have an installer"), which means `user://`\-written log files sit inside browser sandbox storage the author has no way to retrieve directly, unlike the author's own desktop testing. The author chose the automatic-upload path over a manual download-and-send flow, accepting the tradeoff named alongside the choice: the log payload itself will carry no personal data (per the design intent), but an ordinary web server's own access logs typically record the caller's IP address by default, at the infrastructure level, separate from anything this project writes into the payload — "no personal data collected" and "no personal data reaches any server, including infrastructure-level logs" are two different guarantees, and this choice accepts the former, not the latter, unless the hosting choice below is deliberately configured to avoid persistent IP logging.**

**Hosting — BUILT AND DEPLOYED, 2026-09-13; corrected v40, 2026-09-15 (Codex review). `https://three-colors-worker.dejunai.workers.dev`, source at `cloudflare/three-colors-worker/` in this repo. A full, direct read of `cloudflare/three-colors-worker/src/index.js` (141 lines) this pass shows the original "Worker\+R2 only" account above is stale: the Worker now **dual-writes** every accepted event batch to both D1 and R2 in a single `Promise.all` — `env.DB.batch(statements)` alongside `env.BUCKET_ONE.put(...)` — meaning the handoff doc's original Worker\+D1 ask (`docs/LOG_PLAYER_ASK.md`, for plain SQL aggregate queries) is in fact what's live, not a deviation from it. D1 writes are idempotent: `INSERT OR IGNORE INTO game_events (event_key, ...)`, where `event_key` is a SHA-256 hash of the JSON-stringified event (`eventKey()`, via `crypto.subtle.digest`), so a retried or duplicate batch cannot double-count. A new `conversation` event type is now allowlisted (`npc_id`, `topic_id`, `coat_state`, `world`, `day`, `phase`), with `coat_state` restricted to `["police", "plain"]` and `phase`/`new_phase` restricted to `["morning","noon","evening","night"]`. The privacy side is unchanged and reconfirmed against source: strict per-event-type field allowlisting, UUID-v4 session-id format validation, batch/size limits, CORS restricted to the real distribution origins (GitHub Pages, itch.io), and no request headers, IP, or user-agent ever written anywhere. This document's previous "offline script against R2" tradeoff no longer describes the current architecture — a live SQL query against D1 is now genuinely available. Codex's own claimed deployed-version id and a live-D1-retrieval test are reported only, not independently confirmed this pass (no network access from this session).**

**Telemetry field normalization and conversation-event semantics — recorded v41, 2026-09-15 (Codex review), confirmed by direct source read. Real-duration fields are not uniformly rounded against game-time fields: every real-seconds field — `real_seconds_elapsed` (`district_transition`), `real_seconds_since_day3_start` (`day3_bed_reached`), and `total_real_seconds` (`session_end`) — is computed via `roundi(...)` in `scripts/shared/playthrough_log.gd` (whole seconds), while `game_minutes_elapsed` (`district_transition`) retains `snappedf(..., 0.1)` (one decimal minute); all D1 columns are typed `REAL` regardless, so this is a documentation note, not a schema issue. Separately, `conversation` events fire only once a dialogue segment's complete authored path finishes: `chapter_one_dialogue.gd::_segment_done()` calls `g.playthrough_log.conversation(...)` behind an explicit source comment — "Log only after the full authored path completes. Opening a topic menu, abandoning a card sequence, and intermediate FORK segments are not completed conversations." Reached either as `dialogue.start()`'s completion callback (all cards in the segment exhausted) or via the resume-from-save path once `offset == segment.cards.size()`.**

**Missable-NPC redundant-carrier pattern — AUTHORED, 2026-09-13 (v32). All twelve load-bearing facts from the shortlist below now have a second-source topic committed to the repo; see this document's v32 revision note above for the full NPC/topic assignment list. Verification status, corrected 2026-09-14 (v36): generic `dialogue_lang_flow.gd`/`dialogue_template_flow.gd` (grammar and authoring-template tests) pass, and `dialogue_catalog_flow.gd` successfully parses the full committed corpus (47 NPCs, 368 topics per Antigravity's rerun). None of these tests reference any of the twelve new topic IDs specifically — `dialogue_lang_flow.gd` runs against its own dedicated fixture files, and `dialogue_template_flow.gd` tests the authoring template mechanism, not this content. So what's actually confirmed is that the twelve topic/evidence pairs exist in source and parse cleanly, and Antigravity's own static dependency/gate audit across all evidence chains did not surface a lockout affecting any of the twelve specifically; their individual `GATE:`s and runtime reachability — reaching each one, confirming the correct NPC state, confirming the grant, via an actual playtest — remain untested. Original scoping note, retained: Design goal, stated by the author: NPCs should stop being schedule billboards you can look up and walk to with certainty. If a scheduled NPC is genuinely missed (their window passes, or a future revision adds real absence beyond `SCHEDULE:`'s current fully deterministic `(npc, phase) → location` lookup — confirmed against `dialogue_catalog.gd`'s `slot()`, which has no chance element of any kind today), the fact they would have supplied should still be reachable through a different NPC, differently worded — "the same info in a word salad." No new engine mechanism is needed for this: the existing `EVIDENCE:` / `GATE: ... AND NOT evidence(id)` idiom already does the whole job, the same idiom already used for `crew_omission`'s six-NPC worded-differently convergence, just aimed at redundancy instead of flavor (that pattern itself grants no `EVIDENCE:`, only a `NOTEBOOK:` note). The author's own scoping call: with Chapter One spanning three days and nearly every NPC keeping at least one non-`closed` slot per day, there is already ample structural opportunity to meet everyone — so this pattern should be applied to "several, not all" facts, not systemically. No triage-by-guesswork needed either: a fact only creates a real dead end if something else downstream checks `evidence(that_id)` in a `GATE:`; if nothing does, losing it costs one flavor beat, not a blocked thread.**

**Full audit, direct source scan of all 42 `.dialogue` files: 88 distinct `EVIDENCE:` ids exist, 87 single-sourced, 15 from the game's 9 fixed-appointment NPCs (no `SCHEDULE:` header — not at risk under this mechanic), 74 from `SCHEDULE:`\-driven NPCs (the real candidates). Cross-referencing every `evidence(id)` reference across all `.dialogue` and `.gd` files narrows that further: only 35 of the 88 granted ids are ever checked downstream at all, and most of those trace back to Almy (fixed-appointment, `naomi`/`lay_lead`/`service_work`). Stripping those out leaves the actual shortlist — schedule-driven, single-sourced, and load-bearing:**

| Evidence ID | Source NPC | Topic | Thread | Downstream refs |
| ----- | ----- | ----- | ----- | ----- |
| **`gazette_correction_printed`** | **`gazette_editor`** | **`print_correction`** | **Newspaper correction** | **3 (incl. a `filed()` ending gate)** |
| **`press_suppression`** | **`gazette_editor`** | **`the_omitted_two`** | **Newspaper correction** | **3** |
| **`gazette_correction_terms`** | **`gazette_editor`** | **`correction_terms`** | **Newspaper correction** | **2** |
| **`reader_omission_letter`** | **`schoolteacher`** | **`reader_letter`** | **Historian/schoolteacher** | **3** |
| **`sanitized_textbook`** | **`schoolteacher`** | **`town_founding`** | **Historian/schoolteacher** | **2** |
| **`ophion_myth_classical`** | **`local_historian`** | **`ship_origin`** | **Historian/schoolteacher** | **2** |
| **`quay_inquiry`** | **`quay_bookkeeper`** | **`unfamiliar_woman`** | **Freight/quay/Naomi-sighting** | **3** |
| **`new_bedford_letters`** | **`post_office_clerk`** | **`naomi_letters`** | **Freight/quay/Naomi-sighting** | **2** |
| **`naomi_quay_inquiry`** | **`quay_docker`** | **`naomi_sighting`** | **Freight/quay/Naomi-sighting** | **2** |
| **`estate_freight`** | **`quay_bookkeeper`** | **`estate_freight`** | **Freight/quay/Naomi-sighting** | **2** |
| **`chandlery_island_delivery`** | **`chandlers_boy`** | **`island_delivery`** | **Freight/quay/Naomi-sighting** | **2** |
| **`kessler_carriages`** | **`widow_kessler`** | **`club_standing`** | **(standalone)** | **2** |

**Twelve facts (this document's own prior "eleven" was a miscount of its own table, corrected in v32 above), three of the document's own Part Three "Three Separate Investigative Threads" plus one standalone, each anchored by one or two NPCs whose absence would cost forward progress rather than color. The other 63 `SCHEDULE:`\-sourced facts are audited and explicitly marked no-backup-needed — nothing downstream depends on them, so losing one to a miss is fine as designed. AUTHORED, 2026-09-13 (v32): all twelve rows now have a committed second-source topic — see the v32 revision note for the full NPC/topic list. Verification status, corrected 2026-09-14 (v36): generic `tests/dialogue_lang_flow.gd`/`dialogue_template_flow.gd` pass and `dialogue_catalog_flow.gd` parses the full corpus clean (47 NPCs, 368 topics per Antigravity's rerun), but none of those tests exercise these twelve topic IDs specifically — the first two run against their own fixtures/template mechanism, not this content. What's actually confirmed: the twelve topic/evidence pairs exist in source and parse, and a static dependency/gate audit found no lockout affecting them. Each one's individual `GATE:` and runtime reachability, via an actual playtest, remains untested. See Part Six.**

**Pocket watch HUD button — SHIPPED, 2026-09-14, commit `3f582c2`. The `[Tab]` "Personal Effects" panel now shows a working pocket watch reading `DayClock`'s exact current day, twelve-hour time, and phase, free of `TIME:` cost as specified. Open follow-up, at the author's own request: the shipped readout is text (day/time/phase as numerals and words), which the author considers anachronistic for a 1920s-set pocket watch. The fix is purely visual — a proper analog clock face with hands and a twelve-hour dial, driven by the same underlying `DayClock` data the working version already computes; `DayClock` and the panel logic itself don't change. Same category of concern as the Stage 6 model-upgrade-pilot idea (production art standard vs. placeholder). Not yet started.**

**Object-evidence hotspot visibility is one-way only — open, noted 2026-09-14 (v37). `sync_points()` erases a hotspot once its `GATE:` goes false, but nothing restores it if the `GATE:` later becomes true again. Not a live bug today, since every current `GATE:` in the shipped `.object` files is one-directional by nature (e.g. `rose_bodies_removed`); would need addressing before any future object content relies on a hotspot reappearing. See "Object-Evidence Authoring System," Part Three.**

**Object-evidence unknown `GATE:` fields silently parse — open, noted 2026-09-14 (v37). An unrecognized field or function name inside a `GATE:` expression does not raise a parse error today; it's accepted and presumably evaluates as false or is ignored, rather than failing loud the way a malformed `TAG`/`TIME`/`EVIDENCE`/`OBJECT` id already does (see "Object-Evidence Authoring System," Part Three). A typo'd or misspelled `GATE:` condition could silently produce an always-unreachable hotspot instead of an authoring-time error. Worth closing the same way the id-validation gaps already were.**

**NPC-to-NPC eavesdropping — thought-bomb only, not a priority, no *general* system exists yet, 2026-09-13. Corrected v33: this document's own v29 entry already contradicts a flat "no systems exist yet" — one fixed, deliberately atmosphere-only eavesdropping interaction (the `speakeasy_bar` overheard exchange) shipped that same pass. The distinction that matters: no general proximity-based system exists (see the four gaps below, still all open), but that is not the same claim as "no eavesdropping content of any kind." Part of the author's ongoing "more life" direction (see the missable-NPC pattern above): anything that works against the game's built-in reward for completionist play — talk to everyone, exhaust every line — is worth keeping on paper, even far off. One shape floated: place two NPC models adjacent to each other, use a subtle looping instrument cue (trombone and violin mixed low, a "wah-wah-wah") as a proximity sound cue while approaching, and once in range let the player trigger an eavesdropping action — overhearing a short scripted exchange between the two NPCs, with Walter absent from the conversation entirely. The author was explicit this is a "thought-bomb" for the record, not a spec: not a current priority, not being scoped or built now, and no new engine work should be generated on its account yet. For whenever it is revisited, the real gaps confirmed against source: (1) no proximity-detection system of any kind exists in the codebase today — NPCs are placed as plain `Node3D` primitives with no `Area3D` or collision shape, and the only "in range" logic anywhere (`chapter_one.gd`'s `_find_focus()`) is player-to-point for Walter's own `[E]` interact prompt, not point-to-point between two actors; (2) `VOICE:` cues are currently one-shot stingers tied to a dialogue line's on-screen appearance, not continuous or looping positional audio, so an ambient proximity cue is a new audio layer, not a reuse of the existing system; (3) an eavesdropped exchange is a new shape in the dialogue DSL — two NPCs speaking to each other with no player choices and no `GATE:` gating Walter's participation, distinct from every existing `TOPIC:` block, all of which assume Walter as a party to the conversation; (4) scheduling has no way today to deliberately place two specific NPCs together at one location/phase. The one case where two NPCs currently do end up at the same coordinates — Mr. Whitehouse and the school parent, both resolving to `upper_house_3` in the evening per `dialogue_catalog.gd`'s `slot()` — is an accidental collision, not an authored pairing, though it's a plausible first test case if this is ever built deliberately.**

**Voice-cue library replacement pipeline — complete for the present 144-cue architecture. The game uses reusable cue IDs, not one recording per line. `tools/build_elevenlabs_voice_library.py` converts the completed 228-file ElevenLabs source batch into the stable twelve-styles/three-lengths/two-takes game matrix. Trombone takes map to complementary delivery styles; violin length variants use pitch-preserving cadence changes from the supplied mood takes. The output is normalized mono 44.1 kHz 16-bit PCM, and the generated manifest preserves source-to-cue provenance. The broader source vocabulary remains available for later selective expansion without forcing dialogue changes now. This closes out this document's own earlier "not a strict drop-in, compatibility mapping planned but not yet built" language (v18–v20) — the mapping is built and shipping.**

**`tests/test_placement_audit.gd` parse error — still open, reconfirmed by direct execution v40, 2026-09-15 (Codex review): running the script directly returns `SCRIPT ERROR: Parse Error: Identifier "pri" not declared in the current scope. at: (res://tests/test_placement_audit.gd:55)`, matching the earlier static read exactly. Needs a source fix — not yet handed to Codex/Antigravity/VS Code Sonnet as of this revision.**

**`AVERAGE JOE` placeholder speaker label / `tests/instrument_voice_flow.gd` collision — RESOLVED, 2026-09-15 (v40). All twelve dialogue files (`business_resident_01`–`06`, `pickman_resident_01`–`06`) were renamed off the shared literal `AVERAGE JOE` label to authentic period-appropriate (1920s) names by a separate session (VS Code Claude Sonnet) — `MR. HOLLOWAY`, `MR. PETTIGREW`, `MR. GUNDERSON`, `MR. TREADWELL`, `MR. OAKLEY`, `MR. STRAND` (business_resident_01–06) and `MR. BENNETT`, `MR. FALLOW`, `MR. CRANDALL`, `MR. WEEMS`, `MR. THORNE`, `MR. ABBOTT` (pickman_resident_01–06) — independently reconfirmed here: zero `AVERAGE JOE` hits remain repo-wide and no cross-file surname collisions exist. `tests/instrument_voice_flow.gd` was separately fixed for a methodology bug (bare `assert()` calls inside a nested nested per-speaker loop were silently swallowing violations without failing the run) and now genuinely passes: `INSTRUMENT VOICE PASS: 144-cue manifest, 705 NPC lines`, confirmed by direct execution.**

**`route_lower` / `tests/town_expansion_flow.gd` — CLOSED as stale/misattributed, 2026-09-15 (v40, Codex review); reframed v41, 2026-09-15. Reconfirmed again this pass: `route_lower` still does not appear anywhere in `tests/town_expansion_flow.gd`, so the Manus AI audit's original claim doesn't describe current source. In its place, a real, newly-identified issue in the same file is logged: the test hangs after printing `"START EXPANSION"` and never reaches its own `quit()` call, which sits on the file's very last line, only reachable after many more assertions past the failure point. This is not a script bug of "forgetting" to call `quit()` — Godot's `assert()` does not exit a headless script on failure, it simply breaks, so nothing past the failing assertion ever runs. The likely fault line remains the save/load assertion at line 37, `assert(g.state.estate_visits_completed==1 and not g.state.birch_bodies_removed)`, immediately following `g._save_game()`/`g._load_game()` at lines 35–36 — but whether that assertion's failure reflects a real save/load defect or an artifact of this session's own restricted `user://` save environment (Godot run headless via device shell with `$env:APPDATA` redirected to a local scratch folder) is not settled either way. Recorded as a test-harness/environment issue pending reproduction under normal Godot storage, not a confirmed game/save defect; needs a follow-up pass with debug tracing through the save/load cycle under normal conditions.**

**`tests/staging_flow.gd` stale assertion against `gatehouse_boy.dialogue` — CONFIRMED failing, 2026-09-15 (v40, Codex review, upgraded from "not confirmed failing"). The test still asserts the pre-migration `Story.SCENES.boy`/`boy_repeat`/`boy_return` pattern against the now-migrated `gatehouse_boy.dialogue`. `tests/staging_flow.gd` is not a standalone `SceneTree` script — running it directly errors with `"doesn't inherit from SceneTree or MainLoop"` — so this session could not reproduce the failure by direct execution; it runs only through an integrated `--qa-staging`-style mode not attempted this pass. Codex's own execution report, combined with the already-confirmed static source mismatch, is treated as confirmation. Needs a source fix to the assertion, matching the resolved AVERAGE JOE precedent.**

# **Part Five — Open Production Questions**

**Ekon's fatal outcome and the preservation/denial archive mechanic are already fully specified in Bible v15; what remains open is narrower — whether his final action includes lighting a sealing fuse, as distinct from the archival "sealing" the bible already describes.**

**Cross-story consistency pass, 2026-09-13, after the grammar edit to all six short-story files and the Bible v15→v16 edit. A full read of all six stories against each other (by pair) and against the Bible surfaced one likely copyediting artifact, already fixed, and one real open decision:**

* **FIXED: `3) Scorched-Earth-canonical.md` had Ekon read a private-club scandal as "six-decades-dead," contradicting both the Bible's stated twenty-year Walter-to-Ekon span and this same story's own opening line ("the town clerk, twenty years back..."). The paired `total-denial` telling has no age attached to the same clause at all, which is what makes this read as a grammar-pass artifact rather than an intentional divergence — the two tellings share pre-branch setup facts by design. Corrected to "twenty-year-dead scandal."**  
* **OPEN, author's call needed: Ward's book has two incompatible provenance stories. The Bible states it "passed out of \[Fenn's\] estate after his death; Ward acquired it secondhand." Both `Total-Recovery` tellings instead have Ward find it in situ in the ruins, "wedged with a stack of a dead predecessor's field notes" — an on-site find, not a secondhand purchase. Both stories agree with each other, so this isn't a pass artifact between the pair; it's the Bible's account versus both stories' shared account for the object the Bible itself calls the trilogy's "object-passed-hand-to-hand device." Needs a decision: bring the Bible in line with the stories, or bring both stories in line with the Bible. Not touched pending that call.**  
* **Noted, not corrected, low confidence: `1) No-Exit-Wound-canonical.md` has Walter pass "three of them" working the kitchen-wall hedge on arrival; the paired `the-fight` telling has him count "four of them" on a different specific afternoon ("the afternoon he finally located the pantry"). These may describe two different moments rather than the same one — left alone rather than silently changed on a guess.**

**The per-protagonist presentation scheme remains unwritten and not currently being worked on. Chapter Two/Three audio is explicitly still deferred. Chapter One now has a confirmed, working instrumental-voice-cue architecture, but that is a Chapter One-scoped implementation, not a decision about Chapters Two or Three. Whether Chapter Two/Three dialogue extends the same cue system, adopts something else, or goes fully voiced for the player character remains open and untouched, and should stay off any "in progress" list until the author actually takes it up. The contiguous-town preference is no longer a stated long-term item only — see v25/v26 and Part Six: Phase 1 is an active branch with six steps landed as of 2026-09-13. This line is corrected in place rather than left stale. The HPLHS pitch remains a long-term aspiration only.**

# **Part Six — Next Steps**

**Unchanged production direction: *validate and improve the first 30 minutes of investigation — discovery, social consequences, evidence linking, and a compelling next lead — before expanding production toward the chapter's climax.***

**In priority order:**

1. **A cold playthrough of the newest development revision specifically, distinct from the frozen Web build, focused on whether the opening 30 minutes' three investigative threads (Part Three) — the historian/schoolteacher chain, the steward appointment progression, and the newspaper-correction thread — actually land for a first-time player and produce a compelling next lead, now including the waterfront hub, the confirmed voice-cue system, and the two dialogue-grammar extensions confirmed implemented this pass. This is a diagnostic step; per Part One, it does not authorize replacing the frozen Web build regardless of outcome.**  
2. **Address whatever that playthrough surfaces in the opening 30 minutes before touching anything past it.**  
3. **\~\~Fix the `odell_precinct` topic-gate namespace defect (Part Three/Four)\~\~ — RESOLVED, 2026-09-12. Precinct conversations now resolve once rather than remaining repeatable.**  
4. **\~\~Implement `WEIGHT:` on `TOPIC: default` and `FORK:`/`OUTCOME:` one-time resolution (Part Three/Four)\~\~ — RESOLVED — both were already implemented and confirmed this pass against source and test. No implementation work remains; only ordinary authoring use of them going forward.**  
5. **\~\~Migrate `mrs_ashcroft.dialogue`, `mrs_whitlock.dialogue`, and `mr_wick.dialogue` to the documented `morning/midday/evening/night` schedule vocabulary.\~\~ — RESOLVED, 2026-09-14 (v36, Antigravity). All three files migrated from `dawn`/placeholder destinations to the documented vocabulary. No further action.**  
6. **\~\~Confirm or rule out the `$84,000` rendering item live.\~\~ — RESOLVED, 2026-09-12, for the original two lines; a separately-discovered `NOTEBOOK:`\-line instance (`county_clerk.dialogue`'s `clerk_ophion_1`) found live 2026-09-14 is RESOLVED the same day. Text-rendering defect from the `$` character; fixed in all three known instances by spelling the amount out in words, with no numerals (Part Three). No further action.**  
7. **Playtest the completed ElevenLabs compatibility library in context and hand-tune individual `VOICE:` assignments only where delivery clashes with the written line. The stable 144-cue replacement and source mapping are complete.**  
8. **\~\~Fork revisit-with-recap pattern (Part Three/Four)\~\~ — RESOLVED, 2026-09-12. Codex implemented resolution-with-recap directly in the runtime (`_locked_fork_options()`), confirmed against source and `tests/dialogue_template_flow.gd`; all eight previously one-shot-protected forks are migrated. \~\~Remaining loose end: Odell's reported one-shot intake fork is not yet traced to its actual source location.\~\~ — RESOLVED, 2026-09-14 (v34): located in `odell.dialogue`'s `TOPIC: default` block, retiring via the ordinary one-shot default mechanism. No further action.**  
9. **Conversation-time/travel-time scale mismatch (Part Four) — three of four factors diagnosed, no solution chosen; the fourth (dev-only brisk-speed inflation skewing the author's own playtesting) is RESOLVED, 2026-09-13 — see Part Four. Not a priority while investigation logic is the focus; revisit and pick a direction (raise conversation costs, or shrink phase thresholds) once ready to tune day-clock pacing, now from a representative playtesting speed.**  
10. **Missable-NPC redundant-carrier pattern (Part Four) — AUTHORED, 2026-09-13 (v32). All twelve load-bearing facts (three investigative threads plus one standalone; this document's earlier "11" was a miscount, see v32) now have a committed second-source topic. The other 63 schedule-sourced facts are marked no-backup-needed. Verification status, corrected 2026-09-14 (v36): generic grammar/template tests pass and `dialogue_catalog_flow.gd` parses the full corpus clean (47 NPCs, 368 topics per Antigravity's rerun) — but none of those tests reference the twelve backup topic IDs specifically. Confirmed: the twelve topic/evidence pairs exist in source, and a static dependency/gate audit found no lockout affecting them. Not yet confirmed: each one's individual `GATE:` and runtime reachability, plus a manual playtest of each in Godot. Separately: this same pass found and fixed two unrelated fork-locked dead ends (`estate_conduit_map` in `local_historian.dialogue`, `gray_salt_cakes` in `widow_kessler.dialogue`) — see this document's v36 revision note — the same class of problem as this pattern, triggered by a dialogue choice rather than a missed schedule window; worth folding into this pattern's scope on the next authoring pass.**

**"More life" work — five-stage priority sequence, proposed by Codex and adopted by the author, 2026-09-13, superseding the former flat list of items 10–13. Rationale, per Codex: this order should produce the intended Gothic effect — the town feels too socially dense to strip-mine comfortably, while the investigation stays robust enough to survive a human player's imperfect route.**

10. **Stage 1 — Finish and visually validate the contiguous town. Phase 1 (Pickman/business/upper) landed 2026-09-13, six steps (see v26). Phase 2 (lower district and full waterfront) landed the same day on branch `dev/contiguous-town-phase-2`, stacked on Phase 1, confirmed against source in the v29 diff pass: shared lower-district geometry, a physical waterfront descent with an unreachable whaling-station silhouette, and the shared waterfront exterior itself, all with scheduled-actor coordinates translated the same way Phase 1 did it. Desktop baseline for the complete town: 113.56 ms construction, 2,284 nodes, 71.84 MiB static memory — a 6.40 ms / 4.52 MiB increase over the Phase 1 baseline, not by itself a case for streaming or HLOD per Codex's own decision rule. Remaining, per both phases' QA docs: the actual Web-profile handoff task (`docs/qa/PHASE_ONE_WEB_PROFILE_HANDOFF.md`) is written but not yet executed. Corrected v33 — this contradicted Part Two's Verification section, which confirms a Web export now exists in-repo (`build/web/`, 2026-09-13) covering this complete contiguous town. So a Web export has been produced; what's still missing is specifically the profiling/measurement pass against it, not the export itself.**  
11. **Stage 2 — Populate the finished town in layers with scheduled life. Refined 2026-09-14, evidence corrected: initial population already exists, but not in the form previously cited here — `speakeasy_bar` (v29) is a fixed interaction, not scheduled-life population, and isn't evidence of Stage 2 work. The real early evidence was the six scheduled NPCs credited in v32 (`drayman`, `fish_smoker`, `lamplighter`, `night_owl_one`/`night_owl_two`, `speakeasy_bartender`). In progress, live, 2026-09-14: Codex reports Stage 2's "first townwide population layer" complete — six new lower-district residents (domestic/labor routines, commit `b98035a`) and six new waterfront workers (present mornings/middays, gone before the evening speakeasy period, commit `04c44f9`), for 23 new scheduled ambient NPCs total across four districts. Per Codex: lower-population, waterfront-population, contiguous-town-Phase-2, and full-dialogue-catalog tests all PASS; the Phase 1 traversal test hung without reporting a failure and was stopped rather than investigated, deliberately, because other agents (this document's own dialogue-content track) are currently editing that area — geometry left untouched. Not yet independently confirmed by this document (no `device_bash` this session). The broader layered population pass this stage describes is further along than "not yet started" but not stated as complete by Codex either — status is genuinely in-progress, not a settled milestone. Watch for overlap with `dialogue_catalog.gd`'s existing `SCHEDULE:`/`slot()` machinery — see the dependency note above.**  
12. **Stage 3 — Add the twelve identified backup routes (Part Four, "Missable-NPC redundant-carrier pattern"; this document's earlier "eleven" was a miscount of its own table, corrected in v32). AUTHORED, 2026-09-13 (v32) — all twelve backup topics committed to the repo. Authored ahead of Stage 2 rather than after it, since Stage 2 (populating the town with scheduled life) has not yet started for any of the source NPCs — the dependency note above (hold authoring until Stage 2 lands for a touched NPC) did not yet apply. Revisit each of the twelve backup topics' `GATE:`/`SCHEDULE:` interaction once Stage 2 actually lands for its NPC, in case scheduling changes shift when either the primary or the backup route is reachable. Verification status, corrected 2026-09-14 (v36): generic `tests/dialogue_lang_flow.gd`/`dialogue_template_flow.gd` pass and `dialogue_catalog_flow.gd` parses the full corpus clean (47 NPCs, 368 topics per Antigravity's rerun), but none of those tests reference these twelve topic IDs specifically. Confirmed: the twelve topic/evidence pairs exist in source, and a static dependency/gate audit found no lockout affecting them. Not yet confirmed: each one's individual `GATE:`/reachability, plus a manual in-game playtest of each route.**  
13. **Stage 4 — Introduce ambient paired conversations and eavesdropping (Part Four). Thought-bomb only as of v27; no *general* system exists yet (no proximity detection, no ambient/positional audio, no two-NPC DSL shape, no deliberate co-placement scheduling) — corrected v33, one fixed exception already shipped: the atmosphere-only `speakeasy_bar` overheard exchange (v29), which grants no `EVIDENCE:`/`NOTEBOOK:` and doesn't reopen this stage's scope. Sequenced last among these five so it layers onto a town whose density and investigative redundancy are already in place, rather than being built against a town still in flux.**  
14. **Stage 5 — Add the Tab-menu pocket watch (Part Four). SHIPPED, 2026-09-14, commit `3f582c2`. Open follow-up, not started: replace the shipped text readout with an analog clock face — see Part Four.**  
15. **Playthrough telemetry (Part Four) — DEPLOYED AND TRANSPORT-TESTED, 2026-09-13, now including the debrief pairing; hosting detail corrected v40, 2026-09-15 (Codex review) — see Part Four, "Hosting." Label narrowed v33 from "LIVE end to end" — accurate for everything short of an actual tester session, per the caveat this same item already closes on below. Both telemetry halves confirmed against source, not taken on report: the endpoint (`https://three-colors-worker.dejunai.workers.dev`, Cloudflare Worker, now confirmed dual-writing to both D1 and R2 with idempotent, `event_key`-hashed D1 inserts, not R2-only as this document previously stated) and the game side (`scripts/shared/playthrough_log.gd`, instantiated and wired into `chapter_one.gd` — `begin()`/`resume()` on new-game/load, `observe()` ticking every frame in `tick_world()` for phase/first-objective/NPC tracking, `district_transition()` at every `_travel()`, `end()` on both quit and completion; disables itself automatically under `test_mode` so QA runs don't pollute the log). The previous "R2 instead of D1, offline script needed" tradeoff no longer describes the current architecture; see Part Four. Added and then hardened this pass: the two-question debrief at the Day-3 bed sequence (with a de-duplication guard and a fix so a finished playthrough can't replay it), and `day3_bed_reached()` finally wired to its call site (it had been dead code since it was first written). Full automated coverage now exists on both sides (`tests/playthrough_log_flow.gd`, `tests/debrief_flow.gd`, `cloudflare/three-colors-worker/test.mjs`), and the redeploy is done and verified live — no outstanding deployment step. Not yet exercised: an actual real tester playthrough (as opposed to automated tests) confirming events land in the bucket end to end.**  
16. **\~\~`curriculum_abridgment` bottleneck: the school-reader-omission thread's backup route shared its primary's own 4-witness `crew_omission` gate, so it wasn't independently reachable.\~\~ — RESOLVED, 2026-09-14 (v36, Antigravity). `curriculum_abridgment` added as an alternate grant in `school_parent.dialogue`'s `the_reader_lesson`, and `the_covering_letter`'s gate loosened; missing the tally no longer blocks the thread. \~\~Stale test assertion in `tests/dialogue_playback_flow.gd`, written for the old auto-retirement fork behavior and never updated for the resolution-with-recap mechanism.\~\~ — RESOLVED, 2026-09-14 (v36, Antigravity). Assertion corrected to match the current contract; test passes. See this document's v36 revision note for the full pass, including the two fork-locked dead ends fixed alongside this (logged under item 10 above).**  
17. **Object-evidence authoring system — MIGRATED, WIRED, AND FAIL-LOUD PROTECTED, 2026-09-14 (v37). See Part Three, "Object-Evidence Authoring System" and "Fail-Loud Guards." All eleven scoped hotspots live; four independent-review gaps closed; both object-side and dialogue-side fail-loud guards confirmed in source. Remaining, tracked as open Part Four items: one-way hotspot visibility, and unknown `GATE:` fields silently parsing instead of erroring. `docs/OBJECT_AUTHORING.md`'s byline correction ("Copilot" → Claude Sonnet, VS Code) is a TODO for whoever next touches that doc, not this document's to make directly.**  
18. **Day 2 montage removal / enactment — IMPLEMENTED, CONFIRMED, 2026-09-14 (v37). See Part Three, "Montage Removal / Day 2 Enactment." No further action; old-save resume behavior deliberately preserved.**  
19. **~Camera jumps on menu closures (regressed)~ — RESOLVED, 2026-09-14 (v38). Root cause: browsers can report the entire pointer movement that happened while the mouse was free as one inflated relative-motion delta on the first sample after pointer lock re-acquires post-menu-close, and can emit more than one such synthetic sample before the next physics frame notices the mouse-mode change — the prior single-input-swallow guard was never sufficient for a multi-sample burst. Fix: `RECAPTURE_MOTION_GUARD_SECONDS = 0.12`, armed via `_arm_mouse_motion_guard()` whenever `capture_mouse()` re-engages pointer lock; `_apply_mouse_look()` discards every motion sample for the whole guard window. Confirmed by `tests/camera_recapture_flow.gd`. No further action.**  
20. **Antigravity's coordinate-collision audit (2026-09-14) — reported, not independently confirmed. Three schedule fixes claimed (evening-slot collision fixes in `school_parent.dialogue`/`schoolteacher.dialogue`, a curfew-header normalization in `local_historian.dialogue`) plus a reported 61-living-NPC/0-collision full-suite pass. The 61-NPC figure is unreconciled against this document's own 70-NPC count (Part Two, Codex's Stage 2 checkpoint, same day) — not resolved here, just flagged, consistent with this document's ongoing catalog-count churn this session.**  
21. **`docs/qa/` housekeeping — non-urgent, per the author. 30+ accumulated one-off pass-report files, several image/import pairs, and this session's own leftover `_commit_test.txt`. Not a disk-space concern; a triage pass (keep as permanent record vs. archive/delete) whenever there's time for it.**  
22. **Portal authoring system — MIGRATED, WIRED, AND REVIEWED THROUGH TWO INDEPENDENT PASSES, 2026-09-14 (v38); scope figure corrected v40, 2026-09-15, file-count wording corrected v41, 2026-09-15. See Part Three, "Portal Authoring System." Ten unique portal ids live across five live `.portal` files (not four, as this document previously stated) — a sixth file, `background_portal_template.portal`, is an example-only cookbook, not a source of live ids; five Codex-found defects (three integration, two hardening) all confirmed fixed; a new `tests/portal_content_flow.gd` closes the live-adapter test gap Codex's own review identified. No further action; no open items carried forward — the one documented "known limitation" (dynamic post-arrival content kept as a small named adapter escape hatch) is a settled design decision, not an open question.**

23. **`tests/test_placement_audit.gd` parse error (Part Three/Four) — open, reconfirmed by direct execution 2026-09-15 (v40). A truncated identifier at line 55 blocks this test from running at all. Needs a source fix.**
24. **`AVERAGE JOE` placeholder speaker label across twelve dialogue files (Part Three/Four) — RESOLVED, 2026-09-15 (v40). All twelve renamed to authentic period-appropriate names; zero collisions confirmed; `tests/instrument_voice_flow.gd` now passes clean at 705 NPC lines.**
25. **`tests/town_expansion_flow.gd` save/load assertion failure at line 37 (Part Three/Four) — open, newly identified 2026-09-15 (v40), reframed v41, replacing the now-closed `route_lower` discrepancy (that claim was confirmed stale/misattributed — `route_lower` does not appear in this file). The test hangs after "START EXPANSION" because it never reaches its own `quit()` call following the line-37 assertion failure — normal `assert()` behavior in a headless run, not a missing `quit()`. Recorded as a test-harness/environment issue (possibly this session's own restricted `user://` sandbox) pending reproduction under normal Godot storage, not a confirmed game/save defect.**
26. **`tests/staging_flow.gd` stale `gatehouse_boy.dialogue` assertion (Part Three/Four) — open, CONFIRMED failing 2026-09-15 (v40), upgraded from "not confirmed failing" per Codex's own execution report plus the already-confirmed static source mismatch. Needs a source fix.**

**Explicitly not current priorities: the ontological break and fatal-comprehension ending; Chapter Two's playable slice and trinket/procedural-geometry pipeline; the Chapter Two/Three audio-pipeline architecture decision (Chapter One's completed voice-cue work does not change this — see Part Five); the per-protagonist presentation scheme; Ekon's sealing-fuse staging detail; the Part Seven difficulty screen and achievement registration; gamepad support and full key rebinding; an NPC-to-NPC eavesdropping mechanic (thought-bomb only, no systems built — see Part Four and Part Six Stage 4, item 13); and any further work toward the HPLHS pitch.**

&nbsp;

&nbsp;

&nbsp;

&nbsp;

&nbsp;

&nbsp;