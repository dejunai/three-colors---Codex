**THREE COLORS OF MADNESS**

***Technical Design Document***

**Internal working document. Not for external distribution. Companion to the Design Bible.**

**Dejunai (founder, author/producer; lead on dialogue content specifically) · Claude Sonnet (co-author, co-designer — designed and built the dialogue grammar and interpreter with the author in VS Code, and separate VS Code Claude Sonnet sessions built the object-evidence and portal authoring systems under the author's lead) · Codex (core programmer — live integration of the dialogue/object/portal systems into play, interpreter fixes and extensions, and independent review of new systems against source) · Antigravity (dependency/gate auditing and dialogue-content execution under the author's lead, and the project's exclusive bug hunter/stomper) · Manus AI (independent outside source-backed repository audit, and the first reviewer with the ability to actually play the game rather than only read source) · Perplexity (leads red-team review of docs and code for logic gaps/inconsistencies; also research support for dialogue content) · Grok-bot-minis (red-team the conceptual commercial release) · Lumo/MetaAI (occasional outside repo assessments after major updates) · Copilot 365 (ideation support for dialogue content). Full role definitions: `AGENTS.md`.**

## **Contents**

**Part One — What This Document Is**

**Part Two — Current Build**

**Part Three — Architecture As Built**

**Part Four — Architecture Decisions Needed**

**Part Five — Open Production Questions**

**Part Six — Next Steps**

# **Part One — What This Document Is**

**This is the Technical Design Document (TDD) for Three Colors of Madness. It is the companion to the Design Bible, not a replacement for any part of it.**

**The division between the two documents is simple. The bible owns the what and the why — thesis, Design Laws, narrative, the meaning of every mechanic. This document owns the how — engine architecture, data schemas, production sequencing, and the specific open questions a playtest still needs to settle before a mechanism the bible describes gets its final shape. Where the two documents ever appear to disagree, the bible is correct and this document is wrong until revised — never the reverse.**

**This is v44. As of the prior revision, the document is split in two: this file holds only the current, settled state of the build — what's implemented, how it works, what's still open — and a separate Historical Change Log holds the revision-by-revision record of how the project got here. The HCL lives outside this repository, in a sibling folder, not at `docs/design/TCM_Historical_Change_Log.md` — see `AGENTS.md`, "Where things live." Update this document going forward to state the current truth plainly; log the story of the change in the HCL instead of in this file's prose.**

# **Part Two — Current Build**

## **Engine and Target**

**Godot 4.7 (the Web export identifies as 4.7.2.stable), using the GL Compatibility renderer (not Forward+), targeting broad hardware reach over cutting-edge rendering fidelity. Window target 1440×900, canvas-items stretch mode. No import dependencies beyond the engine itself.**

## **Scope of the Current Slice**

**The Ophion estate grounds (rose garden, service passage, tunnel entry), Pickman Street (boardinghouse, cobbler's shop/Walter's room, precinct, morgue), three neighborhood hubs (business district, upper-residential ridge, lower-residential quarter), a post office, and the waterfront: a playable outdoor hub with scheduled residents, its own travel time, full save restoration, and the offshore whaling station visible as unreachable scenery.**

**Geography is contiguous, not hub-and-spoke. Phase 1 (Pickman Street, business district, upper ridge) and Phase 2 (lower district, full waterfront) are one shared walkable exterior; only interiors remain separate scenes. Baseline was stale as of Manus's 2026-09-20 audit and independently re-run this pass with a matching result: `tests/contiguous_town_profile.gd` against current `main` (`72f159d`) now reports 2,859 nodes, 2,339 meshes, 107 bodies/collision shapes, 68 interaction targets, 47 scheduled actors, and ~77 MiB static memory (this pass measured 77.87 MiB, Manus measured 76.70 MiB — close enough to read as the same figure at normal run-to-run variance). Construction time is hardware-sensitive and not comparable across machines: this pass measured 109.19 ms, Manus measured 195.90–273.47 ms across repeats in their own sandbox. The node/mesh/target/actor counts are not hardware-sensitive and confirm the old 2,284-node baseline was simply superseded by population growth (Stage 2 below), not a regression. The still-outstanding task is the actual Web-profile handoff (see below), not a repeat of this desktop topology count.**

**The day/night clock drives scheduled-resident placement, and separately drives the sun, directional lighting, fog color, and street-lamp visibility, purely as presentation. The clock does not govern body removal. Story progression does: the rose-garden bodies clear after the player's first departure to town; the birch victims (the woman and boy) remain visible and unattended through at least a second estate visit and clear only once Day 3 has begun, with no accompanying scene or notification either time.**

**Fixed, same day (2026-09-20; see HCL (25)): a mid-conversation NPC-disappearance bug, caused by the day/night clock's `populate()` re-scheduling actors while a multi-topic conversation was still open — a phase shift crossing mid-dialogue could move the interlocutor off their allowed slot and evaporate them out from under the player. Antigravity's fix (independently verified against live source and live execution rather than taken on report) defers the clock: `case_state.gd` gained `pending_dialogue_minutes`/`defer_dialogue_clock`; `dialogue_runtime.gd::commit_through()` now accumulates a topic's `TIME:` cost into the pending total instead of advancing the clock immediately whenever a conversation is active; `chapter_one_dialogue.gd` tracks a `session_actor` that `allowed()` always permits regardless of what a mid-session phase shift would otherwise compute, and moved the actual `populate()`/daylight update out of `_segment_done()` and into a new `end_session()`, called from `chapter_one.gd::_close()`, so the world only re-schedules once a conversation genuinely ends. Confirmed: the diff matches Antigravity's description exactly across all four files; the aggregate 13-suite QA run passes clean; `dialogue_catalog_flow.gd` (71 NPCs/449 topics), `day_clock_flow.gd`, and `dialogue_live_flow.gd` all independently re-run live and pass — `dialogue_live_flow.gd` failed once on a stale leftover `user://opening_qa.json` save file from an unrelated prior test run (these focused tests share a real save path rather than an isolated one per run, so back-to-back or out-of-order runs can leave stale state behind); deleting that file and re-running produced a clean pass, so this reads as a test-isolation gap in the harness, not a defect in the fix itself, and is worth a harness fix at some point rather than a re-open of this item. The Web build was re-exported with fresh timestamps matching the claim. One process note: Antigravity's own action log shows it ran `git checkout -- "docs/design/07) Three Colors of Madness — TDD v44.md"` mid-session, discarding whatever uncommitted TDD edits existed in the working tree at that moment — this HCL's own concurrent-work discipline exists precisely to prevent silent collisions like this. It cost nothing this time only because this session's own subsequent delivery landed after it and restored the current content (confirmed intact via `git status -s` and a direct grep for this pass's own additions); worth flagging to Antigravity's operators that blindly discarding a doc file's diff is not a safe default action.**

**The live dialogue system spans 71 NPCs and 449 nonempty authored `TOPIC:` blocks (`tests/dialogue_catalog_flow.gd`, confirmed live 2026-09-20). This figure moves as content authoring continues; re-derive rather than trust this snapshot indefinitely. Real cross-NPC and paperwork-gated content exists beyond simple witness menus — see Part Three.**

**Wordless instrumental voice cues are implemented across every authored NPC line. 723 NPC lines currently carry a voice cue (`tests/instrument_voice_flow.gd`), drawn from a 144-entry cue manifest (a cue is reused across every line tagged with it — this is not one recording per spoken line). See Part Three for the architecture. Walter, narration, beats, objects, notebook entries, and system text are intentionally silent by design, not by omission.**

**The completed ElevenLabs Sound Effects delivery contains 228 source recordings; a documented compatibility build (`tools/build_elevenlabs_voice_library.py`) maps the broader trombone vocabulary and supplied violin mood takes onto all 144 stable game cue IDs, so every `.dialogue` file's `VOICE:` assignments remain valid without source-file changes. Trombone takes map to complementary delivery styles; violin length variants use pitch-preserving cadence changes from the supplied mood takes. Output is normalized mono 44.1 kHz 16-bit PCM, with a generated manifest preserving source-to-cue provenance. The broader ElevenLabs source vocabulary remains available for later selective expansion without forcing dialogue changes now.**

**Object-evidence content is authored in a parallel flat-file grammar rather than hardcoded in GDScript. All eleven scoped hotspots (estate's seven, town's three, tunnel's one) are migrated and live — see Part Three, "Object-Evidence Authoring System," and Part Four for the one known, accepted limitation carried forward as an open item (unknown `GATE:` fields/functions now fail loud rather than silently parsing — see Part Four's Perplexity/Manus section).**

**The Day 2 montage no longer plays on a new playthrough; Day 2 is now enacted, playable content culminating in the steward's second visit — see Part Three, "Three Separate Investigative Threads," item 2.**

**Not yet implemented: the fatal-comprehension ending, Chapters Two and Three as playable slices, gamepad support, full key rebinding, a full encumbrance/leveling system, and Ekon's later consumption of Walter's record. This is a build-status list, not a lore gap — the reference novellas (`08`, `09`, `10`) fully dramatize Ekon's use of Walter's record in prose; it doesn't exist in the build yet. Narrative canon is expected to lead implementation during this phase. The glass-shattering ontological break, formerly on this list, is now implemented, merged to `main`, published (GitHub Pages Web export and itch.io), and played through by the author on the merged build — see `docs/qa/GLASS_BREAK_PASS.md` and the entries below.**

## **Saves**

**`CaseState.VERSION` is 11.**

## **Verification**

**A real Godot 4.7.2 standard executable is available (distinct from the mono-only editor used for the earlier native staging-suite pass, which could run gameplay tests but not export Web builds). Current authoritative checks, run against it, pass (re-confirmed live 2026-09-20 via a fresh `python tests/run_all_qa.py` pass — all 13 aggregate suites clean):**

* **Dialogue catalog: 71 NPCs, 449 topics.**
* **Instrument voices: 144 cues, 723 NPC lines.**
* **Live dialogue flow: PASS.**
* **Usability and object interactions: PASS.**

**The WEIGHT/OUTCOME mechanism is confirmed by direct source read of `dialogue_lang.gd` (parser), `dialogue_runtime.gd` (selection/commit logic), and `dialogue_state.gd` (persisted outcome storage), plus `tests/dialogue_lang_flow.gd`'s coverage.**

**Direct source reading also covers `chapter_one_dialogue.gd` (the live dialogue adapter, its `allowed()` function specifically) and `chapter_one_staging.gd` (the steward's three-visit progression). The voice-cue architecture described in Part Three is confirmed against source. Status update, 2026-09-20: the earlier "frozen Web build vs. newest development revision" split no longer describes the project. `main` has since merged the glass-shatter ending, the pocket-watch/paper-doll UI work, and the pacing tuning pass, and been published to both GitHub Pages and itch.io (glass ending and publication: `ba76939`, `8f2c809`; pacing tuning: `be38066`; pocket-watch/paper-doll merge: `c5d0e17`; confirmed via `git log`); the author has played through the current merged build directly. That playthrough is what prompted the `be38066` pacing change (Part Four), which means it necessarily ran against the build's prior timing costs, not the revised ones the change produced — the revised timing has since received only spot-testing, not a full validating playthrough, per Part Four's own note (line 203). External testers had earlier surfaced real issues (an exit problem, a ledger problem, a linking problem) against a prior Web build specifically; those were addressed in subsequent commits, not re-audited individually this pass. A cold playthrough by someone other than the author remains a live, current priority (Part Six item 1) — that's an open diagnostic step, not evidence of a still-frozen separate release artifact.**

**A current Web export exists in-repo (`build/web/`): `index.wasm`, `index.pck`, `index.js`, `index.html`, and icon assets, continuously redeployed to GitHub Pages from `main` whenever `build/web/` changes (`.github/workflows/deploy-pages.yml`, per `README.md`). Partially profiled as of 2026-09-20 (see `docs/qa/CONTIGUOUS_TOWN_PHASE_ONE.md`'s new "Web profile — finally executed" section): against the live published build (GitHub Pages, commit `72f159d`, same as itch.io), `index.wasm` is 37.68 MB decoded/9.77 MB transferred (~3.76s) and `index.pck` is 34.98 MB decoded/32.19 MB transferred (~6.59s), all four core assets loading eagerly at the title screen before play begins; JS heap at that point measured 137.7 MB used (the author's own browser-reported ~137 MB, independently corroborated). Still open: an in-game walk of the full town for peak in-play memory, frame stalls, and phone-viewport behavior — the original handoff task's steps 3-5.**

# **Part Three — Architecture As Built**

## **Story Actors vs. Catalog Residents**

**`chapter_one_dialogue.gd`'s `allowed(g, actor)` function implements two genuinely different systems:**

* **Catalog-scanned additional residents (`extra_actors`, populated by `catalog.scan()` in `setup()`) are placed through `dialogue_catalog.gd`'s `slot()`. Ordinary catalog residents return an empty placement at night, closing for the phase; a small, explicitly named exception list, `NIGHT_ACTIVE` (currently `speakeasy_bartender`, both `night_owl_one`/`night_owl_two`, and `lamplighter`), stays available through the night phase instead. `SCHEDULE:` is a fully deterministic `(npc, phase) → location` lookup with no chance element.**
* **Story actors — Odell, the coroner's assistant (`assistant`), the groundskeeper (`crew`), the old woman, and the steward (`barman`) — have their own bespoke, state-driven availability rules in the same `allowed()` function, none of which reference the day/night clock: Odell and the assistant are available whenever `world == "estate"` and the estate isn't complete; the groundskeeper once the lounge has been exited; the old woman while in town and not yet recorded as evidence; the steward keyed to the lounge and whether Mrs. Almy has been spoken to. None of these actors hide at night.**

**Returning staff for later days are implemented: `odell_precinct` and `coroners_assistant_morgue` exist as live scheduled NPCs. `day_two_check`, `day_two_pressure`, and `day_three_final` test `topic_done(odell_precinct, ...)`, matching the identity under which the runtime records them; a regression test verifies each topic disappears after completion without conflating precinct Odell with the estate encounter.**

## **Three Separate Investigative Threads**

**Three separate threads, each with its own prerequisites, not chained together:**

1. **The historian/schoolteacher witness-count thread. `local_historian.dialogue`'s `crew_omission_followup` topic gates on `topic_done(local_historian, crew_omission_official) AND topic_count(crew_omission) >= 4 AND NOT topic_done(...)` — ask enough people, consistently tagged, before the historian goes further. `schoolteacher.dialogue` carries a related chain keyed off `evidence(naomi)` and `evidence(reader_omission_letter)`. This thread's own internal chain (historian → four witnesses → historian → schoolteacher) does not connect to either thread below.**
2. **The steward appointment thread. `chapter_one_staging.gd`'s `sleep()` blocks *sleeping* — not the first conversation — when `steward_visits == 0`, `intake_done` is false, or `evidence.has("naomi")` is false; while blocked, the player gets an "Get up" panel. Reaching the steward at all is gated separately, through `chapter_one_dialogue.gd`'s `allowed()`: the lounge itself only becomes reachable via the `service_entrance` travel action once `visited.has("almy")` is true, and the steward's dialogue is only `allowed` while `world == "lounge"` — so the real first-visit gate is "spoken to Almy, then travel to the lounge." Visit two is enacted, playable Day 2 content (see "Montage Removal / Day 2 Enactment" below), culminating in the steward's second visit, relocated to the smoking lounge and ending on `THE STEWARD: "Come back tomorrow, and don't bring your badge."` Completing it sets `day = 3` and `steward_visits = 2`. The substantive third encounter is gated by `case_state.gd`'s `steward_ready()`, which requires all four of `visited.has("almy")`, `day == 3`, `steward_visits >= 2`, and `coat == "Plain wool coat"` — the plain coat specifically, not just the day, is a real and separate requirement. This thread does not depend on the historian/schoolteacher thread or the newspaper thread.**
3. **The newspaper-correction thread, gated on a distinct mechanism: `filed()`, not `evidence()`. `gazette_editor.dialogue`'s `print_correction` topic requires `evidence(gazette_correction_terms) AND filed(eight) AND filed(naomi) AND filed(lodging)` and awards `EVIDENCE: gazette_correction_printed` on completion. `filed()` checks received report pages specifically, not the current evidence inventory — this thread depends on paperwork actually having been filed and received, separate from simply having collected the underlying evidence. `mrs_almy.dialogue`'s `show_printed_correction` topic gates directly on `evidence(gazette_correction_printed)`; once available, showing her the slip has her read Naomi's name aloud and acknowledge Walter's effort ("You brought back something I can read. Thank you."), recorded via `NOTEBOOK: correction_seen`.**

## **Montage Removal / Day 2 Enactment**

**The montage sequence (`scripts/chapters/montage_still.gd`) is disconnected from the active flow of a new playthrough — its art and code are not deleted, just unwired. Day 1 is unchanged (intake, Naomi ID, first steward visit). Sleeping ends Day 1 and returns control on Day 2 at 6:00 a.m., which has real content culminating in the steward's second visit, relocated to the smoking lounge, ending on the verbatim line `THE STEWARD: "Come back tomorrow, and don't bring your badge."` (`dialogue/steward.dialogue`'s `steward_second` topic, `GATE: day >= 2 AND day < 3 AND (topic_done(steward, steward_first) OR topic_done(steward, steward_first_lead))`). Sleeping before that second visit is blocked with a redirect back to the lounge. After the second visit, sleeping advances to Day 3 at 6:00 a.m.; the existing plain-coat third steward visit is unchanged.**

**Old-save resume is deliberately preserved: `chapter_one.gd::_load_game()` reads `elif state.montage_index >= 0: staging.draw_montage(self)` — gated entirely behind an old save already carrying a set `montage_index`, so the montage-resume path is structurally unreachable from a fresh game while remaining fully intact for anyone mid-montage on an existing save. `chapter_one_staging.gd` still contains the full still-renderer/montage-index logic, untouched.**

**Test coverage: `tests/steward_sleep_flow.gd`, `tests/day_clock_flow.gd`, `tests/dialogue_live_flow.gd`, `tests/staging_flow.gd`, `tests/town_flow.gd`. Nothing in the montage granted evidence, so nothing needed to move to the new Day 2 content; the new content has its own `GATE:`-driven pacing rather than inheriting the old montage's timing.**

## **`$84,000` Formatting — resolved**

**This was a plain text-rendering defect, not a deliberately engineered glitch: the `$` character did not render correctly in the wage-claim dialogue script. The fix removes the currency symbol from the source entirely — the amount is spelled out in words ("eighty-four thousand dollars"), with no numerals or symbol present anywhere in the corpus. All three known instances are fixed: two lines in `county_clerk.dialogue`/`odell_precinct.dialogue`, and a separately-found `NOTEBOOK:` entry in `county_clerk.dialogue`'s `clerk_ophion_1`.**

**Note: an AI-generated podcast circulated a "leaked design docs" narration describing this as an intentional, expensive UI glitch simulating real-time save-file corruption. That account is fabricated and is not a source for anything about this project.**

## **Instrumental Voice Cue System**

**The wordless instrumental voice pack is wired into every authored NPC line:**

* **`VOICE: cue_id` is authored immediately before an NPC's spoken line in the `.dialogue` source.**
* **The parser attaches that cue to the line as part of compilation.**
* **The runtime emits a `[speaker, text, voice]` triple per line.**
* **Chapter One plays the matching WAV when the dialogue card appears on screen, and stops it when the player advances past that line.**
* **A 144-entry manifest validates cue names during dialogue loading and automated content validation, catching typos or unregistered cue IDs.**
* **Instrument voice volume has its own independent mixer control, separate from music/SFX.**
* **Walter, narration, beats, object descriptions, notebook entries, and system text are intentionally silent — the voice layer characterizes NPCs the player interrogates, not the protagonist or the game's own narration.**

**The same parser and runtime is the shared foundation for Chapters Two and Three — that reuse decision is settled. What each chapter still needs to supply before its dialogue integrates: a chapter-provided protagonist identity (replacing the hardcoded Walter label) and its own audio contract. Whether that audio contract means full spoken-audio manifests, the same instrumental-cue system Chapter One uses, or something else again is a separate, still-open question — see Part Four.**

## **`WEIGHT:` on `TOPIC: default`**

**Ships in `dialogue_lang.gd`/`dialogue_runtime.gd`, covered by `tests/dialogue_lang_flow.gd`:**

* **`WEIGHT: n` (a positive number, not restricted to integers) is authored as line-level metadata inside a `TOPIC: default` block, alongside `GATE:`/`LABEL:` on that topic.**
* **Authoring on any topic other than `default` is a hard parse error — `"WEIGHT is only valid on TOPIC: default"`. A non-positive value (`WEIGHT: 0` or negative) is a second, separate parse error.**
* **Multiple `TOPIC: default` blocks are allowed, and the first eligible block controls selection. If unweighted, it wins deterministically, preserving specific-before-fallback gate cascades. If it carries `WEIGHT:`, the runtime draws only from eligible defaults that explicitly carry `WEIGHT:`; later eligible unweighted defaults remain outside the pool as deterministic fallbacks.**
* **No-immediate-repeat rule: the runtime tracks the winning default's source line per NPC (by source line, not line text; session-scoped, not saved) and excludes it from the next draw's pool, falling back to allowing a repeat only when excluding it would leave the pool empty.**
* **Defaults never appear as menu entries, weighted or not. Non-`default` topic IDs must stay unique per NPC; sharing a topic ID across different NPCs still intentionally supports `topic_count()`.**
* **Authoring guidance (`docs/DIALOGUE_AUTHORING.md`): use weighted defaults for interchangeable exhausted/ambient remarks, and keep required evidence, notes, progression, and unique information in deterministic (non-default) topics.**

## **`FORK:`/`OUTCOME:` Resolution With Recap**

**Ships in `dialogue_lang.gd`/`dialogue_runtime.gd`/`dialogue_state.gd`, covered by `tests/dialogue_template_flow.gd`:**

* **Inside a `FORK:` block, any `CHOICE:` branch may additionally contain `OUTCOME: decision_id = value_id` — both sides must be a simple identifier, or the parser raises a parse error. The chosen value is held as a pending outcome for the session and only committed to `dialogue_state.gd`'s `outcomes` dictionary once the branch finishes playing; `set_outcome()` refuses to overwrite an existing key, so a decision is immutable for the rest of the playthrough.**
* **`topic_available()` is pure `GATE:` evaluation — it does not inspect `outcome_keys`. A topic with a resolved `OUTCOME:` fork is NOT excluded from the NPC's menu.**
* **`render()` computes which of a topic's `outcome_keys` already have a recorded value (`locked_outcomes`), and when playback reaches a `FORK:` step, `_locked_fork_options()` filters the presented `CHOICE:` options down to only the one whose authored `OUTCOME:` matches what was already chosen — reopening a resolved fork's topic shows the full conversation again, with the fork menu narrowed to one option: the branch actually taken. The rejected branch cannot be reached again.**
* **Replay is genuinely free: `dialogue_state.gd`'s `record_fact()`, `discover()`, `complete_topic()`, and `set_outcome()` are all idempotent, and `commit_through()`'s `TIME:` charge is gated on `first_completion`, so replaying the locked branch fires no duplicate `NOTEBOOK:`/`EVIDENCE:` effects and charges no additional day-clock time.**
* **No separate authoring is required to get recap behavior — the same original topic replays itself, filtered. Net authoring cost for a fork opting into this is exactly `OUTCOME:` on each `CHOICE:` and nothing else.**
* **Forks using only `CHOICE:` (no `OUTCOME:`) keep their plain repeatable behavior — this stays opt-in per fork.**
* **Captain Odell has one fork that stays fully one-shot (his automatic intake conversation): it lives inside `odell.dialogue`'s `TOPIC: default` block (`GATE: NOT topic_done(odell, default)`), so it retires via the ordinary default-topic mechanism rather than an explicit `OUTCOME:`.**

## **`CHOICE:`/`FORK:` Shared Interpreter and Consequential Outcomes**

**`FORK:` choices hardcode Walter as the player speaker and remain silent in Chapter One — no voice cue plays for a fork prompt itself. `VOICE:` immediately before `FORK:` is a parse error; spoken lines inside a branch can carry normal voice cues. The project extends this interpreter for Chapters Two and Three rather than replacing it — settled — so protagonist labeling and an audio contract must become chapter-provided concerns before their dialogue is integrated; what form that audio contract takes (full spoken-audio manifests vs. Chapter One's own instrumental-cue system vs. something else) remains open — see Part Four. `OUTCOME:` supplies the persistent, once-only choice residue needed across all three chapters.**

## **Presentation Frame**

**Dialogue cards, NPC topic menus, and Walter's response menus share the warm wood-and-brass conversation frame. Physical examinations use a separate blue-gray archival frame — a deliberate visual split between "talking to someone" and "examining something." The shared card renderer keeps the Continue control anchored to the bottom of the presentation frame regardless of text length.**

## **Dialogue, Object, and Portal Authoring Systems — provenance**

**The dialogue grammar and interpreter (`dialogue_lang.gd`, `dialogue_runtime.gd`, `dialogue_state.gd`) were designed and built by the project's author working with a Claude Sonnet instance in VS Code. Codex's role extends beyond integration: live-wiring the interpreter into actual play, subsequent fixes and extensions to the interpreter itself — including schedule-alias handling in `dialogue_catalog.gd`'s `slot()`, the `VOICE:`/cue-manifest extension, and the `WEIGHT:`/`OUTCOME:` extensions above — and authoring `docs/DIALOGUE_AUTHORING.md` as the writer-facing reference.**

**Dialogue-content provenance, distinct from the interpreter/engine provenance above: the author leads dialogue content specifically — the `TOPIC:`/`NOTEBOOK:`/`EVIDENCE:` writing and editorial judgment. Antigravity executes dialogue-content work under that lead, including the Stage 2 scheduled-life population pass and missable-NPC/fork-lock audits and fixes. Perplexity provides research support for dialogue content; Copilot 365 provides ideation support. Codex's own role remains interpreter/engine-side, distinct from this dialogue-content track.**

**The object-evidence system was built by the author working with a separate Claude Sonnet session in VS Code, under the author's direct lead — the same working pattern that produced the dialogue grammar and interpreter. `docs/OBJECT_AUTHORING.md`'s own byline currently credits "Copilot," a GitHub-tooling attribution stamp applied regardless of which agent actually wrote the code, not real attribution — both the author and the implementing agent prefer it credited as Claude Sonnet (VS Code); this is a correction still owed to that doc.**

**The portal authoring system was built the same way, under the author's direct lead, mirroring exactly how the dialogue and object systems were each built.**

## **Object-Evidence Authoring System**

**Replaces the prior state, where evidence like `lodging` lived scattered across GDScript object-interaction scripts — outside the dialogue system, invisible to any dialogue-only audit, and requiring engine-scripting fluency to touch. The replacement is a flat-file-per-object grammar authored in the same declarative style as `.dialogue` files: `GATE:`/`EVIDENCE:` are identical syntax to dialogue's. It is not a 1:1 reuse of the dialogue grammar — object files carry `TAKE: item_id` for pickup/collection, a directive dialogue files don't have.**

* **Parser/runtime/state: `scripts/shared/object_lang.gd`, `object_runtime.gd`, `object_state.gd`. `object_lang.gd` has explicit `is_valid_identifier()` checks and parse-time errors for `TAG`, `TIME`, `EVIDENCE`, `TAKE`, `OUTCOME`, `NOTEBOOK`, and `OBJECT` ids.**
* **`case_state.gd` moved to schema version 10, additively: `inventory`/`object_state` fields added, old save versions 1–9 still validate.**
* **All eleven scoped hotspot ids are migrated and wired: estate's seven (`wounds`, `eight`, `knife`, `watch`, `gas`, `register`, `shoes`), town's three (`gazette`, `lodging`, `exemption`), and tunnel's one (`tunnel_record`) — deliberately excluding NPC-stand-in ids (which stay in `.dialogue` files) and tunnel's bespoke game-logic ids (which belong in neither grammar). Real content lives in `objects/estate.object`, `objects/town.object`, `objects/tunnel.object`. `chapter_one.gd::_interact()` calls `objects.interact(self, "estate", id)`; `_town_interaction()`/`_town_observation()` call `objects.interact(self, "town", id)`; `_tunnel_interaction()` calls `objects.interact(self, "tunnel", id)` — through `scripts/chapters/chapter_one_objects.gd`, mirroring the shape of `chapter_one_dialogue.gd`.**
* **FORK/OUTCOME presents live choices through `chapter_one_objects.gd::_present_fork()`/`_choose()`, which calls `ObjectRuntime.resume()`. `LABEL:` drives hotspot hover text through `sync_points()`, which overwrites `points[id]["title"]` from the label on every sync.**
* **Old hardcoded fallback content (e.g. `tunnel_record`'s prior `_cards([["THE LAST SUPPORT",...)` call) is left physically present in `chapter_one.gd` but dead/unreachable — the function returns before reaching it once the object call succeeds. Not a bug; matches the migration doc's documented pattern.**
* **Known, accepted limitation, open item in Part Four: hotspot visibility is one-way only (`sync_points()` erases a hotspot once its `GATE:` goes false, but nothing restores it if the `GATE:` later becomes true again — not a live bug today, since every current `GATE:` is one-directional by nature). The former companion item — unknown `GATE:` fields/functions silently parsing instead of raising an error — is RESOLVED: `object_lang.gd::_evaluate_cmp()` now `push_error()`s and returns false for both an unknown gate function and an unknown gate field (confirmed live, `object_lang.gd:437,443`), matching `portal_lang.gd:477,483`. See Part Four's Perplexity/Manus section.**
* **Documentation: `docs/OBJECT_AUTHORING.md` (grammar/authoring reference), `docs/OBJECT_MIGRATION_HOWTO.md` (migration recipe), `docs/ARCHITECTURE.md` (cross-cutting). Test coverage: `tests/object_template_flow.gd` (grammar/template mechanism), `tests/object_content_flow.gd` (the real shipped `.object` files and a live FORK through the adapter).**

## **Portal Authoring System**

**A third sibling to `.dialogue` and `.object`, not built on either. A travel point's defining content is *where it leads*, not what it says: alongside the same shared GATE grammar and id-cascade convention the object system established, its step vocabulary adds `GO: destination | x,y,z | yaw | flags`. Reaching a `GO` halts step processing immediately and hands control back to the caller, exactly like `FORK`, just with no choice to make — only `chapter_one_portals.gd`, never the shared runtime, actually calls `_travel()`. Content authored after a `GO` renders as arrival narration once the caller resumes via `Runtime.after_go()`.**

* **Parser/runtime/state: `scripts/shared/portal_lang.gd`, `portal_runtime.gd`, `portal_state.gd`, mirroring the object system's shape.**
* **`case_state.gd` moved to schema version 11, additively: `portal_state` added, old save versions 1–10 still validate.**
* **Ten unique portal ids are migrated and wired across five live `.portal` files: `service_entrance` and `exit` (`portals/estate.portal`, the latter locked/unlocked on `visited(almy)`); `street_precinct`, `street_almy`, `street_room`, `street_estate`, and `route_post` (`portals/town.portal`); `lounge_exit` (`portals/lounge.portal`); `route_speakeasy` (`portals/lower.portal`, phase/coat-gated, three blocks — a blocked attempt always surfaces an authored dialogue card, morning/noon-locked `CELLAR BULKHEAD` or coat-mismatch-locked `THE VIEWING SLIT`, never a toast); and `tunnel_exit` (`portals/tunnel.portal`, two destinations depending on `evidence(lower_foundation)`). A sixth file, `background_portal_template.portal`, is a documented cookbook/template containing only non-canonical `example_*` placeholder ids and contributes no live portal.**
* **`TIME:` semantics: `portal_lang.gd` always fills an omitted `TIME:` with the string `"3"`, a valid float. Every portal, whether `TIME:` is omitted (defaulting to 3) or explicitly authored (including `TIME: 0`), takes the same branch in `_perform_go()`: the amount is applied once, the first time this portal identity completes (checked via `portal_done()` before charging) — exactly like `object_runtime.gd::commit_through()`'s own `first_completion` guard for the equivalent object case.**
* **Cross-system sharing: `OUTCOME`/`flag()` read/write through the shared `dialogue_state` instance; `evidence()`/`filed()` reuse `dialogue_runtime.gd`'s helpers; `dialogue_runtime.gd` and `object_runtime.gd` both have matching `portal_done`/`portal_count` entries in `make_context()`, and `object_runtime.gd` also has `spoken_to`/`taken`. A `visited(id)` GATE function (reading `case_state.gd`'s `visited` array directly) exists because it is subtly stricter than `spoken_to(npc_id)` (true the instant a conversation merely begins, not once it fully finishes) — `service_entrance`'s gate needs the stricter sense.**
* **A small, explicitly named escape hatch (`_before_travel()`/`_extra_cards()` in `chapter_one_portals.gd`) handles the rare travel point whose post-arrival content is genuinely dynamic — computed from live state, not static prose — rather than forcing it into the flat grammar: currently `tunnel_exit`'s custody-result card (branches on which supplement was sent) and `lounge_exit`'s `state.lounge_exited` flag. Documented as a known, accepted limitation in `docs/PORTAL_AUTHORING.md`, matching the same principle the object/dialogue systems established for their own dynamic content. This is a settled design decision, not an open question.**
* **Test coverage: `tests/portal_lang_flow.gd`/`tests/portal_template_flow.gd` (parser/runtime, via `commit_through()`/`after_go()` calls) and `tests/portal_content_flow.gd` (drives every migrated route through the actual `chapter_one.gd` adapter in a live `main.tscn` instance, including `route_speakeasy` reached via a real `_travel("town", ...)` into the contiguous exterior, and a synthetic `TIME:`-bearing portal proving the once-per-identity charge).**
* **Documentation: `docs/PORTAL_AUTHORING.md`, mirroring `docs/OBJECT_AUTHORING.md`'s structure, including its own "known limitation" section for dynamic post-arrival content. `docs/ARCHITECTURE.md` and `README.md` both updated to match.**

## **Fail-Loud Guards**

**Design intent: stop broken or incomplete gate logic from letting an NPC or object go silently unresponsive — an NPC turning into an "unresponsive totem pole" — instead surfacing an obvious on-screen developer error. Implemented as one centralized check per system rather than duplicated boilerplate per file.**

* **Object-side: `chapter_one_objects.gd::interact()`/`_fail_loud()`. Fires when a hotspot the player can actually click (already present in the location's `.points`) resolves to no currently-eligible `GATE:` block. `push_error()`s and shows an on-screen "EOF ERROR — PLEASE ALERT THE DEVELOPERS" panel naming the location and object id. Lives in the chapter adapter, not the shared `object_lang.gd`/`object_runtime.gd`, because an object file having nothing eligible is often *correct* (the hotspot may simply not exist yet) — only the adapter, which already knows the hotspot is clickable, can distinguish the two cases.**
* **Dialogue-side: `chapter_one_dialogue.gd::interact()`, in the chapter adapter rather than the shared `dialogue_runtime.gd`. Fires when `Runtime.enter()` returns an empty session *and* the NPC's menu has zero eligible entries *and* the actor isn't `"odell"` (the documented exception, matching Odell's one-shot intake conversation and post-answer notebook dismissal). `push_error()`s `"DIALOGUE FAIL-SAFE: NPC '%s' has no eligible default or menu topics! (EOF Error)"` and shows a `[DEVELOPER WARNING]` / `"EOF Error: NPC '<npc_id>' has no available dialogue state. Please alert developers."` card.**
* **Documented in `docs/OBJECT_AUTHORING.md` ("Fail-loud: an unresolved hotspot is an error, not silence"), `docs/DIALOGUE_AUTHORING.md`, and `README.md`.**

## **Character and Environment Models**

**All character, cast, corpse, prop, and (partially) exterior building models are Meshy-generated, produced under the author's own artistic direction (poses, silhouettes, materials, and which real-world references to build from are the author's calls) — see `AGENTS.md` for the full credit. Two corrections are consistently required after generation, independent of subject: per-model scale (Meshy normalizes every export to a roughly uniform bounding box regardless of intended real-world size, so every model below is tuned against a known reference — Walter's height, another NPC, a doorway — before integration) and texture size (raw exports ship uncompressed 4K dual textures; production assets are downsampled to 2K, or 1K where acceptable).**

**Named characters, each with a bespoke model and its own tuned `SCALE_FACTOR` in `scripts/shared/`: Walter (1.30x, ~2.21m, `walter_model.gd`), Captain Odell (1.30x, ~2.34m, `captain_odell_model.gd`), the Gatekeeper boy (1.05x, ~1.78m), the Club Steward (1.15x, ~1.95m, `steward_model.gd`, with a custom `Clean_Glass` idle animation that reacts to whether he's speaking or being spoken to). The Coroner and Coroner's Assistant share one model (1.18x, ~2.01m, `coroner_model.gd`) across both the `assistant`/`coroners_assistant` and `coroners_assistant_morgue` actor keys.**

**Six generic archetype models (`cast_upper_man/woman`, `cast_lower_man/woman`, `cast_observer_man/woman`, 1.05x, ~1.99m, `cast_model.gd`) cover ambient and unnamed background NPCs, assigned per-NPC by a deterministic name/gender/location classifier (`cast_model.gd::archetype_for_npc()`) rather than randomly generated — this replaced the project's original primitive-geometry placeholders for every NPC not listed above. A shared murder-victim model covers the six rose-garden corpses; a shared covered-body model covers Naomi Freeman and her son.**

**Five quay/waterfront props (boat frame, cargo cluster, dock crane, dock shed, fishing boat) are integrated at `assets/models/waterfront/`, optimized from raw ~28–30MB Meshy exports to 2K JPEG textures (1.7–3.9MB each) per `docs/qa/QUAY_PROP_AUDIT_2026-09-21.md`, and scaled individually against Walter at placement.**

**Exterior building/landscape model integration is now complete across all five districts plus the estate, landed selectively rather than wholesale since the models' own folder/subject labels didn't reliably match their correct street — each pass independently confirmed by reading its own `docs/qa/*_MODEL_PASS.md`/`*_PASS.md` doc, not taken on report. Every pass follows the same collision pattern: the original primitive shell is kept, hidden, and remains the actual collision authority (`Legacy*CollisionVisuals`); the rendered model is presentation-only, added under a `Rendered*Landmarks`/`Rendered*Frontage` node; routes, labels, schedules, and interiors are explicitly unchanged in every pass.**

* **Pickman Street** (`PICKMAN_EXTERIOR_MODEL_PASS.md`): three Pickman-labeled models placed for Precinct 4, Mrs. Almy's boardinghouse, and Walter's rooms.
* **Lower district** (`LOWER_EXTERIOR_MODEL_PASS.md`): a mislabeled "Pickman house" model placed as Dwelling No. 2, chosen for its landmark silhouette and its tie to the salt mender's schedule; Dwelling No. 5 deliberately left alone so its speakeasy cellar door stays legible.
* **Waterfront** (`WATERFRONT_STOREFRONT_PASS.md`, plus the five quay/hero props above): two of three available storefront models placed as the Harbor Supply chandlery and the fish-stores front; the third was left unplaced as a deliberate choice (its footprint would need misleading collision or an unnatural stretch), also preserving some visual variation between buildings rather than making every frontage equally ornate.
* **Business district** (`BUSINESS_SCHOOLHOUSE_MODEL_PASS.md`): the schoolhouse's procedural presentation replaced with a modeled equivalent, source texture kept rather than overridden since it already matched the district's palette.
* **Upper district** (`UPPER_RESIDENCE_MODEL_PASS.md`): Residence No. 4 given a modeled house plus a restrained landscape composition (one fence/gate, two hedge sections, two tree/bush clusters) — concentrated on one residence deliberately, so maintained grounds read as a class marker rather than repeating town-wide.
* **Estate** (`ESTATE_LANDSCAPE_MODEL_PASS.md`): landscape-only, and deliberately conservative — the authored iron gate, rose-garden, and boundary hedges were all kept as-is rather than replaced, since their collision/evidence/route behavior is functional and a generated closed-gate model would misrepresent progression at that spot; two generated tree/bush clusters were added at the outer entrance walls only, clear of the gate, lodge, garden, birch grove, service door, and every evidence space.**

# **Part Four — Architecture Decisions Needed**

**`CHOICE:`/`FORK:` shared-interpreter decision is settled: Chapters Two and Three extend the same parser/runtime. Before Chapter Two dialogue lands, the hardcoded Walter choice label must move to chapter-provided protagonist identity, and a chapter-provided audio contract must be added without breaking Chapter One's instrumental cues. Whether that audio contract is full spoken-audio assets, an extension of Chapter One's own instrumental-cue system, or something else is a separate, still-open question — not decided by the shared-interpreter choice above. `OUTCOME:` already supplies the persistent, once-only choice residue needed across all three chapters.**

**Tunnel exhausted-resources fail state — still open, unspecified.**

**Conversation-time/travel-time scale mismatch — in progress; a first tuning pass has landed, not yet validated by a full playthrough. `day_clock.gd`'s `CONVERSATION_MINUTES = 30.0` still only fires for a small enumerated list of legacy story-scene conversations (`ESTATE_TALKS`/`TOWN_TALKS`), not ordinary dialogue-file topics. Regular `.dialogue` topics go through `dialogue_runtime.gd`'s `commit_through()`; as of commit `be38066` (confirmed live via `git diff`/grep, 2026-09-20), `DEFAULT_MINUTES` moved from `3.0` to `5.0`, and every corpus-wide `TIME:` value (explicit or default) was raised by 2 minutes globally, in response to a real playthrough (all estate NPCs + 2 full districts + necessary Pickman Street NPCs) still leaving the clock at midday under the old costs. `TRAVEL_MINUTES` is unchanged at `30.0`. Author's own read from subsequent spot-testing: this feels better, but no full playthrough has yet been run against it end-to-end. A further increase — `WANDER_RATE` (currently `5.0/60.0`, i.e. 5 game-minutes per real-time minute of passive free-roam) rising from 5x to 7x — is under consideration but explicitly gated on whether it's still needed after the TIME-cost change above is validated; not committed to, and no timeline attached.**

**A related, separately-motivated change: bridge activators that leap the clock forward 30 minutes now carry a 60-second real-time cooldown per bridge id, to stop a player who crosses and then immediately reverses course from double-charging the clock. `chapter_one.gd::on_bridge_crossed(bridge_id)` replaced its old same-bridge/3-second debounce with a `_bridge_cooldowns: Dictionary` keyed by bridge id against a new `BRIDGE_COOLDOWN_SECONDS = 60.0` constant. Confirmed committed (`72f159d`, "second attempt") and independently re-verified live, 2026-09-20: the diff matches exactly, the full `python tests/run_all_qa.py` pass (all 13 suites) is clean against it, and the Web export/itch.io zip were refreshed in the same commit. The author reports the cooldown behaves as intended in a manual playtest (crossing, reversing, and re-crossing within the window no longer double-charges the clock) — noted as the author's own direct play observation, not independently reproducible from this pass since a manual playtest leaves no artifact to re-check against. Still has no automated regression test targeting the cooldown behavior specifically — neither the aggregate suite nor this playtest report substitutes for one exercising the 60-second window by name.**

**A passive real-time tick also runs independently: `chapter_one.gd`'s `tick_world(delta)` calls `DayClock.advance(state, delta*DayClock.WANDER_RATE)` every frame while `page == "play"` — unchanged this pass, still 5 game-minutes per real-time minute (the candidate 7x change above, if made, applies here). `main.gd` defines three movement tiers — `WALK_SPEED = 2.15`, `BRISK_SPEED = 4.0` (the intended shipping Shift-sprint, ~1.9x walk), and `DEVELOPER_BRISK_SPEED = 10.5` (~2.6x shipping brisk, opt-in via `[F3]`'s `developer_brisk` toggle, false by default and not save-persisted). Ordinary Shift-held play now runs at the intended ship speed by default.**

**Playthrough telemetry — built and deployed. A unique per-playthrough session id (no personal data in the log payload) tags a local event log. Metrics actually captured, confirmed by direct read of `scripts/shared/playthrough_log.gd`'s event functions: real minutes from session start to the first meaningful objective (`first_objective`); real and in-game minutes per district-to-district transition (`district_transition`); distinct NPCs spoken to before each `DayClock.phase()` change (`phase_change`); each completed conversation (`conversation`); real time from Day 3's start to reaching the ending sequence (`day3_bed_reached`); and two subjective impressions via the debrief pairing below (`debrief`). Whether the player checks the Tab-menu pocket watch is not tracked — no such event exists in source; misses of scheduled NPCs aren't logged as a distinct event either, though they could in principle be reconstructed offline from the logged location/timestamp events replayed against the deterministic schedule table.**

* **Mechanically loggable events funnel through existing chokepoints: district transitions and time at `chapter_one.gd`'s `_travel()`/`tick_world()` and `DayClock`; NPCs spoken to via `state.visited`, appended once per completed conversation in `_segment_done()`; phase changes via `DayClock.phase()`.**
* **`day3_bed_reached` and the debrief no longer fire at the boardinghouse bed/`close_day` — that framing is stale, superseded by the glass-ending build (Part Two). Both now fire from `scripts/chapters/chapter_one_break.gd`, confirmed live by direct read: `day3_bed_reached()` fires from `begin()` when the glass sequence starts (line 68), and `g.staging.debrief(g)` fires from `_finish()` once the glass beat completes (line 133) — `state.finished` is already `true` by then, set in `_break_glass()` earlier in the same sequence. The event keeps its old `day3_bed_reached` name for schema/D1-column compatibility; its current meaning is "reached the final glass sequence," not "returned to bed."**
* **Debrief pairing — built, verified, hardened, and deployed: two single-tap optional questions, now shown once after the glass-ending sequence resolves rather than after the old Day 3 "close_day" bed cards (`chapter_one_staging.gd`'s `debrief()`, called from `chapter_one_break.gd::_finish()`, via `_debrief_town_feel()`/`_debrief_time_natural()`). First: a five-way choice ("Alive, and hard to fully take in" / "Confusing" / "Too large for the time given" / "Easy enough to navigate" / "Skip"). Second: Yes / No / Skip on whether time felt natural while investigating. Both answers land in one `debrief` event (`town_feel`, `time_natural`, each including `"skipped"`) via `playthrough_log.debrief()`. A `debrief_sent` guard prevents double-logging. Covered by `tests/playthrough_log_flow.gd` and `tests/debrief_flow.gd`.**
* **Transport: automatic upload, not manual download-and-send — chosen because the Web export's `user://`-written log files sit inside browser sandbox storage the author cannot retrieve directly. Tradeoff accepted: the log payload itself carries no personal data, but an ordinary web server's own access logs typically record the caller's IP address by default at the infrastructure level, separate from anything this project writes into the payload, unless the hosting is deliberately configured to avoid persistent IP logging.**
* **Hosting: `https://three-colors-worker.dejunai.workers.dev`, a Cloudflare Worker, source at `cloudflare/three-colors-worker/`. The Worker dual-writes every accepted event batch to both D1 and R2 in one `Promise.all` (`env.DB.batch(statements)` alongside `env.BUCKET_ONE.put(...)`). D1 writes are idempotent: `INSERT OR IGNORE INTO game_events (event_key, ...)`, `event_key` a SHA-256 hash of the JSON-stringified event, so a retried or duplicate batch cannot double-count. A `conversation` event type is allowlisted (`npc_id`, `topic_id`, `coat_state`, `world`, `day`, `phase`), `coat_state` restricted to `["police", "plain"]`, `phase`/`new_phase` restricted to `["morning","noon","evening","night"]`. Privacy: strict per-event-type field allowlisting, UUID-v4 session-id format validation, batch/size limits, CORS restricted to the real distribution origins (GitHub Pages, itch.io). One deliberate, narrow exception added same day (2026-09-20; see HCL (28)) to what was previously an unconditional "no request headers ever written": the Worker now stores the request's `Origin` header verbatim as `page_origin`, plus its own server-received time as `received_at` — both server-computed, never client-supplied, and neither is personal data (Origin is the distribution site/host serving the page, never an IP or user identifier). No other request header, IP, or user-agent is written anywhere. Verified end to end live: schema migrated via two `ALTER TABLE` statements, Worker redeployed, a synthetic test POST with a custom `Origin` header confirmed both new columns populate correctly, test row deleted afterward. Separately discovered investigating this: the CORS allowlist only permits the real distribution origins plus exactly `http://localhost:5173`/`http://127.0.0.1:5173` for local dev — a local Web build served from any other port (e.g. `:8060`) has its telemetry silently dropped by the browser's CORS preflight before it ever reaches the Worker, with no error surfaced to the player. This is a real, separate gap from the timestamp-offset defect below, not its cause. **Settled, same day (2026-09-20; see HCL (29)): the allowlist stays narrow, deliberately, rather than opening to any origin against a future download/self-host scenario — port `:5173` is the intended opt-in signal (anyone who wants to help is told to run their local copy on that specific port), not a technical default; any other port silently sending nothing is the correct, privacy-respecting behavior, not a bug to fix. Worth noting for accuracy: broadening CORS was never actually an IP-logging question — the Worker always sees the caller's IP as a basic property of the request regardless of CORS policy, and never writes it anywhere either way; the allowlist only governs which origins may successfully submit telemetry at all.**
* **Field normalization: real-seconds fields (`real_seconds_elapsed`, `real_seconds_since_day3_start`, `total_real_seconds`) are computed via `roundi(...)` (whole seconds) in `scripts/shared/playthrough_log.gd`, while `game_minutes_elapsed` retains `snappedf(..., 0.1)` (one decimal minute) — two different granularities on sibling fields of the same event; all D1 columns are typed `REAL` regardless.**
* **`conversation` events fire only once a dialogue segment's complete authored path finishes, from `chapter_one_dialogue.gd::_segment_done()` — opening a topic menu, abandoning a card sequence, and intermediate `FORK` segments are not counted as completed conversations. Reached either as `dialogue.start()`'s completion callback or via the resume-from-save path once `offset == segment.cards.size()`.**
* **The game side is wired into `chapter_one.gd`: `begin()`/`resume()` on new-game/load, `observe()` ticking every frame in `tick_world()` for phase/first-objective/NPC tracking, `district_transition()` at every `_travel()`, `end()` on both quit and completion; disables itself automatically under `test_mode` so QA runs don't pollute the log. Real events have been observed landing in both stores — `docs/qa/D1_TELEMETRY_PASS.md` confirms a full seven-event batch (`session_start` through `session_end`) was sent, accepted (HTTP 204), and queried back from live D1 with idempotent retry behavior confirmed; a separate pasted event batch was pulled directly from the deployed R2 bucket earlier still. Neither of those is confirmed to be an organic external-tester session end to end — the D1 pass used a reserved synthetic session id, and the R2 batch's sub-second transition timings read like a fast manual click-through rather than a real playtest. So: the pipeline is confirmed working end to end; a genuine external tester's playthrough specifically has not been confirmed to land yet.**
* **New defect, same day (2026-09-20; see HCL (27)): recorded event timestamps appear to run roughly a full day behind real time. The author identified their own recent minimal-engagement test session's D1 timestamp as off by about that margin. Traced to the client: `_log()` stamps every event with `Time.get_datetime_string_from_system(true)` at the moment it fires and `_flush()` sends it immediately with no batching delay, and the Worker only validates and stores the string verbatim rather than generating or adjusting it — so this is not ingest lag or a query-side artifact, it's the Web-exported client itself reporting the wrong time, most likely in how Godot's HTML5/WASM export resolves the UTC flag against the browser's actual clock. Not yet root-caused to the specific line, and not yet fixed. Practical impact: every timestamp currently in the pipeline should be treated as unreliable by roughly this margin until fixed — this blocks correlating telemetry sessions against itch.io's own day-by-day view/play counts, which is exactly the cross-check needed once real external cold-player sessions start arriving.**

**New defect, found 2026-09-21: `debrief` and `session_end` events are not landing in practice, despite the pipeline itself working end to end in isolation (the synthetic D1 pass immediately above). Queried live `three-colors-db` directly rather than trusting the completed-run report at face value: across the table's full history (36 distinct sessions, 2026-09-14 onward) there is exactly one `debrief` row ever recorded, from an unrelated and much shorter session, and zero `session_end` rows at all — despite the schema and the standard analytics queries (`cloudflare/three-colors-worker/queries.sql`) both being built around that event type. This includes the author's own deliberate, verified-progression speedrun-plus-survey test (session `ae190c59`, 21:22:19–21:27:23 UTC, 32 events): the session genuinely reached Day 3 morning along the real critical path — Odell's intake, Almy's `identify`/`service_work`, two steward visits, the gatehouse boy, a Day 3 townsperson — but stopped roughly 90 seconds to 2 minutes short of its own reported completion with neither `debrief` nor `session_end` recorded. Not yet root-caused: whether the client-side event simply never fires at that point in the glass-ending sequence, or fires but is dropped client-side (page unload, tab close) before its POST confirms, is still open. Until this is fixed, `day3_bed_reached`/`debrief` completion-rate analytics (Part Four, "Playthrough telemetry") cannot be trusted as representative — the pipeline logs earlier-stage events reliably but appears to systematically miss the very last stretch of a run. A fuller telemetry review is planned separately.**

**Status update, 2026-09-22: not resolved, and not yet root-caused — one counterexample complicates but does not close this. A later long local playthrough (session `59429204`, 289 events, `session_start` through `session_end` both present, `debrief` recorded `town_feel: "alive"`/`time_natural: "yes"`, reached Day 3) completed cleanly end to end. This run was on a local build rather than the published Web build and had `developer_brisk_speed` enabled — both variables absent from how a real external player would encounter the game — so it demonstrates the mechanism can work, not that the drop affecting the other 36 sessions (including the author's own verified speedrun, `ae190c59`, above) is fixed. Codex reviewed this run directly and is satisfied with its own results; that review answers "did this one session work," not "why do most sessions lose their last two events," and should not be read as resolving this item. Two concrete follow-ups now in motion: Codex has added a flag recording whenever `developer_brisk_speed` is enabled during a session, closing a real gap (there was previously no way to tell a dev-speed session apart from a normal one in the data); and Codex has been handed a specific investigation brief distinguishing the two live hypotheses — the event never firing at that point in the glass-ending sequence for most players, versus firing but being dropped client-side (page/tab teardown) before its POST resolves — plus the separate, narrower question of whether local testing has been running on a CORS-allowlisted port (`:5173`) at all, which would independently explain a local-only drop without bearing on the production one. No finding yet.**

**Missable-NPC redundant-carrier pattern — authored. A full audit performed when the corpus held 42 `.dialogue` files (the corpus has since grown to 72 files/71 NPCs; this audit has not been rerun against the current corpus and its coverage claim should be read as of that earlier snapshot) found 88 distinct `EVIDENCE:` ids; only 35 are ever checked downstream at all; twelve of those are schedule-driven, single-sourced, and load-bearing — three from the newspaper-correction thread, three from the historian/schoolteacher thread, five from the freight/quay/Naomi-sighting cluster, one standalone:**

| Evidence ID | Source NPC | Topic | Thread | Downstream refs |
| ----- | ----- | ----- | ----- | ----- |
| `gazette_correction_printed` | `gazette_editor` | `print_correction` | Newspaper correction | 3 (incl. a `filed()` ending gate) |
| `press_suppression` | `gazette_editor` | `the_omitted_two` | Newspaper correction | 3 |
| `gazette_correction_terms` | `gazette_editor` | `correction_terms` | Newspaper correction | 2 |
| `reader_omission_letter` | `schoolteacher` | `reader_letter` | Historian/schoolteacher | 3 |
| `sanitized_textbook` | `schoolteacher` | `town_founding` | Historian/schoolteacher | 2 |
| `ophion_myth_classical` | `local_historian` | `ship_origin` | Historian/schoolteacher | 2 |
| `quay_inquiry` | `quay_bookkeeper` | `unfamiliar_woman` | Freight/quay/Naomi-sighting | 3 |
| `new_bedford_letters` | `post_office_clerk` | `naomi_letters` | Freight/quay/Naomi-sighting | 2 |
| `naomi_quay_inquiry` | `quay_docker` | `naomi_sighting` | Freight/quay/Naomi-sighting | 2 |
| `estate_freight` | `quay_bookkeeper` | `estate_freight` | Freight/quay/Naomi-sighting | 2 |
| `chandlery_island_delivery` | `chandlers_boy` | `island_delivery` | Freight/quay/Naomi-sighting | 2 |
| `kessler_carriages` | `widow_kessler` | `club_standing` | (standalone) | 2 |

**All twelve rows have a committed second-source topic, each granting the identical `EVIDENCE:` id as the primary source, so every existing downstream `GATE:` opens regardless of route taken, with no `chapter_one_archive.gd` LINKS changes required. Each backup topic's `GATE:` mirrors its primary topic's own precondition with `NOT evidence(id)` in place of the primary's `NOT topic_done(...)`. The other 63 `SCHEDULE:`-sourced facts are audited and marked no-backup-needed — nothing downstream depends on them. Generic grammar/template tests pass and `dialogue_catalog_flow.gd` parses the full corpus clean, but none of those tests exercise these twelve topic IDs specifically. Confirmed: the twelve topic/evidence pairs exist in source and parse. Not yet confirmed: each one's individual `GATE:` and runtime reachability, via an actual manual playtest.**

**Pocket watch HUD button — RESOLVED. The prior text-readout open item is closed: `scripts/ui/pocket_watch_button.gd`/`pocket_watch_face.gd` now render a proper analog clock face (hands, twelve-hour dial) reading `DayClock`'s exact current day and phase, free of `TIME:` cost, driven by the same underlying `DayClock` data. Confirmed live (2026-09-20) via `tests/pocket_watch_flow.gd`, which passes clean — a focused test, not part of the maintained 13-suite `run_all_qa.py` aggregate (`tests/run_all_qa.py` doesn't reference it; that aggregate's 13 suites are the language/template/content flow tests plus the `--qa`/`--qa-town`/`--qa-loop`/`--qa-usability` integrated modes and `break_flow`).**

**Personal Effects (paper doll) UI navigation — two tweaks landed, both confirmed already committed (`1a18c2a`), not merely in progress as first described. (1) The "Return to the grounds" button moved off its own anchored position into the bottom of the existing button `VBoxContainer` in `chapter_one_archive.gd::_case_file()`, alongside the notebook/case-file/pocket-watch/flask/badge/revolver/boots buttons, rather than anchoring the window separately. (2) UI screens launched from the paper doll (notebook, case file, journal, fact views) now return to the paper doll on close instead of dropping the player back into gameplay: `chapter_one.gd` gained a `return_to_personal_effects` flag and an `_open_from_personal_effects(callback)` wrapper that sets it before opening a sub-screen; `_close()` checks the flag for `page in ["case","journal","fact","notebook"]` and re-opens `_case_file()` instead of closing straight to play, clearing the flag once consumed. Confirmed via direct `git show 1a18c2a` diff read and corroborated by a clean `tests/paper_doll_flow.gd` run, 2026-09-20.**

**Object-evidence hotspot visibility is one-way only — open. `sync_points()` erases a hotspot once its `GATE:` goes false, but nothing restores it if the `GATE:` later becomes true again. Not a live bug today, since every current `GATE:` in the shipped `.object` files is one-directional (e.g. `rose_bodies_removed`); would need addressing before any future object content relies on a hotspot reappearing.**

**NPC-to-NPC eavesdropping — a "thought-bomb" concept the author has explicitly deferred, not a priority and not being scoped or built now. One fixed, deliberately atmosphere-only eavesdropping interaction already exists (the `speakeasy_bar` overheard exchange — see Part Six), but no *general* proximity/eavesdropping system exists. Real gaps that would need closing before any general system is built: (1) no proximity-detection system exists — NPCs are plain `Node3D` primitives with no `Area3D` or collision shape, and the only "in range" logic anywhere (`chapter_one.gd`'s `_find_focus()`) is player-to-point, not point-to-point between two actors; (2) `VOICE:` cues are one-shot stingers tied to a dialogue line's on-screen appearance, not continuous or looping positional audio; (3) an eavesdropped exchange is a new shape in the dialogue DSL — two NPCs speaking to each other with no player choices and no `GATE:` gating Walter's participation, distinct from every existing `TOPIC:` block; (4) scheduling has no way today to deliberately place two specific NPCs together at one location/phase. One shape floated for later: two NPC models adjacent, a subtle looping instrument cue (trombone and violin mixed low) as a proximity sound cue, then a player-triggered eavesdropping action overhearing a short scripted exchange with Walter absent. The Mr. Whitehouse/school parent evening collision this item used to cite as a plausible first test case no longer exists — confirmed via `dialogue_catalog.gd`: Whitehouse's evening `home` resolves to `upper_house_3`, but the school parent's evening slot is now its own `upper_east_road`, a distinct location. No accidental scheduling collision currently exists to test against.**

**Voice-cue library replacement pipeline — complete for the present 144-cue architecture. `tools/build_elevenlabs_voice_library.py` converts the 228-file ElevenLabs source batch into the stable twelve-styles/three-lengths/two-takes game matrix. The broader source vocabulary remains available for later selective expansion without forcing dialogue changes now.**

**`tests/test_placement_audit.gd` parse error — RESOLVED. Confirmed live (2026-09-20): the suite now passes clean, 62 living NPCs, 0 coordinate collisions across all four construction phases. (The fix itself landed in an earlier pass; this entry replaces the stale "open" status carried forward from `docs/qa/TDD_PENDING_NOTES.md`, which was already flagged in that doc's own banner as provenance-only.)**

**`tests/town_expansion_flow.gd` — RESOLVED. Confirmed via `git log` (fix landed 2026-09-16, `1bf452c`, "town tests seperated") and a fresh live run, 2026-09-20: passes clean. The special-cased `lower`/`route_lower` lookup described in the prior entry no longer applies.**

**`tests/staging_flow.gd` — RESOLVED. The proper integrated invocation (`--qa-staging`, not launching the file directly via `--script`) passes completely: body scheduling, repeat gatehouse-boy dialogue, all three steward visits, coat gating, save migration, and tunnel continuation all confirmed. Independently re-run live this pass with the same result (`STAGING PASS: service-entry gates; rose bodies removed; birches retained on same-day revisit/load; reachable optional gardener; departed staff; boy repeat/return dialogue; enacted Day 2 and three steward visits; no repeat farming; coat gate; groundskeeper on exit; immutable notebook; legacy migration; revisits and tunnel continuation`). The prior stale-assertion concern no longer applies. Still not one of `tests/run_all_qa.py`'s 13 named suites — it needs to be run separately via `--qa-staging` — but that's a suite-list gap, not an unresolved test.**

**`tests/speakeasy_flow.gd` bar-eavesdropping assertion failure — RESOLVED. Confirmed live (2026-09-20): the bar-eavesdropping route passes cleanly, including the blocked-`route_speakeasy` dialogue-card assertions and the `page == "dialogue"` eavesdrop-start assertion that previously failed at line 60.**

**`docs/qa/` housekeeping — open, non-urgent. 30+ accumulated one-off pass-report files and several image/import pairs need a triage pass (keep as permanent record vs. archive/delete) whenever there's time.**

**Antigravity's coordinate-collision audit — RESOLVED by independent direct check, superseding the "reported" status below. `tests/test_placement_audit.gd` (see above) confirms 62 living NPCs, 0 coordinate collisions, directly verified rather than relayed. The 61-vs-62 figure is close enough to read as the same claim at slightly different corpus snapshots, and the direct check corroborates "0 collisions" either way; the specific schedule fixes originally claimed (evening-slot collisions in `school_parent.dialogue`/`schoolteacher.dialogue`, a curfew-header normalization in `local_historian.dialogue`) were not individually re-diffed this pass.**

# **Part Five — Open Production Questions**

**Ekon's fatal outcome and the preservation/denial archive mechanic are already fully specified in the Design Bible (specified since v15, retained in the current v17); what remains open is narrower: whether his final action includes lighting a sealing fuse, as distinct from the archival "sealing" the bible already describes.**

**Current narrative-canon state (condensed; the revision-by-revision account of how each item below was reached — Ward's book provenance, the six cult members' death-from-the-opening status, Ekon's age, the official-record wording, Sarah Munn's naming, and the glass-shatter beat's addition to `01`/`04` — moved to the Historical Change Log, 2026-09-20, per the "current state only" rule this document is supposed to follow):**

* **Ward's book provenance: acquired secondhand from a Boston dealer prior to posting, consistent across the Bible and both `Total-Recovery` tellings (`02`/`05`); the badge appears alongside the flask and whistle in both tunnel-find scenes.**
* **The six cult members are dead from the opening scene in all three Chapter One tellings (`01`, `04`, `08`); `08`'s investigation is posthumous, matching `01`/`04` and the Bible's Crime section.**
* **Ekon's age at the 1923 events: twenty-one and already independent in `10`, consistent with `01`/`04`/`03`/`06`'s account and `03`/`06`'s stated "forty-one years" in Chapter Three.**
* **The official record of the mother and son's deaths: named and filed, but under "transients" — consistent across the Bible, `08`, and `10`.**
* **Sarah Munn is named by name in all three Chapter One tellings (`08`, `01`, `04`); the ring-wearing grounds-crew woman and the older vanished "kitchen maid" remain deliberately unnamed, distinct figures.**
* **The glass-shattering ontological crack appears in all three tellings, but not in one shared order — confirmed by direct read of the live engine and all three story files, 2026-09-20. The live build (`scripts/chapters/chapter_one_break.gd::_play()`): color returns, then the cough (twice), then silence, then the glass breaks, then the beat ends — no mother's voice at all; this is the shipped slice's actual ending. `01`/`04` (prose): color, then the glass breaks, then sound floods in behind it — cough, then voice. `08`'s glass-shatter beat (Section V, the washstand) is a different, earlier moment in that telling, not its climax, and doesn't share this ordering question at all. The live build's silence-before-glass-and-no-voice shape is its own thing, not a dramatization of `01`/`04`'s color-glass-then-voice climax.**
* **Ekon's death in `10` is unambiguous: he has already lit the fuse and chosen collapse over retreat before the record goes dark.**
* **`dialogue/sarah_munn.dialogue` is a live, migrated NPC file (its own `chandlery_upstairs` slot, no collision with `chandlers_boy`), included in the current 71-NPC/449-topic corpus figures elsewhere in this document.**

**A minor, low-confidence inconsistency noted but not corrected: `1) No-Exit-Wound-canonical.md` has Walter pass "three of them" working the kitchen-wall hedge on arrival; the paired `the-fight` telling has him count "four of them" on a different specific afternoon ("the afternoon he finally located the pantry"). These may describe two different moments rather than the same one — left alone rather than silently changed on a guess.**

**The per-protagonist presentation scheme remains unwritten and not currently being worked on. Chapter Two/Three audio is explicitly still deferred. Chapter One has a confirmed, working instrumental-voice-cue architecture, but that is a Chapter One-scoped implementation, not a decision about Chapters Two or Three. Whether Chapter Two/Three dialogue extends the same cue system, adopts something else, or goes fully voiced for the player character remains open. The HPLHS pitch remains a long-term aspiration only.**

**Perplexity docs/code review and Manus script audit (2026-09-20) — fixes confirmed landed, independently re-verified against live source rather than trusted from either report. Both passes fed `docs/qa/PERPLEXITY_REVIEW_TODO.md`; the items below were checked directly this session:**

* **Unknown `GATE:` identifiers now fail loud in the object/portal grammars. `push_error("Unknown object gate function '%s'" % name)` / `push_error("Unknown object gate field '%s'" % name)` confirmed live at `object_lang.gd:437,443`; the equivalent `portal_lang.gd:477,483` confirmed live too. (Object-side, this was previously listed as a known limitation in Part Four above — that item is now resolved as far as the exact-identifier case goes.)**
* **Dialogue include-cycle guard confirmed live: `dialogue_runtime.gd::load_npc()` carries a `loading_stack: Array` parameter and raises `push_error("Cyclic dialogue include detected: %s" % cycle)` on re-entry.**
* **Three Manus-flagged items remain open, not independently re-verified this pass: a per-physics-frame `_find_focus()` raycasting throttle, uncached procedural meshes in `estate.gd`, and a travel-teardown-ordering runtime test. See `docs/qa/PERPLEXITY_REVIEW_TODO.md` for the full item list and current checkbox state.**

**A second Perplexity architecture audit (2026-09-20, scoped to `scripts/shared` and the three flat-file inventories) reported five findings; all independently checked against live source this pass, not taken on report. Two confirmed and fixed directly: `docs/OBJECT_AUTHORING.md` and `docs/OBJECT_MIGRATION_HOWTO.md` still told authors that unknown object `GATE:` fields/functions "still parse instead of erroring" — stale, since `object_lang.gd::_evaluate_cmp()`'s fail-loud fix (confirmed above); both docs corrected directly, independent of this TDD's own already-correct wording. One confirmed as a real, currently latent gap: `tests/audit_dialogue_ast.gd`'s field allowlist (`coat`, `day`, `phase`, `estate_complete`, `steward_ready`) is missing five fields the runtime actually registers (`dialogue_runtime.gd`'s `make_context()`: `rose_bodies_removed`, `birch_bodies_removed`, `lounge_exited`, `report`, `report_filed`) — confirmed by direct read of both files. No shipped `.dialogue` file currently gates on any of the five missing fields (confirmed by corpus search), so a live run of the audit tool reports 0 errors/0 warnings today; the gap would only surface as a false "Unknown gate field" error the day an author's `GATE:` legitimately uses one of them. Not fixed this pass — it's a test-file edit, not a docs edit, and outside this session's remit without being asked. Two more confirmed accurate as stated, no fix needed: no shipped `.dialogue` file currently uses `report_filed` (confirmed, 0 matches — the field is real and registered, just unexercised in content); and the `phase = midday`-vs-`noon` warning (`dialogue_lang.gd:479-480`) has no equivalent in `object_lang.gd`/`portal_lang.gd` (confirmed, neither file mentions `midday`) — both parsers would silently fail to match rather than warn if a `.object`/`.portal` file made the same mistake, though a corpus search found no live instance of it. The audit's one-way-visibility finding restates the already-documented Part Four item above; nothing new there.**

**Manus's full TDD-and-in-game audit (2026-09-20, against `72f159d`, both the repo and the live GitHub Pages build) — headline verdict "credible public alpha... not presentation-complete." Its two concrete corrections to v44 are both confirmed and applied: the stale "bridge cooldown uncommitted" wording (fixed above and in Part Four/Six already, independent of this audit — Manus's own external regression additionally confirmed the behavior directly: first crossing charged 30 minutes, immediate reversal charged nothing, a different bridge kept an independent cooldown), and the stale complete-town profile baseline (fixed above, this pass's own re-run matching Manus's topology figures exactly). One finding, now RESOLVED: the opening HUD toast used to read "Tab case file" though Tab opens Personal Effects with the case file nested inside; fixed same day by the author (`chapter_one.gd:267` now reads "...Tab personal effects...", committed `0592307` "Doc Sync") and re-confirmed live this pass, both by direct read of the current line and a fresh `python tests/run_all_qa.py` (all 13 suites clean). Its 13-suite-aggregate-plus-17-focused-test claim, manual Chromium playtest observations (save/continue/reload persistence, F1 pause, nested Personal Effects navigation, analog-watch behavior), and specific in-game UX findings (opening camera/iris/interaction-radius wayfinding friction, sparse blockout environments, the tunnel maintenance-copy tone note, six focused tests' benign ObjectDB shutdown warnings, Web-integrated routes exceeding a 240-second harness cap) are Manus's own direct testing and judgment calls — recorded as reported, not independently re-executed this pass, and not automatically treated as settled fact the way a re-run test or re-read source file is. Its five "genuinely unproven" items and its recommended order of work (cold playthrough first, then the cheap clarity fixes, then this TDD correction, then a stronger release gate, then backup-route/profile validation before scope expansion) match this document's own existing Part Six priorities and are not a new direction — no action needed beyond what Part Six already schedules.**

**PROPOSED, not yet decided: `walter_certainty` — an unreliable, player-leaning case file. Source: "Walter — Bible Addendum: Observers, Binary Cosmology & the Unreliable Case File" (2026-09-16), reconciled into Bible v17's Walter section as fixed canon (the waiver-as-shame framing, the cough as a real diegetic sound Walter misfiles as his dying mother's remembered rasp, the glass-shatter crack, and the deliberately unresolved dutiful-son/trapped-Ethan reading). The mechanic itself is proposed, not yet built or scheduled: a single tracked int, nudged (not gated) by investigative choice type across `.dialogue`/`.object`/`.portal` — accept-at-face-value choices push toward "dutiful son," cross-referencing/pursuing-discomfort choices push toward "trapped Ethan." Both poles hit the same fixed glass-shatter beat; only epilogue framing text differs, implemented as a presentation-layer lookup (`beat_id, certainty_band → text_variant`) sitting on top of the `GATE:`/`LINKS` engine rather than inside it, so it shouldn't need the fork/OUTCOME-lock analysis redone — but that claim comes from the addendum's own author, not from anything verified against the actual `GATE:`/`LINKS` implementation in this pass, and should be confirmed against the real engine before being treated as settled. Three open sub-questions carried over from the addendum: whether `walter_certainty` persists across chapters or resets; whether `.portal` encounters (not just `.dialogue`) should be able to move it, given portals are where Observer/Ophion material would most naturally surface; and whether a floor/ceiling extreme should unlock a third epilogue variant, or a clean two-pole split is preferable for demo scope.**

# **Part Six — Next Steps**

**Unchanged production direction: *validate and improve the first 30 minutes of investigation — discovery, social consequences, evidence linking, and a compelling next lead — before expanding production toward the chapter's climax.* Note per the author: the original 30-minute figure was always a target for that opening beat specifically, not a ceiling on the now much larger three-day slice — see the standalone pacing audit, `docs/qa/PACING_AND_CONSISTENCY_AUDIT_2026-09-20.md`, for the full corpus-growth accounting (29→71 NPCs, 135→449 topics, one day→three days plus the waterfront and the glass-break ending) behind the 45–60 minute playtest figures.**

**In priority order:**

1. **A cold playthrough of the current published build, focused on whether the opening 30 minutes' three investigative threads (Part Three) — the historian/schoolteacher chain, the steward appointment progression, and the newspaper-correction thread — actually land for a first-time player and produce a compelling next lead, now including the waterfront hub, the confirmed voice-cue system, the WEIGHT/OUTCOME dialogue-grammar extensions, and the pacing tuning in item 4 below. The prior "frozen Web build vs. newest development revision" distinction no longer applies — `main` is merged, exported, and published (GitHub Pages and itch.io); this is a diagnostic step for the current live build, not a comparison against a separately-frozen one. As of 2026-09-20, this is independently the top-ranked open item across four separate outside reviewers working from four different kinds of material — Manus (playing the live build), Grok-bot-minis (production/commercial status), Copilot 365 (the design docs, commercial/creative red-team), and DeepSeek V4 (unscoped review) — with DeepSeek going furthest and recommending development pause entirely until a cold playthrough happens. The author's own note, recorded here rather than smoothed over: there is no playtester available on demand, itch.io's own audience may or may not surface one, and the placeholder/prototype-quality 3D models are a real, observed deterrent independent of the writing or systems — a household member who would otherwise be a strong candidate playtester has declined specifically because of the current model quality. This is a genuine open constraint, not a to-do that can simply be scheduled. **Update, 2026-09-21 through 2026-09-22: this constraint has substantially narrowed since it was first recorded. Every named character, the six generic cast archetypes, the corpses, the quay/waterfront props, and — as of the 2026-09-22 pass — every district's selected exterior building/landscape models are now real, rendered Meshy-generated assets rather than placeholders or bare procedural geometry (Part Three, "Character and Environment Models"). Whether this is enough to change the declined playtester's answer has not yet been re-tested — the stated success condition below (item 2's "ask the declined household playtester again") still has not been reported as attempted. Not everything is textured or dressed to a final standard (see item 2's district texture pass below, and this section's own note that nothing about the current build is being treated as final), but the specific "placeholder/prototype-quality" deterrent as originally described is now materially addressed rather than merely narrowed.** **Update, same day (2026-09-20; see HCL (26)):** the author reported itch.io showing 5 views/4 plays in a day, which could not be corroborated against the live D1 telemetry pipeline directly (no rows from that day exist there — itch.io's own counter and this project's in-game telemetry are separate, non-overlapping measurements). Separately, a recent telemetry session initially read as a possible disengaged cold-player bounce turned out, per the author, to be their own deliberate minimal-engagement speedrun test — and the game held: the session's own event data shows exactly the compressed, low-interaction progression Design Law 13 predicts, reaching Day 3 with the causal spine intact on almost no engagement. A genuine, confirmed-not-the-author cold playthrough remains the open item.**
2. **A district texture pass, decided 2026-09-20 (Codex's proposal, author-approved, Claude Sonnet co-design input folded in below) — deliberately scoped to run alongside item 1's constraint, not instead of it. Rationale, made explicit: this is the one piece of available work that directly targets a *named* deterrent rather than generic polish — both the author's own household test case and Copilot 365's independent commercial read (Part Four) cite current placeholder/prototype model quality as a real reason a known, available person has declined to play. The working hypothesis is that this pass could turn that specific person into the actual first cold playtest, not merely make the town look nicer. Scope, as agreed: a limited, low-resolution (128–256px, coarse detail, restrained palette, visible tiling broken by variants) material family per district — Pickman Street (soot-darkened brick, worn stone, damp cobbles), business district (keep its current character as the quality bar — independently corroborated by Manus's own in-game audit calling it "already the strongest district compositionally" — add brick variation, trim, awnings, signage, repaired masonry only), upper district (pale stone/stucco, slate, dark iron, clipped greenery — elevated/maintained), lower district (patched brick, exposed timber, mismatched plaster, muddy cobbles), waterfront (salt-bleached boards, tarred timber, rusted metal, algae stains). Sequenced outward from Pickman Street on a branch: shared material library → Pickman Street → business district → upper district → lower district → waterfront. Collisions, portals, schedules, interaction points, and building identities stay untouched — this is a pure material/prop pass over existing angular geometry, not a systems change. Two amendments from design review, both agreed rather than merely suggested: (a) move the Web-profile checkpoint (originally scheduled only at the end) to run after Pickman Street specifically, not just after all five districts — cheap insurance against discovering a memory/load-time problem only after committing the same texture density to four more districts, especially since neither `docs/qa/PHASE_ONE_WEB_PROFILE_HANDOFF.md` nor a `build/web/index.pck` size comparison has been run against textured geometry yet; (b) a quick pass-consistency check of each district's chosen material palette against how the Bible/TDD already describe that district in prose, before locking it, so visual and narrative characterization don't drift apart independently. Success condition recorded explicitly: after Pickman Street lands, ask the declined household playtester again. **Update, same day:** execution diverged from the staged sequencing above — Codex is texturing all five districts concurrently rather than one at a time, on the reasoning that there are no existing textures to lose, so reverting any district that causes a problem is cheap and low-risk regardless of when it's caught. That reasoning is sound for the *risk* the staged sequencing was managing (getting stuck with bad, hard-to-undo work) — reversibility genuinely lowers that risk to near zero. It doesn't fully replace the other reason for the Pickman-first checkpoint: catching a memory/load-time problem after texturing one district is still cheaper in elapsed time than discovering it after texturing five, even when undoing either is easy — the checkpoint was about not doing avoidable extra work, not just about avoidable risk. Given the choice has already been made, the Web-profile comparison against this session's pre-texture baseline (Part Two above, `docs/qa/CONTIGUOUS_TOWN_PHASE_ONE.md`) should still happen once all five districts land, before calling the pass done — later than originally planned, but not skipped. **Landed and independently verified, same day (2026-09-20; see HCL (23)):** committed on branch `feature/district-textures`, commit `1a2b651`, via a shared procedural surface library (`scripts/shared/district_surfaces.gd`) generating 128×128 textures at runtime from a seeded RNG — confirmed by direct read to add no bitmap image assets, so this pass does not grow `index.pck`. Confirmed by independent re-execution: `tests/contiguous_town_profile.gd` returns `nodes=2859 meshes=2339 bodies=107 collisions=107 targets=68 scheduled_actors=47 static_mb=78.93`, exactly matching the pre-texture topology baseline (Part Two above) and confirming geometry/collisions/routes/NPC placement are unchanged; memory rose only ~1 MB; `tests/contiguous_town_phase_two_flow.gd` PASS; `git diff --check` clean. Not independently confirmed this pass: `tests/contiguous_town_phase_one_flow.gd` — five attempts against the live device all failed to complete (four 60-second response timeouts, one detached run that stayed alive but consumed under 0.1s of CPU over nearly two minutes before being killed), a reproducible device-side stall rather than a disconfirmed result; recorded as reported by Codex, not independently re-verified. The Web-profile comparison called for above (post-Pickman-Street originally, now post-all-five per the sequencing revision) still has not been run against the textured build — remains open. One new issue surfaced during this verification pass, unrelated to the texture work per Codex's report: `tests/town_flow.gd:159` ("Departure survives loading," an old-woman save/load assertion) fails in the broader town suite. `town_flow.gd` was not among the 11 files changed in commit `1a2b651`, consistent with Codex's "pre-existing, unrelated" framing but not conclusively traced — no prior record of this failure exists in this document's history. Flagged here as a new known-open item, status unresolved. **Confirmed in-game by the author, same day (2026-09-20; see HCL (24)):** textures render in live play with genuine material variety within each district, not just between them — a direct playtest observation, recorded as reported rather than re-derived from source. Still open: whether this specifically moves the declined household playtester (item 1 above) to try again — the stated success condition from the original decision — has not yet been reported as attempted.**
3. **Address whatever the cold playthrough (item 1) surfaces in the opening 30 minutes before touching anything past it.**
4. **Playtest the completed ElevenLabs compatibility library in context and hand-tune individual `VOICE:` assignments only where delivery clashes with the written line.**
5. **Conversation-time/travel-time scale mismatch (Part Four) — in progress. A first tuning pass landed (`be38066`: corpus-wide `TIME:` +2, `DEFAULT_MINUTES` 3→5) in direct response to real playtest telemetry; feels better in spot-testing but not yet validated by a full playthrough. A further `WANDER_RATE` increase (5x→7x) is under consideration, gated on whether it's still needed after the current change is validated — don't act on it before that.**
6. **"More life" work, five-stage sequence:**
   - **Stage 1 — the contiguous town: complete (Phase 1 + Phase 2). The Web-profile handoff task is now partially executed (2026-09-20, see Part Two above and `docs/qa/CONTIGUOUS_TOWN_PHASE_ONE.md`) against the live published build rather than a local dev-branch temp export, since the project has moved well past Phase 1 scope: load size/time and title-screen memory measured. Remaining: an in-game walk of the full town for peak memory, frame stalls, and phone-viewport behavior — feed this pre-texture baseline into item 2's post-Pickman-Street Web-profile checkpoint for comparison once textures land.**
   - **Stage 2 — populate the finished town in layers with scheduled life: in progress. 23 new scheduled ambient NPCs exist across four districts, confirmed by commit (`git log --diff-filter=A`, 2026-09-20): six on Pickman Street (`pickman_resident_01`–`06`, `c513828`), five in the upper district (`upper_companion`, `upper_delivery_boy`, `upper_driver`, `upper_gardener`, `upper_housemaid`, `08a6185`), six on the waterfront (`waterfront_cooper`, `waterfront_fish_porter`, `waterfront_ice_carrier`, `waterfront_sail_mender`, `waterfront_stevedore`, `waterfront_tally_runner`, `04c44f9`), plus the earlier six: `drayman`, `fish_smoker`, `lamplighter`, `night_owl_one`/`night_owl_two`, `speakeasy_bartender`. The broader layered population pass is further along than "not started" but not yet complete. Watch for overlap with `dialogue_catalog.gd`'s existing `SCHEDULE:`/`slot()` machinery when authoring the twelve backup routes below for any NPC this stage touches.**
   - **Stage 3 — the twelve backup routes (Part Four, "Missable-NPC redundant-carrier pattern"): authored. Revisit each backup topic's `GATE:`/`SCHEDULE:` interaction once Stage 2 lands for its source NPC, in case scheduling shifts when either route is reachable.**
   - **Stage 4 — ambient paired conversations and eavesdropping (Part Four): not built beyond the one fixed `speakeasy_bar` exception. Sequenced last so it layers onto a town whose density and investigative redundancy are already in place.**
   - **Stage 5 — the Tab-menu pocket watch: shipped and RESOLVED, including the former analog-face follow-up (Part Four) — the text readout was replaced with a proper analog clock face; nothing further open here.**
7. **Remaining test-suite items (Part Four): `tests/test_placement_audit.gd`, `tests/town_expansion_flow.gd`, `tests/speakeasy_flow.gd`, `tests/staging_flow.gd` (via `--qa-staging`), and Antigravity's coordinate-collision audit are all RESOLVED as of the 2026-09-20 verification pass.**
8. **`docs/qa/` housekeeping — non-urgent triage of accumulated one-off pass-report files.**
9. **The 60-second bridge-activator cooldown (Part Four) — committed (`72f159d`) and QA-suite/author-playtest verified as of this pass; still needs a dedicated regression test targeting the cooldown window specifically, the one open part of this item.**

**Explicitly not current priorities: the fatal-comprehension ending (the ontological break itself shipped — see below); Chapter Two's playable slice and trinket/procedural-geometry pipeline; the Chapter Two/Three audio-pipeline architecture decision; the per-protagonist presentation scheme; Ekon's sealing-fuse staging detail; the Part Seven difficulty screen and achievement registration; gamepad support and full key rebinding; the NPC-to-NPC eavesdropping mechanic (thought-bomb only, no systems built); and any further work toward the HPLHS pitch.**
