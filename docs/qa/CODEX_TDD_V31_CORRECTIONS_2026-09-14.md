# Codex's corrections to TDD v31 — merge status as of v35 (with in-place fixes, no version bump)

**2026-09-14. Reported by Codex, relayed by the author to Claude (Cowork) with the explicit instruction: record this, but do not fold it into the TDD directly.** Per the TDD's own standing rule, nothing here was written into that document until it could be independently confirmed — not taken on Codex's report alone. On 2026-09-14 the author asked for a merge pass ("go ahead and merge everything you can verify... this is our routine dance with Codex"). That pass is done: seven items were confirmed by cross-referencing the TDD's own already-source-confirmed content and merged as revision v33. A second Codex pass followed the same day, this one showing actual command output (a real `tests/dialogue_catalog_flow.gd`/`instrument_voice_flow.gd` run, live greps against source) rather than claims — a materially stronger evidentiary bar. Three more items from that pass are merged below as v34. The remaining items still need `git log`/`git show` or a direct answer from Codex — none of which this session has (no `device_bash` shell) — and stay here, unverified.

## Merged into the TDD as v33 (confirmed by cross-reference, no git/Godot needed)

- **Line 31 / Part Six item 15** — Codex said game-side telemetry wiring being "still open" (Part One's v31 entry) contradicts Part Six item 15's own account of it being built. On inspection this wasn't actually a live contradiction — the "still open" line is dated historical text describing the state as of v31, and Part Six item 15 already carried the later, correct status. No edit needed there specifically, but see the next item — the label on item 15 itself was overstated and is fixed below.
- **Line 103 (hub-and-spoke)** — MERGED. Confirmed against this document's own v25/v26/v29 entries and Part Six Stage 1, which already describe both contiguous phases as landed. Part Two's "Scope of the Current Slice" now says the geography is contiguous, not hub-and-spoke.
- **Lines 239–244 (telemetry "not started")** — MERGED. The paragraph directly contradicted the Debrief/Transport/Hosting paragraphs sitting immediately below it in the same Part. Rewritten as historical rationale for a decision now built and deployed.
- **Line 307 (no Web export)** — MERGED. Confirmed against Part Two's own Verification section, which already stated a Web export exists in-repo. Reconciled: the export exists; what's actually still missing is the profiling/measurement pass against it.
- **Line 312 ("LIVE end to end")** — MERGED. Part Six item 15's opening label is narrowed to "DEPLOYED AND TRANSPORT-TESTED," matching the caveat the paragraph already closed on (no real tester session observed yet).
- **Line 213 ("currently at its own v18")** — MERGED, reframed rather than removed. Now stated as a dated historical fact (as of 2026-09-12), not a claim about Codex's current version, which this session can't verify.
- **Line 278 (eavesdropping "no systems exist")** — MERGED. Confirmed against this document's own v29 entry, which already describes the fixed, atmosphere-only `speakeasy_bar` interaction as shipped. Both the Part Four and Part Six (Stage 4, item 13) mentions now distinguish "no general proximity system" (still true) from "no eavesdropping content at all" (false since v29).
- **Lines 105/119–120 (catalog figures)** — PARTIALLY MERGED, as a caveat rather than new numbers. This document's own later v32 audit counted 42 `.dialogue` files against these same 35-NPC/217-topic figures — an internal inconsistency confirmable by cross-reference. A caveat is now attached in both places flagging the figures as stale pending a catalog-test rerun. The actual new NPC/topic/line counts Codex reported are NOT merged — those still need the test rerun, not just a note.
- **Line 209 (`.dialogue.txt`)** — Closed 2026-09-14, per the author directly (see below) — not a TDD matter at all.

## Merged into the TDD as v34 (second Codex pass, run against real source/tests)

- **Catalog figures (35 NPCs/217 topics/455 lines)** — MERGED, with real numbers this time, not just a caveat. A real `tests/dialogue_catalog_flow.gd`/`instrument_voice_flow.gd` rerun reports 41 NPCs, 337 topics, 614 NPC lines (144-cue manifest unchanged). This also resolves the v33 caveat about the figure conflicting with the 42-file audit: 42 `.dialogue` files exist, 41 resolve to a distinct NPC. Part Two and Verification both updated.
- **New `$84,000` regression** — MERGED as a newly-opened item, not folded into the already-closed v22 item. `county_clerk.dialogue`'s `clerk_ophion_1` `NOTEBOOK:` entry still has the literal `$84,000` form; the original fix's two lines remain correctly spelled out. Flagged in Part Three, Part Four, and Part Six item 6 as open, with the fix needed spelled out.
- **Odell's one-shot intake fork location** — MERGED. Located by direct read in `odell.dialogue`'s `TOPIC: default` block (`GATE: NOT topic_done(odell, default)`) — retires via the ordinary one-shot default mechanism, not an `OUTCOME:`. Closes the previously-open "not found this pass" loose end in the v24 entry, Part Four, and Part Six item 8.
- **v31's "still open: wiring the game side" telemetry line** — NOT treated as a fresh correction. Re-checked and it's dated revision-history text describing the state as of v31 specifically, already superseded elsewhere in the same document (later entries, Part Six item 15) — not a live contradiction, just easy to misread in isolation. No edit made on this account.

## Merged into the TDD as v35 (third Codex pass — a review of v34 itself)

- **337-topic figure** — MERGED, corrected further. Already stale by the time v34 shipped it; a rerun of `tests/dialogue_catalog_flow.gd` against the currently-committed corpus reports 349 topics, matching the twelve v32 backup topics as the gap. v34's growth explanation (crediting the six new NPCs alone) is also corrected — existing-NPC dialogue expansion and the backup topics both contributed to the topic count, not the new NPCs by themselves. The 614-line/144-cue voice figures were independently reconfirmed correct and are unchanged.
- **Backup-route automated verification status** — MERGED. `tests/dialogue_lang_flow.gd` and `tests/dialogue_template_flow.gd` both now pass against the twelve backup routes. The "not yet run" language in Part Four's redundant-carrier section and Part Six items 10 and 12 is corrected to reflect this. Manual in-game verification of each individual route is still explicitly called out as outstanding — not claimed as done.
- **Line 308 (Stage 2 "not yet started")** — RESOLVED, no longer ambiguous. Codex's own review supplied the precise framing this file previously flagged as needed: some scheduled-life population already exists (the `speakeasy_bar` content, v29), but the broader, planned layered population pass has not started. Part Six item 11 now states both halves rather than the flat "not yet started."

## Further in-place fixes to v35 (round three), 2026-09-14 — includes a real source fix, not just prose

Two more items caught on review, both closed:

- **Line 31's Stage-2 evidence citation still said `speakeasy_bar`** — the same wrong evidence Part Six item 11 (line 324) had already been corrected away from. Line 31's summary paragraph was missed in that earlier fix. Now matches: the six scheduled NPCs (`drayman`, `fish_smoker`, `lamplighter`, `night_owl_one`/`night_owl_two`, `speakeasy_bartender`), not `speakeasy_bar`.
- **The `$84,000` recurrence itself was fixed, not just documented as open.** `county_clerk.dialogue`'s `clerk_ophion_1` `NOTEBOOK:` entry (line 126) was edited directly — "eighty-four thousand dollars," no `$` or numeral, matching the original fix's treatment — and committed to the device. The TDD's three status passages (the `$84,000` Formatting Item section, Part Four's status line, and Part Six item 6) are all updated from "open"/"reopened" to resolved, both instances now closed.

Also fixed this same pass, unrelated to Codex's list: two files with the same content now existed on disk (`7) Three Colors of Madness - TDD v31.md`, which this session had kept editing under its old name, and `7) Three Colors of Madness —  TDD v35.md`, apparently renamed directly by Codex partway through this process). All further edits, including this pass's, now go to the correctly-named `v35` file exclusively. The stale `v31`-named duplicate needs deleting by the author directly — this session has no shell access on the author's machine to delete files itself.

## Further in-place fixes to v35 (round two), 2026-09-14

Codex caught two more spots repeating the overclaim just fixed elsewhere: the v35 revision-summary paragraph (line 31) still said the twelve backup routes "pass" the generic tests, and Part Six item 12 (Stage 3) still said those tests "run clean against all twelve." Both corrected to match the accurate wording already used at lines 265/284/319: generic tests pass, the catalog parses the full corpus (41 NPCs/349 topics), the twelve pairs exist in source, individual gates/reachability remain untested. Codex approved v35 as final after this pair of fixes.

## In-place fixes to v35 itself (no version bump, per the author), 2026-09-14

Codex reviewed v35 and flagged three points that needed correcting within v35, not as a v36. Applied directly:

- **Stale "not yet" status at Part Four's redundant-carrier paragraph (the one preceding the shortlist table)** — this paragraph had been missed in the earlier v35 edit pass, which only updated the paragraph *after* the table. Both now say the same corrected thing.
- **Overclaimed backup-route test coverage** — the earlier v35 wording implied `dialogue_lang_flow.gd`/`dialogue_template_flow.gd` had verified the twelve backup routes specifically. Codex clarified they don't: `dialogue_lang_flow.gd` runs against its own dedicated fixtures, `dialogue_template_flow.gd` tests the authoring template mechanism — neither references any of the twelve new topic IDs. Corrected in all three places (the pre-table paragraph, the post-table paragraph, and Part Six item 10) to the accurate status: generic grammar/template tests pass; `dialogue_catalog_flow.gd` parses the full corpus clean (41 NPCs/349 topics); the twelve topic/evidence pairs exist in source; their individual gates and runtime reachability remain untested.
- **Wrong evidence cited for Stage 2 population** — Part Six item 11 had cited `speakeasy_bar` as evidence that initial scheduled-life population exists. Codex corrected this: `speakeasy_bar` is a fixed interaction (Stage 4 atmosphere content, v29), not scheduled-life population, so it isn't evidence for Stage 2 at all. The real evidence is the six scheduled NPCs already credited elsewhere in this document (Part Two's catalog-growth note): `drayman`, `fish_smoker`, `lamplighter`, `night_owl_one`/`night_owl_two`, `speakeasy_bartender`. Corrected in place.

## Still unmerged — needs `git log`/`git show` or Codex directly

- **Line 249** — commit `b39598d` ("Verify and guard player debrief") provenance. Codex says it was Codex's own work; git may record it under the repository owner's configured identity. Needs `git log`/`git show`.
- **Commit `085299d`** — cited by Codex as containing the new Web export. Needs `git log`/`git show` to confirm it's the same export Part Two's Verification section already lists as present in-repo.
- **Codex's own current TDD version number** — the v18 reference is now reframed as historical (merged above), but Codex's actual current version isn't knowable from source; needs asking Codex directly.

## Codex's own caveat

Codex says the telemetry and debrief implementation details in lines 247–253 and item 15 are otherwise accurate as written, including Worker version `87e1bd4e-9646-468a-b3be-1397534feb4b`, R2 storage, field allowlisting, debrief deduplication, and the completed-game bed guard — i.e., this isn't a wholesale rejection of that section, just the surrounding summary language elsewhere in the document.

## Before the remaining items become a TDD revision

Each item still listed as unmerged needs `git log`/`git show` for the two commit-provenance claims, a direct answer from Codex on its own current TDD version, or a direct follow-up on whether Stage 2 population has actually begun — this session still doesn't have a `device_bash` shell on the author's machine, so none of the git-dependent items could be checked either pass. Everything confirmable by cross-reference against the TDD's own already-source-confirmed content merged as v33; everything confirmable by an actual Godot/test rerun (done elsewhere, with real command output) merged as v34, 2026-09-14 — see above.
