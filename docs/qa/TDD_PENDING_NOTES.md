# Pending TDD notes — small items, held for a future revision

A running holding pen for small todos and observations that don't warrant a TDD version bump on their own. Add freely; fold into the TDD (and clear from here) only when there's a real batch of substance to write up — not one item at a time.

## Open

- **Pocket watch UI — replace digital/numeral readout with an analog clock face.** The pocket watch (Tab → Personal Effects, shipped 2026-09-14, commit `3f582c2`) currently displays day, exact twelve-hour time, and phase (Morning/Midday/Evening/Night) as text. Per the author: a digital numeral readout is anachronistic for a 1920s-set pocket watch — the fix is a proper analog clock face with hands, twelve-hour dial, showing the same underlying data the working version already computes. Purely visual; `DayClock` and the panel logic itself don't change. Same category of concern as the Stage 6 model-upgrade-pilot idea (production art standard vs. placeholder).

- **Antigravity coordinate-collision audit (2026-09-14) — three schedule fixes, reported not yet independently confirmed.** Antigravity reported finding and fixing evening-slot coordinate collisions and a curfew-header inconsistency:
  - Eliminated evening coordinate collisions by adjusting `SCHEDULE:` placement/timing for `school_parent.dialogue` and `schoolteacher.dialogue` (two NPCs whose evening positions overlapped another NPC's).
  - Normalized the night-curfew `SCHEDULE:` header in `local_historian.dialogue` to match the convention used elsewhere.
  - Reported verification: full engine run showing 61 living NPCs, 0 coordinate collisions, and the full test suite passing.
  - **Flag:** this "61 NPCs" figure has not been reconciled with the 70-NPC figure just committed in TDD v36 (from Codex's Stage 2 checkpoint, same day). Not resolving that discrepancy here — just noting it, same as prior catalog-count churn this session. As with all Codex/Antigravity reports, this is recorded as *reported*, not independently confirmed — this session has no shell access on the author's machine to re-run the audit or check the diffs directly.

- **Static placeholder NPC removed near Pickman Street (2026-09-14) — reported, tool source not stated in the relayed message.** A non-interactable static NPC mesh in `scripts/worlds/town.gd` (`person(Vector3(12,0,14),"414f42").rotation.y=-1.4`) was removed — an early placeholder figure near the morning-paper bench (`Vector3(8.5, 0, 17.3)`) with no actor target, schedule, or dialogue attached. Reported verification: `tests/town_flow.gd`, `tests/contiguous_town_phase_one_flow.gd`, `tests/contiguous_town_phase_two_flow.gd`, `tests/dialogue_template_flow.gd`, `tests/dialogue_content_flow.gd`, and `tests/estate_cleanup_flow.gd` all reported passing. Per the author, the `walkthrough.md` these tool-trace passes generate is a self-describing step-by-step record of that pass's own actions, unrelated to the game itself, and gets overwritten on each run — not a project doc, nothing to pull in. As with the item above: reported only, not independently confirmed here — no `device_bash` shell was used for this pass, and the report didn't name which tool performed it.

- **BUG (regressed) — camera jumps on random menu closures.** Reported directly by the author, 2026-09-14, not via a tool trace. Known issue: closing a menu can cause the camera to jump straight up. The known fix is to have the engine ignore the first mouse input received after returning from a menu (a stale-delta guard on menu close). Per the author, this fix was previously in place but the issue has resurfaced — meaning either the guard was reverted/lost at some point, or one mouse-input swallow isn't enough and the engine needs to ignore *several* mouse inputs immediately following menu closure, not just the first. Needs a source check (grep for the existing menu-close mouse-guard code, confirm whether it's still present and where) before deciding which of the two fixes applies. Not yet handed to Codex/Antigravity as of this note.

- **Repo housekeeping (larger-scale, not urgent) — `docs/qa/` has accumulated a lot of one-off pass-report files.** Per the author, 2026-09-14: not a disk-space problem (4TB drive, >2/3 free), just accumulated clutter worth a cleanup pass at some point — `docs/qa/` alone has 30+ standalone `.md` reports from individual passes (`DAY_CLOCK_PASS.md`, `TWINE_SCROLL_FIX.md`, `WATERFRONT_PASS.md`, etc.), several `.png`/`.png.import` pairs, and this session's own leftover `_commit_test.txt` test file (safe to delete any time). No triage done yet on what's worth keeping as a permanent record versus archiving/deleting — that's the actual task, whenever there's a slow afternoon for it.

- **`filed(lodging)` "dead gate" — RESOLVED, was never actually broken.** Per the author, 2026-09-14: `evidence(lodging)` is granted in GDScript, not in dialogue text — it fires on physical-object interaction, same mechanism as `watch`/`knife`. This confirms the hedge from this session's NotebookLM-audit follow-up (it's an external world-item pickup, not a missing `EVIDENCE:` tag) and closes the item — no dialogue-authoring fix needed.

- **MAJOR — object-evidence system being torn out and replaced (design intent clarified, in progress, 2026-09-14).** This is the item that prompted it: per the author, discovering that evidence like `lodging` lives scattered across GDScript object-interaction scripts (outside the dialogue system, invisible to any dialogue-only audit, and requiring Godot-language fluency the author doesn't have to touch) was "the final straw." **Design intent, per the author:** the replacement is not another GDScript-object system — it's a flat-file-per-object system authored in the same declarative syntax already used for `.dialogue` files, so object/evidence authoring becomes text editing instead of engine scripting. Conceptually mirrors the dialogue system closely: the object still exists in the scene script, and the new flat file attaches to it (link mechanism not yet specified); `GATE:`/`EVIDENCE:` are identical syntax to dialogue's. It is not a 1:1 reuse of the dialogue grammar, though — there will be divergent, object-specific directives not present in `.dialogue` files, the first named example being `COLLECT:` (presumably governing pickup/collection behavior for a physical item; exact semantics not yet specified as of this note). The author is having Claude Sonnet in VS Code (a separate session/agent, not this one) rip all objects out of the existing scripts and build this parallel system. This is a genuine architectural shift, not a small fix — worth a real TDD revision once there's something concrete to describe (the actual file format, parser, and object list), not a pending-notes bullet at that point. Nothing has landed yet as of this note — this is design intent only, no implementation reported to this session yet. Next report on this should be treated as the real substance to fold into a new TDD revision (see the holding-pen instruction at the top of this file) rather than added here as another bullet. **Note:** confirmed with the author that VS Code Claude Sonnet only touches code while building this (authoring the new system, not docs) — it will author *new* documentation once the system is complete, but is not to touch any existing docs (this file, the TDD, or anything else in `docs/`). So it is not, and should not become, a second writer on this file. The repeated stale/reverted-content commits seen while writing this entry were a device-bridge/sync artifact on this session's end, not a real content conflict. **Follow-up for this session:** whatever new doc(s) VS Code Claude Sonnet produces on completion should be treated as a fresh report to cross-reference and independently verify before folding anything into the TDD, same as every other tool's report this session — not taken at face value just because it's documentation rather than a chat message.

**Update 2026-09-14 — `docs/OBJECT_AUTHORING.md` has landed (still mid-build) and was independently verified.** Verified directly against source (`scripts/shared/object_lang.gd`/`object_runtime.gd`/`object_state.gd`, `case_state.gd`, `objects/background_object_template.object`, and the `tests/` directory), not taken on the doc's own word. Confirmed accurate: `case_state.gd` really is schema version 10 with `inventory`/`object_state` added additively (old save versions 1–9 still validate); the template and all three `scripts/shared/object_*.gd` files exist and match the doc's description; `estate.gd`/`town.gd`/`tunnel.gd` genuinely have zero `ObjectRuntime` references yet, confirming nothing is wired into scene code as the doc claims. One naming note, not a discrepancy: `COLLECT:` (mentioned to this session earlier) was the author's own suggestion during design discussion; the implementing agent chose `TAKE: item_id` instead — same mechanism, shorter verb, a deliberate call, not an inconsistency or a misremembering. **Update 2026-09-14 (later same day) — `tests/object_template_flow.gd` now exists.** The previously-missing test landed and was independently spot-checked: it's substantive, not a stub — walks the GATE cascade on `example_ledger` (first-look vs. already-examined) and exercises FORK/OUTCOME/TAKE on `example_knife` with a fresh case per branch. That earlier discrepancy is closed.

**Status check 2026-09-14 (later same day) — "wired up" corrected by the author to "exists."** This session initially reviewed the system on the premise that it was wired into the game; grepping `estate.gd`/`town.gd`/`tunnel.gd`/`chapter_one.gd`/`chapter_one_dialogue.gd`/`chapter_one_staging.gd` found zero `ObjectRuntime` references anywhere — nothing in actual scene code calls the new system yet. The author confirmed this was a misstatement: the correct status is that the object system (parser/runtime/state/template/tests) exists and is internally self-consistent, not that it's live in the game. **Next phase, per the author: migrate the hardcoded object content out of GDScript (`estate.gd`'s `Story.SCENES`/`Story.FACTS`/hand-written `state.xyz` conditionals, etc.) into real per-location `.object` flat files**, and only then wire the scenes to call `ObjectRuntime.is_available()`/`.enter()` in place of the old hardcoded lookups (per `OBJECT_AUTHORING.md`'s own "Placement and visibility" section, which already flagged this as future work). `objects/` currently holds only the cookbook template — no real location content has been migrated yet.

**Also currently in progress, unrelated to the object system:** a newly found bug in `tests/town_flow.gd`, per the author — being worked by VS Code Claude Sonnet as of this note. The on-disk file is still the pre-fix version; nothing to review here yet.

**Update 2026-09-14 (later same day) — `docs/OBJECT_MIGRATION_HOWTO.md` landed, migration handed to Antigravity, and the doc was independently verified clean.** Written by VS Code Claude Sonnet as the practical step-by-step recipe for moving hardcoded hotspots out of `estate.gd`/`town.gd`/`tunnel.gd` + `story.gd` + `chapter_one.gd`'s dispatch into real `objects/*.object` files. Spot-checked several specific, falsifiable claims directly against source and every one held exactly: `story.gd`'s `"wounds"` FACTS/SCENES entries match verbatim; `estate.gd::sync_staging()`'s `rose_bodies_removed` block matches exactly; `chapter_one_staging.gd`'s early-return list matches; `day_clock.gd`'s `ESTATE_TALKS`/`TOWN_TALKS` genuinely exclude all seven estate examine ids (confirms the doc's "no TIME cost today, no behavior change" claim); the `lodging` pre-check and `"examine"`-kind classification in `chapter_one.gd` both match precisely. No discrepancies found this pass — noting that positively since the last two docs each had at least one. Identifies the full migration scope precisely: estate's seven ids (`wounds`, `eight`, `knife`, `watch`, `gas`, `register`, `shoes`), town's three (`gazette`, `lodging`, `exemption`), and tunnel's one (`tunnel_record`) — explicitly excludes NPC-stand-in ids that belong in `.dialogue` files instead, and tunnel's bespoke game-logic ids that don't belong in either grammar. **The migration has already quietly begun:** `object_runtime.gd::make_context()` now has `rose_bodies_removed`/`birch_bodies_removed`/`lounge_exited` added to its exposed `fields` dict — exactly the prerequisite step the doc calls out as needed before `wounds` can be migrated. But as of this note, `objects/estate.object` does not exist yet, the `chapter_one_objects.gd` adapter the doc specifies has not been created, and `chapter_one.gd` is unchanged (no `objects.interact(...)` call anywhere) — so Antigravity has done prep only, no actual object content or scene wiring yet. Still not TDD-revision material; wait for a completion report. **Byline note, corrected per the author 2026-09-14:** the doc's own header currently credits "Copilot" — that's a GitHub Copilot tooling stamp that gets applied to all code regardless of which agent actually wrote it, not a real attribution. Per the author (who has discussed this with the implementing agent directly, though that agent's own memory of the conversation didn't persist across its own session boundary): both the author and the agent prefer this credited as Claude Sonnet in VS Code, matching what was said here. **TODO for the next real report on this system:** the doc's byline should get corrected to reflect that when there's a full completion report to fold in — this session should raise it rather than silently editing docs it doesn't own. This whole system remains pre-integration (no scene wiring) and design/architecture only — not yet a candidate for TDD revision on its own; wait for the completion report per the entry above.

**Update 2026-09-14 (later same day) — Phase 1 migration reported done; Codex found legitimate gaps, VS Code addressing them now. Reported only, no specifics yet.** Per the author: the first phase of migrating hardcoded object content into real `.object` files is complete, but Codex — reviewing independently — found gaps in that work that the author is calling legitimate (i.e. not disputing them), and VS Code Claude Sonnet is currently addressing them. No detail has been given yet on what the gaps actually are (missing ids, incorrect GATE logic, a hotspot left unmigrated, wiring left out, something else entirely) or which of the eleven scoped ids (estate's seven, town's three, tunnel's one) Phase 1 actually covered. Nothing to independently verify against source yet — there's no concrete claim here to check. **Next step for this session:** once Codex's specific findings and VS Code's fixes are reported with real detail, verify them directly against source the same as every other report this session (confirm which `.object` files actually exist under `objects/`, whether `chapter_one_objects.gd` now exists, whether `chapter_one.gd` calls `objects.interact(...)`, and what the "gaps" actually were) before folding anything into the TDD proper. Still holding-pen material, not a version bump. **Update 2026-09-14 (later same day) — Antigravity now on Phase 2 migrations.** Per the author: Antigravity has moved on to Phase 2 of the object migration (Phase 1's gap-fixing by VS Code, above, is presumably resolved or resolved enough to proceed — not explicitly stated either way). No detail yet on Phase 2's scope (which of the remaining scoped ids it covers, or whether Phase 1's Codex-flagged gaps were closed first). Nothing to verify against source yet. **Next step for this session:** same as above — wait for a report with real detail (which ids, which files, what changed) before checking source.

**Update 2026-09-14 (later same day) — INDEPENDENTLY VERIFIED against actual source via a real `git status`/`git diff --stat` this time (author enabled real shell access, not the device bridge). This is the most thoroughly confirmed update on this whole thread.** `git status` in the repo root showed genuine modified/untracked files; every claim below was checked directly against the staged file contents, not taken from any doc's word alone.

**CONFIRMED — migration is further along than any prior note here recorded: estate, town, AND tunnel are all migrated and wired.** `chapter_one.gd::_interact()` (line 375) now calls `objects.interact(self,"estate",id)`; `_town_interaction()`/`_town_observation()` (lines 750, 761) call `objects.interact(self,"town",id)`; `_tunnel_interaction()` (line 916) calls `objects.interact(self,"tunnel",id)`. `objects/tunnel.object` exists on disk (untracked, new) and was read directly — a real `OBJECT: tunnel_record` block with `GATE: always`, `NOTEBOOK:`, and `EVIDENCE: lower_foundation`, matching `docs/OBJECT_MIGRATION_HOWTO.md`'s own claim that all 11 scoped ids (estate's 7, town's 3, tunnel's 1) are "migrated and live." This supersedes every earlier "prep only, no real content yet" note above — that was accurate as of its own check, it just isn't the current state anymore. Also confirmed: the old hardcoded `_cards([["THE LAST SUPPORT",...)` call for `tunnel_record` is still physically present in `chapter_one.gd` right below the new `objects.interact()` call — but it's dead/unreachable code (the function returns before reaching it once the object call succeeds), matching the migration doc's own documented "leave the old fallback, it's harmless once dead" pattern. Not a bug, just not cleaned up yet.

**CONFIRMED — Codex's "legitimate gaps" from Phase 1 are now named with specifics, and the fixes are verified in `object_lang.gd`.** `docs/OBJECT_MIGRATION_HOWTO.md`'s own changelog line spells out what was found and fixed: FORK/OUTCOME wasn't actually presenting live choices (now confirmed live in `chapter_one_objects.gd::_present_fork()`/`_choose()`, which calls `ObjectRuntime.resume()`); `LABEL:` wasn't actually driving hover text (now confirmed in `chapter_one_objects.gd::sync_points()`, which overwrites `points[id]["title"]` from the label every sync); `INCLUDE`d files were dropping repeated cascade variants; and `OBJECT`/`TAG`/`EVIDENCE`/`NOTEBOOK` ids plus `TIME` weren't validated at parse time (now confirmed — `object_lang.gd` has explicit `is_valid_identifier()` checks and error-array entries for all of these, e.g. "TAG must be a simple identifier", "TIME must be a finite number greater than or equal to zero"). All four gaps read as genuinely resolved, not just claimed.

**CONFIRMED, with a correction to this file's own earlier speculation — the object-system fail-loud guard exists, but lives in the adapter, not the parser.** My last update guessed it would land in `object_lang.gd`/`object_runtime.gd`. It doesn't — it's in `chapter_one_objects.gd::interact()`/`_fail_loud()`: when a hotspot the player can click (`estate.points` already contains its id) turns out to have no currently-eligible GATE block, it `push_error()`s and shows an "EOF ERROR — PLEASE ALERT THE DEVELOPERS" panel naming the location and object id, instead of falling through silently. `docs/OBJECT_AUTHORING.md` now has a full "Fail-loud: an unresolved hotspot is an error, not silence" section explaining exactly why this couldn't be a per-id authored fallback the way dialogue's default-topic guard is (an object file's "nothing eligible" is often *correct* — the hotspot is meant to not exist yet — so a fallback would misfire on legitimate absence). This is a real, working, confirmed implementation, not a design intent.

**NOT CONFIRMED, possibly still incomplete — the dialogue-side fail-loud guard.** Despite the earlier note recording Antigravity's confirmation that "the parser can detect the fail state," a direct read of `scripts/shared/dialogue_runtime.gd` (the only dialogue-system file that shows as modified in `git status`) found no trace of it — no "EOF"/"ALERT THE DEVELOPERS" text, no push_error tied to an empty topic cascade in `menu()`/`enter()`/`_choose_default()`. `chapter_one_dialogue.gd`, which would be the natural place for a dialogue-side adapter guard (mirroring where the object-side guard actually landed), doesn't even show as modified in `git status`. Two possibilities, not distinguished yet: it's still genuinely in progress despite the earlier "confirmed" language, or it landed somewhere not yet checked. **Flag, don't assume finished** — this is the one piece of the whole fail-loud thread that hasn't actually been found in source.

**Discrepancy found — `docs/OBJECT_MIGRATION_HOWTO.md`'s own embedded code sample is stale.** Its §2 "current content" block quoting `chapter_one_objects.gd` in full does *not* include the fail-loud guard (`known_ids()` check, `_fail_loud()` call) that the actual current file has and that `docs/OBJECT_AUTHORING.md`'s own "Fail-loud" section correctly describes. The two docs now disagree with each other on what the adapter looks like — `OBJECT_AUTHORING.md` is current/accurate, `OBJECT_MIGRATION_HOWTO.md`'s quoted snippet is one revision behind the file it's quoting. Minor (doesn't affect the recipe's instructions, just the illustrative code block), but worth a fix whenever that doc gets touched again.

**Minor style observation, not a bug — `tunnel.object`'s speaker labels.** The `tunnel_record` block uses `THE LAST SUPPORT:` and `THE PASSAGE CONTINUES:` as literal speaker labels rather than beat brackets (`[...]`). This is valid per the grammar — `OBJECT_MIGRATION_HOWTO.md` §4 explicitly says a place/scene-setting label works fine either as a speaker line or a beat — but the same doc says it *prefers* a beat for scene-setting narration like this. Not flagging as wrong, just noting the style choice differs from the doc's own stated preference.

**Remaining known-open items, per the docs' own admission (not independently probed further):** visibility is one-way only — `sync_points()` erases a hotspot once its GATE goes false but nothing restores it if the GATE later becomes true again (not a live bug today since every current GATE, e.g. `rose_bodies_removed`, is one-directional by nature); and unknown GATE fields/functions still silently parse instead of raising an error.

**Given the scope of what's now confirmed — three full locations migrated, wired, and tested; four real gaps found and fixed; a working fail-loud guard; all independently verified against source, not just relayed — this may be the "real batch of substance" the holding-pen note at the top of this file has been waiting for.** Recommend the author decide whether this is ready to fold into an actual TDD revision (with the dialogue-side fail-loud guard's status called out as still-open) rather than staying a pending-notes bullet — that's the author's call, not something to do unilaterally here.

- **"Fail Loud"/"Fail Safe" error state — REVISED to a parser-level fix, not a per-file topic (design decision, revised 2026-09-14).** Superseded design: the author's original idea was a default no-gate topic appended to every `.dialogue` file. Per the author, on further thought this was the wrong shape — that would mean one bit of boilerplate duplicated across all ~70 dialogue files today and every future one forever. **Revised approach, confirmed by Antigravity:** the parser itself can detect the fail state (a topic cascade with nothing eligible) and issue the "EOF Error, Please report to developers"-style card directly, as one central piece of code (presumably in `dialogue_lang.gd`/`dialogue_runtime.gd`) rather than appended per-file content. Same intent as before — surface an obvious error instead of an NPC going silently unresponsive ("turning into an unresponsive totem pole") — achieved as a parser/runtime guard instead of authored content. Not yet independently confirmed against source: this is Antigravity's confirmation relayed by the author, not yet checked against the actual parser/runtime code — no diff seen, exact detection logic and error-card text/id unconfirmed. Whether this generalizes to the object system's parallel GATE cascade (`docs/OBJECT_AUTHORING.md`) is also unstated/unconfirmed. The original per-file-topic approach is superseded and should not be checked for — if a `.dialogue` file is spot-checked later and does *not* have an appended fallback topic, that is expected, not a regression. **Next step for this session:** once a completion report or diff comes through, verify the parser/runtime change directly against source before treating this as confirmed. Design decision only so far — no implementation verified — so still holding-pen, not a version bump. **Update 2026-09-14 (later same day) — same fail-loud guard being added to the object parser too, in progress.** Per the author: VS Code Claude Sonnet has also been asked to add the equivalent parser-level fail-loud/fail-safe detection to the object system's parser (`scripts/shared/object_lang.gd`/`object_runtime.gd`), and is finishing that work now. This answers the "does this generalize to the object system's GATE cascade" question left open above — yes, by author's direction, not by inference. Not yet independently confirmed: no diff seen for either the dialogue-side or object-side change yet. **Next step for this session:** once VS Code reports this done, verify both the dialogue-parser and object-parser fail-loud additions directly against source in the same pass, since they're now a matched pair. Still holding-pen, not a version bump.
